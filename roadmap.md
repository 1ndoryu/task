# task — Roadmap

> **Descripcion:** app de productividad personal (tareas, habitos, proyectos, notas) con backend Rust/Axum + PostgreSQL y el frontend React original como SPA.
> **Objetivo de paridad:** todo lo que funcionaba en WordPress debe funcionar aqui — los dominios degradados se implementan hasta alcanzar paridad completa, no se descartan.
> **Stack:** Rust (Axum, SQLx, utoipa/Orval) + React 18 (Zustand, Vite) + PostgreSQL.
> **URL produccion:** propuesta `https://task.wandori.us` (pendiente de autorización; el sitio legacy `task.nakomi.studio`/nakomi queda intacto).
> **Deploy:** Coolify (plan `Agente/planes/plan-deploy-task-wandori-us-2026-08-27.md` — pendiente de autorización de push + escrituras remotas).
> **Repositorio:** repo separado `task` (rama `main`), independiente de WANDORIUS.

## Estado

El frontend original (antes de WordPress) es el frontend real del producto, servido como SPA por el backend Rust con sesion por cookie HttpOnly + CSRF. **Paridad de datos con WordPress alcanzada (18-08-2026)**: todos los dominios que funcionaban en WordPress estan conectados a `/api` contra PostgreSQL — dashboard, tareas, proyectos, habitos (upsert + historial), scratchpad de notas + configuracion, perfil (+ cambio de contrasena), notificaciones, equipos, compartidos, actividad, notas/carpetas, mensajes/timeline (con WebSocket en tiempo real), suscripcion/trial, almacenamiento/adjuntos (subida multipart + descarga autenticada), backups (snapshot/restore), feedback (usuario + panel admin), panel de administracion (usuarios, resumen, premium/trial), cifrado E2E y tokens MCP/API. `window.gloryDashboard` es solo el contexto de sesion Rust.

## Auditoría SOLID 2026-08-25 (segunda pasada, completa)

**855/855 archivos revisados — 7/7 hallazgos resueltos ✅ — auditoría al 100%** (0 BLOQUEANTE, 0 ALTA, 2 MEDIA, 5 BAJA). Modo contraste vs la pasada 2026-08-19. **Resueltos 2026-08-25** (ver `Agente/planes/00-PLAN-RESOLUCION-2026-08-25.md`, verificados `tsc --noEmit` + `cargo check`/`test` 11/11): `H-F12-14` (16 `console.warn`→`devWarn`), `H-B03-06` (escapa wildcards ILIKE vía `repositories/escape.rs`), `H-B03-07` (notas de invariante en whitelist `format!`), `H-F13-08` (`console.warn`→`devWarn` en DashboardGrid), `H-F13-09` (drag-resize unificado en `useResizeDrag`), `H-F14-04` (hex muerto eliminado), y **`H-F12-13`** (refactor **estructural** del cluster de sync T7: mappers a `utils/mappersContrato.ts`, `useOnlineStatus`/`obtenerNonce` a `hooks/useOnlineStatus.ts`, `generateBackup` tipado vía `DatosGuardado`, helpers de `useSyncManager` a `utils/syncAyudas.ts`, 4 `@ts-ignore` del cluster eliminados; la máquina init/auto-save queda unificada por acoplamiento de guards). Auditoría **archivada** el 2026-08-25 en `Agente/archivado/auditoria-2026-08-25/` (checklist por módulo + patrones).

Segunda auditoría de principios SOLID iniciada el 2026-08-25: **855 archivos** (backend `src/` 84 + frontend `frontend/src/` 771) con checklist autocontenido por módulo + revisión transversal de **patrones generales**. La auditoría quedó archivada el 2026-08-25 en `Agente/archivado/auditoria-2026-08-25/`; la pasada previa (2026-08-19, 69 hallazgos resueltos) quedó archivada en `Agente/archivado/auditoria-2026-08-19/`. Orden: patrones → backend → frontend. Los cambios sin commitear del usuario en `components/dashboard/*` (creación rápida) se respetaron.

## Bloque actual

🔧 **En curso (07-09-2026): 079A-1 — página permanente local (autoinicio).** task corre
SIEMPRE en local en `http://127.0.0.1:4191`, sin interferir con otros agentes. Puertos
definidos en `.env` (fuente única: `PORT=4190`, `VITE_PORT=4191`, `CORS_ORIGINS=:4191`);
binario propio en `.runtime/target` (gitignored; FUERA de `C:\tmp`, que la tarea
`GloryCargoTargetCleanup` borra). Mecánica: `.freebuff/start-permanente.ps1` (idempotente,
anti-duplicado, lee puertos del `.env`, requiere pwsh 7; flag `-AbrirNavegador`
para abrir la URL en el navegador) + `stop-permanente.ps1` (ahora detiene watchdog
antes de backend/Vite) + `watchdog-permanente.ps1` (bucle cada 15s que relanza el stack
si backend o Vite mueren; PID propio en `.runtime/watchdog.pid`) +
`instalar-permanente.ps1` (acceso directo escritorio `Task (local).lnk` que
arranca/verifica y ABRE el navegador en `http://127.0.0.1:4191`, + tarea programada
`task-app-permanente` al iniciar sesión que ejecuta el watchdog — **no** abre navegador).
La tarea usa `MultipleInstances=IgnoreNew` (una sola instancia del watchdog) y
detiene la tarea existente antes de reinstalarse para evitar estado Running fantasma.
Incluye fix de desync con glory-harness-core
(069A-4): `PuertosHarness` con `web_fetch`/`navegador: None` (scheduler.rs, agente.rs) y
migración `20260907000000_agente_memoria_auditoria` (columnas `origen`/`usos`/`ultimo_uso`
en `agente_memoria`) + adaptador alineado (listar/upsert persisten metadatos). Verificado:
build `.runtime/target` OK, health 200, registro+login reales con cookies, watchdog
recupera backend muerto (PID 36344→22448, health 200 tras relanzar), ciclo
start→stop→start sin duplicar, parada manual coordinada (watchdog→backend→Vite),
acceso directo + tarea activos. Docs: `.freebuff/run.md` §"Página
permanente local" + §Watchdog permanente. Pendiente: gate GLORY-BASELINE + commit.
**Repineo Sentinel ✅ RESUELTO (10-09) — de 0.7.8/`902c45e` a 0.7.9/`a3f5607`.** Aquel bloqueo («la
suite `test:unit` del checkout agota timeouts 10s/30s en los tests git de `taskCoordinator`») **no era
presión de memoria ambiental: era un defecto de la propia suite** — `.mocharc.json` fijaba
`timeout: 10000` mientras los tests de fixture real declaran `spawnSync(..., {timeout: 60_000})`, y
`taskCoordinator`/`lease`/`gateRun` endurecían ese techo con overrides locales de 30 s. Corregido en
`.mocharc.json` y en los 3 overrides (60 s), + 180 s en las dos suites carga-intermitentes de
`taskCoordinator` y 120 s en el hijo de fixture de `lease`. Evidencia: **607 passing, 1 pending, 0
failing, exit 0**. Release **0.7.9 = `a3f5607`** (tag `v0.7.9`, publicado) con FP-S1 (clave computada),
FP-S2 (comentarios) y FP-S3 (HMAC) cerrados por regla. Detalle: `Agente/completados/tareas-2026-09-10.md`
(§Bloque 1.D).

🔧 **En curso (03-09-2026): 029A-1 — campaña cero-deuda Sentinel+VarSense.** Resolver por bloques seguros fuera del WIP del usuario: (1) import-muerto x4 ✅ aplicado + type-check PASS; (2) limite-lineas pequeños + emoji; (3) html-nativo/inline/dom ✅ (7 corregidos + fix sintaxis ModalEditorArbol, resto documentado); (4) Rust sqlx+límites ✅ (sqlx x21→disable justificado, parámetros x3→structs, agente.rs→4 submódulos; css-hardcoded x28 documentado) — sentinel 0e/88w/9h → **0e/66w/6h**, 0 errores, sin commit; (5) VarSense no-WIP ✅ (varsense 4e/436w → **0e/429w**; resto = excepciones documentadas) — sentinel invariante 0e/66w/6h, tsc PASS, sin commit. **Gate reparado ✅ (03-09):** revert 0.7.6 descartado (restore 0559576), VarSense re-pineado a V22 (tag local v2.2.1-v22 + setup 573 tests/smoke OK + lock) — `doctor` 0 issues, readyForGate:true; re-medición invariante. ⚠️ **Reabierto 09-10:** aquel «0e» se midió con Sentinel **0.7.7**; con **0.7.8** activo la regla `expect-produccion-rs` da **17 errores** reales (`.expect()` en rutas de producción, 11 archivos). VarSense sí mejora (429→**303**). **Bloque 1.A ✅ ejecutado (10-09, autorizado por usuario):** 17→**1** errores `expect-produccion-rs` (16 corregidos en 12 archivos; el restante es `glory-rs/backend/src/websocket/ticket.rs:43`, submódulo). **Gate desbloqueado por causa raíz:** el lock seguía en 0.7.7/`0559576` porque `.gitignore` `/scripts/*` ocultaba `scripts/quality/lock-generator.mjs` (nunca rastreable → lock no regenerable); vendorizado + `npm run quality:lock` + negaciones `!/scripts/quality/` → lock regenerado, `doctor` **0 issues**, `readyForGate:true`, y `quality:setup` OK con evidencia release de `902c45e` (resuelve el pendiente bloqueado desde el 07-09). ⚠️ **Sin verificación funcional:** `npm run check:back` falla por **4 errores ajenos commiteados** (adopción de `AmbitoMemoria`, ver `109A-8`); el compilador no emitió diagnósticos en los archivos tocados. Sin commit. Evidencia: `Agente/completados/tareas-2026-09-10.md`.
� **Plan maestro 039A-1 (03-09): `Agente/planes/completados/plan-cero-deuda-todos-proyectos-2026-09-03.md`** — cero-deuda en los 10 proyectos (incluye fix de herramientas ante FPs y sub-planes para riesgo). Estado (10-09): Fase 0 ✅; **Fase 1 con Bloque 1.A ejecutado** (TASKS 17→1 errores, `109A-6`), aún no cerrable por deuda Rust ajena; **Fase 3 (herramientas) ✅ cerrada con el release 0.7.9** — FP-S1/S2/S3 resueltos por regla, lo que cierra los 3 errores compartidos de `glory-rs/backend/src/websocket/ticket.rs:43` **sin tocar el submódulo** (resuelve ese frente de `109A-7`); tras la re-medición con 0.7.9 los únicos errores del área deben ser los 2 monolitos `limite-lineas-nivel-3` (sub-planes de Fase 4). Fase 2 en curso con GLORYPORT ✅; **gloryapi no puede cerrarse** (no tiene lock/setup propios → decisión de diseño pendiente, ver plan §8). Baseline Fase 0: `C:\tmp\fase0-rebaseline\` (0.7.7+V22). Tabla viva: `TABLA-sentinel-varsense-2026-09-02.md` (corte 10-09: TASKS 1e). **Fuente única del área desde 10-09:** absorbe en §9 el frente VarSense que vivía en `workspace-manager/PLAN-corregir-hallazgos-2026-09-06.md` (cerrado como frente con puntero, conservado como evidencia histórica) y registra `039A-3` (la consola del manager medía con 0.7.4 instalado mientras el gate usa 0.7.8 del checkout compartido: alineada la resolución del runtime, `sync:gate` contra el `config.d.ts` en uso y exclusión de los clones de terceros de `glory-harness/data/referencias-cli`). **Cerrado 11-09:** triaje 0.7.10 completo en 5 proyectos + gloryapi/harness/GLORYPORT (379 ítems, 0 errores, 1 fix real, TABLA final en plan §9.7); plan archivado a `Agente/planes/completados/`; evidencia en `Agente/completados/tareas-2026-09-11.md`. Hijas: coolify 119A-3 en su roadmap; REST (DIP/splits/epic) → roadmap cuando el WIP lo permita. Sin commit/push.

🔧 **Infraestructura del gate — `109A-9` ✅ completado (10-09).** El gate ya no se propaga a mano: **`npm run quality:bump`** (escritor en `workspace-manager/scripts/quality/bump.mjs`) reescribe los manifiestos de los 11 consumidores y **la certificación de release se reutiliza por `(tool, commit, scripts, entorno)`** en vez de recalcularse por proyecto — el lote bajó de **42–55 min a 6,1 min**. Los `scripts/quality/{quality-setup,lock-generator}.mjs` de este proyecto son ahora **shims de 46 líneas** al adapter único del área: un fix se aplica una vez, no nueve. Comandos: `quality:bump` (dry-run por defecto; `--write` aplica), `quality:cert:test` (11 tests), `quality:bump:test` (4 tests de regresión). Cierre obligatorio con `npm run sync:quality` + `verificar-alineacion.mjs`. **Familia B cerrada (11-09):** `RESTAURANTE` y `WANDORIUS` commiteados (gitlink + lock 0.7.9 + evidencia de release), con `sync:quality` **11/11** y `verificar-alineacion` **17 ALINEADO**; el setup de `WANDORIUS` necesitó un presupuesto explícito de staging (300 s fijos mataban la suite, que mide 344 s según carga) y se restauró una reversión no intencional del fix de `tar` de `318A-4C`. Evidencia: `Agente/completados/tareas-2026-09-10.md` (§Bloque 1.E) y plan `Agente/planes/completados/plan-certificacion-gate-compartida-2026-09-10.md` §9.

🔧 **Deuda Rust abierta en TASKS (10-09, registrada):**
- **`109A-7` — ✅ verificado y cerrado (11-09) por regla, sin tocar el submódulo.** El error `expect-produccion-rs` de `glory-rs/backend/src/websocket/ticket.rs:43` (`.expect` sobre `HmacSha256::new_from_slice`) desaparece con Sentinel **0.7.9** porque la exención FP-S3 pasó al core. **Evidencia medida contra el artefacto** (no el src): `analizarRust` del checkout 0.7.9 sobre las 3 copias reales del submódulo (`PROYECTO TASKS`, `RESTAURANTE`, `WANDORIUS`, sha256 `c5e0880f2b80`) → **0 hallazgos**, con controles que prueban que la regla no está apagada (un `.expect()` de producción real sigue reportando) y que la exención es estrecha (`hmac_key.expect()` reporta; el ámbito de tests se respeta). La garantía duradera vive upstream en `rustReglasNuevas.test.ts`, que además fija la anti-sobre-exclusión (en AES-GCM/ChaCha20 `new_from_slice` **sí** puede fallar → sigue reportando). No se editó el submódulo ni hizo falta bump de gitlink.
- **`109A-8` — CERRADA 11-09 (Global temporal):** `main` vuelve a compilar — 4 sitios adaptados (`adaptador.rs` + `agente_config.rs:17`) + 5 clippy preexistentes corregidos; `check:back` 0 errores, `npm test` 33 passed, `quality:check` PASS. Evidencia en `Agente/completados/tareas-2026-09-11.md`; plan en `Agente/planes/completados/`. **Sin commit** (WIP mixto inseparable, pendiente decisión del usuario). Aislamiento real por proyecto = fase futura.

🔧 **En curso (03-09-2026): 039A-2 — historial del agente persistente + diff acotado.** Las tarjetas de tools y la barra CONTX sobrevivían solo en memoria SSE y se perdían al recargar; el diff mostraba el archivo completo. Plan: `Agente/planes/plan-039a-2-historial-agente-2026-09-03.md` (core hunks ±3, backend sin migración vía JOIN turnos+acciones, frontend rehidrata + fix mapping misma-tool).

✅ **Resuelto (31-08-2026): refactor visual monocromo (318A-1).** Blanco y negro puro, sin
colores, sin radios, sin sombras. `variables.css` reescrito a escala de grises (2 temas:
`:root` oscuro `original` + `claro`), tema `oscuro` eliminado de `useTema` y TSX, anulación
global vía `monocromo.css` (import al final de `index.css`), 13 CSS limpiados de valores
hardcoded → tokens. Gate `GLORY-BASELINE` PASS. Evidencia:
`Agente/completados/tareas-2026-08-31.md`. Pendiente real detectado (no bloqueante): bug visual
del modal de tareas programadas (`[class*="panel"]` vs `panelAgenteTarea*`).

✅ **Resuelto (verificado 02-09-2026): paridad de sync y export.** Ambas fases implementadas y commiteadas (la deuda era solo documental): (1) **Reappear** resuelto con refresco tombstones-aware + no-clobber en `useSyncManager` (`guardadoPendienteRef`, `lastModified > lastSync`, re-check post-fetch, `aplicarTombstonesAlPayload`; commits `27cce4d`, `7506156`, `dda873d`, `e041bc6`); (2) **Export v2** (`ed27b94`): `DatosDashboardExportados` 2.0.0 con blob `extensiones` (8 stores local-only: recordatorios, grupos ejecución, plugins, time-tracker, config-usuario, grupos-tareas, grupos-FB, panel IA), backwards-compat v1.x, cableado export/import en `useDashboard.ts`; los dominios que el sync por entidad persigue (ayuno/déficit/preferencias/notas) quedan fuera del blob por diseño (el servidor es su respaldo). Verificación: `.freebuff/verify-parity.mjs` **113/0/2** + `tsc --noEmit` exit 0. Plan archivado en `Agente/planes/completados/plan-paridad-sync-export-2026-08-26.md`.

✅ **Resuelto (27-08-2026): scroll interno en modo sidebar.** Cuando el contenido del panel
superaba la altura, la barra aparecía "por fuera" (body/página). Fix CSS acotado a sidebar:
`.dashboardContenedor--sidebar` con `height:100dvh` + `overflow:hidden` (antes solo
`min-height`, crecía con el contenido), `.dashboardSidebarContenido` con `overflow:hidden`
(antes era el contenedor exterior de scroll) y scroll interno en `.dashboardPanelView
.dashboardPanelContent` (`overflow-y:auto` + `min-height:0`, patrón de `.panelAlturaFija`
del grid). Ver `Agente/completados/tareas-2026-08-27.md`.

Quedan dos frentes, ninguno bloqueado por codigo:

1. **Dominios con credenciales externas** (no implementables sin cuentas/llaves de terceros; hoy se degradan con mensaje claro): el **chat IA y la nutrición ya funcionan** vía proxy `/api/ai/chat` + `/api/ai/nutricion` con las envs del proyecto anterior (`CEREBRAS_API_KEY`, `GROQ_API*`, `DEEPSEEK_API*`; ver `.env.example` y `run.md`) — quedan externos: agent actions (WhatsApp/research), Magnific, WhatsApp, Google OAuth, grupos Facebook, pagos Stripe reales (webhooks) y servidor MCP real (el token ya se genera; falta el servidor que lo consume).
2. **Puesta en produccion** (siguiente bloque grande): definir destino (Coolify), gate de exposicion — memoria del proceso, `X-Forwarded-For`, cookies detras del proxy, `docker build`/healthcheck en CI, ingress real; mantener snapshot OpenAPI + cliente Orval sincronizado (`openapi:export` + `codegen` sin drift).

## Pendientes por dependencia

- **318A-13 — Glory Harness (núcleo de IA agnóstico extraíble, 01-09-2026)**: extraer el núcleo de IA de task (`src/agent/` + `src/services/ai.rs`) a una carpeta/repo **`glory-harness`** en `area-trabajo`, agnóstico (sin tablas `agente_*` ni `AppState`), consumible como **lib** (crate path), **CLI** (`glory-harness run`) y **daemon** (proceso de fondo SSE loopback). La **UI del plugin agente se queda en task** (decisión por defecto, ver plan §6.6). Desacople por traits (puertos): `AgentPersistence`, `WebSearchProvider`, `ProviderPort`; task implementa con sus repositorios sqlx; tools de dominio (`crear_tarea`/`crear_habito`/`crear_recordatorio`/`crear_nota`) se quedan en task registradas contra el trait. Contrato SSE (`AgenteEvento`) **invariante** — el frontend no cambia. **Revisado con supervisor-thinking (01-09): VEREDICTO VIABLE** — frontera corregida (§5.3/§6.1). Plan: `Agente/planes/plan-glory-harness-2026-09-01.md`. **Avance (02-09, autorización usuario "continúa con el último plan"):** Fases 0+1 cerradas por el otro agente (skeleton, traits, módulos movidos al núcleo — 44/44 tests core, gate glory-harness autónomo PASS); **Fase 2 (integración lib en task) COMPLETADA** por continuidad: task declara `glory-harness-core` path, se borraron los 6 módulos huérfanos de task (`context/diff/runtime/sandbox/tool/tools_archivo`), `adaptador.rs` implementa `AgentPersistence` (commit task `d9d523f`), `handlers/agente.rs`/`main.rs`/`scheduler.rs`/`tools.rs` construyen el runtime con puertos, `services/ai.rs` reducido a re-export. Evidencia Fase 2: `cargo check` task 0 errores, tests task 23 pass, tests adaptador (skills) pass, core 44/44, gate glory-harness PASS, `export-openapi` incluye `AiMessage` como schema (utoipa, commit `5d89a49`). **Gate task bloqueado por desync varsense preexistente (318A-6VAR)**: `readyForGate:false` por varsense commit `38889aa` no alcanzable + evidencia de compilación ausente; NO se forzó — verificar/realinear en 318A-6VAR antes de cerrar task. Clippy task: 7 errores **preexistentes** en archivos no-Fase 2 (`subscription.rs`, `realtime.rs`, `productivity.rs`, `admin.rs`, `storage.rs`, `agente.rs:731`), no regresión. Pendiente Fase 2: evidencia de turno SSE real con proveedor externo. Fases restantes: 3 CLI+daemon, 4 segundo consumidor.
**Fase 3 (CLI + daemon) y Fase 4 (segundo consumidor) COMPLETADAS (02-09, autorización usuario "terminalo"):** binario `glory-harness` con subcomandos `run`, `daemon`, `tools`, `doctor`; transporte NDJSON TCP en loopback multi-sesión con lock por `session_id` y token obligatorio (R2); persistencia en memoria (`PersistenciaMemoria`). Evidencia: `cargo build`/`test --workspace` 44/44, clippy limpio en cli, daemon validado end-to-end (abrir sesión → stream `AgenteEvento` → `done` → cerrar; token inválido rechazado), gate glory-harness PASS (318A-13), commit `5da99d0`. **Fase 4**: segundo consumidor `examples/consumidor-daemon.mjs` (cliente del daemon), probado end-to-end, documentado en README; sin cambios en el núcleo (commit `201af4d`). Pendientes para cierre total de 318A-13: elegir el consumidor de producción (WANDORIUS u otro; requiere autorización del usuario) y evidencia de turno SSE real con proveedor externo (clave LLM en env).
- **Extender sistema declarativo de formularios a configs restantes (31-08-2026)**: tras cerrar 318A-3 (patrones A/B/C migrados), quedan **~23 archivos de configuración fuera de alcance** (ver plan §12.1), distribuidos en 3 raíces: `components/dashboard/` (`ModalConfiguracionActividad`, `ModalConfiguracionLayout`, `ModalPlugins`, `ModalConfigDeficitCalorico`, `ConfigDeficitCalorico`), `components/configuracion/` (`ModalConfiguracionUsuario`, `ModalConfiguracionGlobal`, `ModalConfiguracionMCP`, `SeccionTokenMCP`; `global/general/`: `SeccionConfigLayout`, `SeccionConfigPreferencias`, `SeccionConfigTemas`, `SeccionConfigPerfil`, `SeccionConfigSeguridad`, `SeccionConfigMCP`, `SeccionConfigBackups`, `SeccionConfigPaneles`, `SeccionConfigPlugins`; `global/paneles/`: `SeccionConfigTareas/Habitos/Proyectos/Actividad` vía ItemToggle) y `plugins/` (`ModalConfigAgente`, `PanelAgente`, `ConfigExp`). **Avance 318A-4 (01-09-2026):** §12.7 resuelto — `ModalConfigAgente` migrado a `FormCampo`/`Radio`/`Boton` (sin iconos en títulos, sin disables, ritmo del sistema; commit PT `2876526`); los 2 `formulario-config-sin-sistema-declarativo` de SeccionConfigPerfil/ConfigDeficitCalorico eran FP del analizador (regex case-insensitive), corregido en v0.7.7 (`0559576`). Resto sin forma de campo compatible (runtime desktop, glory-rs, Canvas) queda como seguimiento explícito: **HECHO 318A-7J1 (02-09-2026)** — form "Programar tarea" de `PanelAgente` migrado: nuevo `ModalTareasProgramadas.tsx` (sistema declarativo Modal/Input/Select/Textarea/Boton, estado del formulario local, `TipoInput` ampliado con `datetime-local`), sentinel PT **106 → 100** (0e/92w/8h, −5 `html-nativo` −1 `limite-lineas`, 0 nuevos, sin disables), type-check + build exit 0, varsense 0 errores. Queda `ConfigExp`/`ConfigDeficitCalorico` como escapes documentados por layout específico. Planes: `Agente/planes/completados/plan-318a-4-publicar-cierre-2026-09-01.md` y `plan-reactivar-reglas-visuales-sentinel-2026-08-31.md` §12.
- **Mecanismo de detección de especificaciones de diseño + centralización (01-09-2026)**: para que el gate GARANTICE consistencia visual (no solo la centralización), hay que (a) **reactivar `css-hardcoded-value`** en glory-sentinel (implementación `verificarCssHardcoded` existe en `staticCssRules.ts` pero está comentada en `ruleRegistry.ts` y sin cablear en `staticAnalyzer.ts` — código muerto) → detecta ~195 hex/rgba literales en CSS del frontend (p. ej. `suscripcion.css`, `tooltip.css`, `selectorBadge.css`, `resizeHandleColumna.css`); (b) **subir `css-especificacion-diseno-local` a `warning`** (hoy `information` en `sentinel.config.json`) — es la regla que detecta "usar componente del sistema + CSS local que reimplementa su receta visual" (el error frecuente de la IA); (c) **reactivar `menu-contextual-override-diseno` y las 4 reglas `modal-*`** (implementadas y cableadas, solo `habilitada: false` en config); (d) **crear la regla nueva `formulario-config-sin-sistema-declarativo`** (no existe hoy ninguna regla que detecte un `ModalConfig*`/`SeccionConfig*` construido a mano sin `FormCampo`/`FormularioConfiguracion` — agnóstica de proyecto para que TODOS los proyectos con el sistema la usen); (e) **ampliar `hardcodedDetection.properties` de VarSense** con `padding`, `margin`, `gap`, `font-weight`, `line-height`, `width`, `height`; (f) **tokenizar literales verificados**: `SelectorIconoProyecto.tsx` (8 hex: `#888888`, `#ef4444`, `#f97316`, `#eab308`, `#22c55e`, `#3b82f6`, `#a855f7`, `#ec4899`), `configuracionTareas.css` (`padding: 2px 0`, `font-weight: 500`, `line-height: 1.2`, `height: 0.8px`, `min-width: 100px`), `modalConfigGlobal.css` (`720px`, `750px`, `90vh`, `400px`, `180px`, `letter-spacing 0.5px`, `padding: 6px`, transiciones `0.15s`/`0.2s`); (g) **caso de prueba canónico de la regla nueva**: `ModalConfigAgente` (hallazgo real de la verificación visual §12.7: títulos de sección con iconos lucide `<Route/>/<Cpu/>/<Gauge/>/...` y campos en columna con `gap: espacioS` vs `FormCampo` en filas `itemOpcionConfig` — además `modalConfigAgente.css` lleva `sentinel-disable-file css-especificacion-diseno-local`/`css-adhoc-button-style`, el lado oscuro del disable). La implementación en glory-sentinel (regla nueva + descomentar/cablear) y las reactivaciones de config requieren autorización (protocolo §6: publicar commit → alinear gitlink/lock → regenerar lock → doctor → gate); esta tarea REGISTRA la deuda. Plan: `Agente/planes/plan-reactivar-reglas-visuales-sentinel-2026-08-31.md` §13 + §14.
  **Hecho 318A-4 (01-09-2026):** (a) `css-hardcoded-value` reactivada y cableada en sentinel; (d) regla nueva `formulario-config-sin-sistema-declarativo` implementada + fix case-sensitive v0.7.7; (g) caso canónico `ModalConfigAgente` resuelto (migrado, sin disables). **Hecho 318A-7V1 (01-09-2026, autorización §6 concedida):** (b) `css-especificacion-diseno-local` subida a `warning` en `sentinel.config.json` de PT (0 hallazgos — no hay patrones `Trigger/Opcion/...` con diseño local); (c) `menu-contextual-override-diseno` reactivada a `warning` — sus 3 hallazgos son **FPs documentados del analizador** (bug de tag self-closing `<MenuContextual ... />` en `{cond && ...}` que no corta el scan: EncabezadoPerfil L110 es un `div` hermano; MenuContextual L67/L73 son los separadores canónicos propios de la propia librería) y quedan **visibles, sin disables** (prueba de línea en evidencia); las 4 reglas `modal-*` quedan **off como excepción documentada**: el contrato que validan no existe en PT (`modalAcciones`/`modalTexto` no están definidos en ningún CSS del design system, `Modal` canónico no las usa) — reactivarlas produciría ~44 FPs por contrato ausente; (e) `hardcodedDetection.properties` ampliado con `font-weight` + `line-height` (padding/margin/gap/width/height excluidos a propósito para no inundar de FPs — se documenta); (f) tokenización verificada: `padding: 2px 0` → `--dashboard-espacioXxs`, `padding: 6px` → `--dashboard-espacioS`, `transition 0.15s` → `--dashboard-transicionRapida` (equivalentes exactos, visual-neutral), clases muertas `selectorOrdenamiento` y `configGlobalTituloSeccion` eliminadas (verificadas sin referencia en tsx/ts); `colorIcono`/paleta de `SelectorIconoProyecto` quedan como **excepción de datos §13.4.6** (valor persistido del usuario, debe seguir siendo literal, no token). Resultado: sentinel PT **100 → 103** (0e/95w/8h — el +3 son exactamente los FPs documentados de menu-contextual, sin regresión de código), varsense **1474 → 1472** (0 errores, −2 clases muertas), type-check exit 0, doctor `readyForGate: true` sin issues, gate **PASS**. Commit `318A-7V1` (stage explícito; **sin push** — el árbol lleva los commits del usuario 318A-8/318A-9 sin publicar). Referencia: `Agente/planes/completados/plan-318a-4-publicar-cierre-2026-09-01.md` + `Agente/completados/tareas-2026-09-01.md`.
- **Plugin EXP / gamificación (26-08-2026)**: panel **real** con barra de vida + EXP/nivel, registrado como el resto de paneles (grid: columna 1 arriba; sidebar: entrada en SidebarMenu + render en `DashboardSidebarGrid`), activable/desactivable en configuraciones. ✅ **Panel real registrado (corrección 3, 27-08)**: reemplaza el montaje "fijo" directo en `DashboardIsland` por `registrarPanel` + `GENERADORES_PROPS` + `panelesIds: ['exp']` (ver `Agente/completados/tareas-2026-08-27.md`). Queda: dificultad automática por IA (solo con plugin activo) guardada en el payload; EXP = dificultad × importancia al completar; vida empieza en 100 y baja por incumplimientos de hábitos (derivado del historial real, hecho durable). Plan: `Agente/planes/plan-plugin-exp-2026-08-26.md`.
- **Refactor del cluster de sincronización (T7) — ✅ HECHO 2026-08-25 (verificado 2026-09-02)**: ejecutado como H-F12-13 (mappers → `utils/mappersContrato.ts`; `useOnlineStatus`/nonce → `hooks/useOnlineStatus.ts`; guards/bienvenida → `utils/syncAyudas.ts`; `useCallbacksWebSocket`/`useSyncTransport`/`generadoresPropsLigeros` extraídos; `generateBackup` tipado, 0 `@ts-ignore`; `useSyncManager` NO partido por decisión documentada de cohesión <450). Tamaños finales: `useDashboardApi` 387, `useSyncManager` 404, `useDashboardSync` 305 (+192), `useSincronizacion` 344, `useSincronizacionTiempoReal` 255, `useNotificadorCambiosWebSocket` 315, `generadoresPropsPanel` 259 (+137) — todos responsabilidad única <450 (criterio H-F12-01), API pública intacta. Evidencia: `Agente/completados/tareas-2026-08-25.md` + `Agente/planes/00-PLAN-RESOLUCION.md` (T7).
- **IA / WhatsApp / Google OAuth / Facebook grupos / Stripe real / servidor MCP**: requieren credenciales externas; elegir proveedores y crear las cuentas antes de implementar.
- **Auditoria de contrato vs WordPress (18-08-2026)**: comparados los servicios PHP originales (`App/Services`, `App/Repository`) contra backend Rust y hooks front — corregidos: ofuscacion de emails en compartidos, trial de un solo uso + degradacion a FREE con estado `expirada` al vencer, backups solo premium con intervalo 30 min y retencion 30 dias/50 max + backup automatico en el save del dashboard, validacion MIME de adjuntos, y trial ahora activa premium de verdad. Suite de regresion `verify-parity.mjs` reforzada: **59/59 asserts**. Queda revisar estados carga/vacio/error con datos masivos si molestan.
- **Gate de exposicion**: memoria, `X-Forwarded-For`, cookies detras del proxy, `docker build`/healthcheck en CI, ingress real; baseline p95 roja por memoria del host (12-08); rate limit single-replica.
- Mantener snapshot OpenAPI + cliente Orval sincronizado (`openapi:export` + `codegen` sin drift).
- **249A-1 — Rate limit en rutas POST no-auth (24-09-2026)**: sentinel `ruta-post-sin-rate-limit` (error) con **25 hallazgos** en `src/handlers/` (admin 3, agente 1, ai 3, auth 3, backup 1, collaboration 1, feedback 2, notes 2, reminders 2, shared 1, storage 1, subscription 2, timeline 3). Verificado: el rate limit existe pero cubre SOLO `auth::public_routes()` (`middleware/rate_limit.rs::auth_rate_limit` + `FixedWindowLimiter`, cuotas `AUTH_RATE_LIMIT_PER_MINUTE`/`AI_*_PER_HOUR` en config); el resto de routers no tiene límite. Evidencia: análisis sentinel 2026-09-24 15:49. Alcance: diseñar cuotas por router (reutilizar `FixedWindowLimiter`, 429 + `Retry-After` vía `AppError::TooManyRequests`) y aplicarlas donde falte; no parchear por ruta suelta. **Gate/salida:** `npm run task:check` con 0 `ruta-post-sin-rate-limit` + prueba de 429 real contra un POST no-auth. ✅ **HECHO 2026-09-24:** 25→0 `ruta-post-sin-rate-limit` (error 0), test 429+`Retry-After` real, 34/34 lib. Ver `Agente/completados/tareas-2026-09-24.md`.
- **259A-1 — Triaje warnings sentinel 0.7.13 frontend+backend (25-09-2026, en curso):** re-medición con 0.7.13 daba 20 archivos/28w+5h (14 `inline-style-prohibido`, 5 `html-nativo`, 3 `css-adhoc-button-style`, 2 `componente-artesanal`, 5 `large-interface-isp` hints, 2 `sqlite-carga-N`, 1 `componente-sin-hook-glory`, 1 `menu-contextual-override`). Fixes reales: `agente.rs`→4 helpers turno/SSE, 11 toggles→`toggle(clave)`, `useMenuCrear`/`useModalCrearRecordatorio`/`useModalDependencias` extraídos, `Input` file + `Input` oculto perfil, 4 literales→tokens monocromo; resto FPs documentados (waivers `sentinel-disable-file`/`inline-style` con `[por que]`, TabsPanel islas span, mensajes render-diff, modal creación patrón bespoke, sqlite startup/framework). `tsc` exit 0, `run-with-db check` OK, clippy 121→120. `check:front` bloqueado por boundary pre-existente `main.tsx` (`gloryDashboard`/`localStorage`). Evidencia: `Agente/completados/tareas-2026-09-25.md`.

## Notas

- **318A-5 (31-08-2026):** chat IA — 5 mejoras de UX pedidas por el usuario. (1) **Barra de uso de contexto** inferior (sobre el input) con tooltip en hover: % usado, tokens usados/máx, salida y skills (`.panelIAContextoBarra*`). (2) **Volver atrás / editar mensaje**: solo en los mensajes del USUARIO, junto a la burbuja en hover, en fila (uno al lado del otro), iconos más grandes que el texto base y solo-icono `←` / `✎` (el nombre va en el tooltip); ambos rebobinan la conversación a ese punto — `POST /api/agente/conversaciones/:id/rebobinar` con `{hastaId, editar}` (`editar` borra también el mensaje objetivo para reescribirlo, `volver` lo conserva); verificado contra BD (DELETE condicional con `USING`, propiedad por `user_id`). (3) **Bug fuente al renombrar tab**: el input del renombrado no heredaba `font-family` (heredaba la del navegador); fix `font-family: inherit` en `.panelAgenteTab*`. (4) **Bordes en tabs**: cada tab queda encerrada en su caja con `border: 1px solid var(--dashboard-bordeSutil)` + fondo de tarjeta en todos los estados; para vencer la regla global `.dashboardPanelView [class*="panel"]` (border:none !important, dashboardPanelView.css) se duplica la clase y se usa `!important` (mismo patrón que `.panelIAInputCaja`); `--activa` con `bordePrincipal`. (5) **Config de IA reorganizada con sidebar**: `ModalConfigAgente` ahora usa sidebar de navegación y el MISMO ancho/alto que el modal global (720px × 750px). El sidebar REUTILIZA las clases del modal de configuración global (`configGlobalSidebar`, `configGlobalNavGrupo`, `configGlobalNavItem` + `Boton` ghost, definidas en `modalConfigGlobal.css`) en vez de clases propias, para que ambas navegaciones sean visualmente idénticas; se eliminaron las clases `modalConfigAgenteSidebar/NavGrupo/NavItem` y los títulos de grupo (General/Comportamiento/Avanzado) por ser redundantes (cada grupo tiene una sola sección) — el nav queda con los 3 ítems planos (`configGlobalNavItem`). (6) **Separador redundante quitado** en la config global de hábitos (sección "Tolerancia de Urgencia", `SeccionConfigHabitos.tsx`): era un `.separadorOpcionesConfig` suelto al final de la sección que ya no aportaba separación. Verificado: `type-check` + `build` frontend limpios, `cargo build` OK, SQL probada en transacción rollback, CSS verificado en galería dev y en el modal real (nav con `configGlobalSidebar`, sin títulos de grupo). Gate: `check:back` sigue rojo por 12 errores clippy PREEXISTENTES de Rust 1.95 (no tocados por esta tarea).

- **318A-6VAR (01-09-2026):** bloque varsense de PT — fallbacks muertos eliminados: 109 literales dentro de `var(--token, literal)` donde el token sí existe (global `:root` en `variables.css` importada vía `dashboard/index.css`), en 8 CSS de app (`panelAgente.css`, `modalEditorArbol.css`, `dashboardVistas.css`, `ConfigExp.css`, `PanelExp.css`, `dashboardSidebarGrid.css`, `panelIA.css`, `monocromo.css`) → **valorHardcoded 149 → 43**, varsense 293→**187** (config-scope) y 1701→**1595** (all-scope), **0 errores**, sin huérfanas nuevas. Los 43 restantes son excepción documentada: 22 App.css legacy shell (valores propios `--item-surface`/`#0f172a` etc. sin token del sistema), 19 glory-core (capa framework §4.2), 2 font-size (10px/11px — tokens resuelven a 8.5/9.5px por `--dashboard-ajusteTipografia: -1.5px`, no sería visual-neutral). `cssInlineReact` (112) + `cssInlineScript` (32): verificada muestra — 100% runtime dinámico (resize %, drag, colores de prioridad, gradientes no-Tailwind) ya con `sentinel-disable inline-style-prohibido` justificado → excepción del bloque. `claseHuerfana` (1206): muestra 28 clases → 17 usadas por word-boundary + 6 vía template literals/`claseAdicional` = **~82% FP del scanner** (el dist del pin `88f281f` NO incluye el fix J-8 que vive solo en `303e7f9` en rama local no publicada); solo ~5-7 genuinamente muertas → no se borra nada a ciegas, el fix real es del analizador cuando se publique. **Desync documentado:** 8 consumidores fijan varsense `303e7f9` (J-8) pero origin/main y el checkout compartido están en `88f281f` (PT alineado); no se fuerzan pins ajenos. Verificación: `tsc --noEmit` exit 0, `quality:doctor` exit 0 con `readyForGate: true`. Pendiente: commit del bloque = decisión del usuario (dejo 8 CSS sin commitear, stage explícito cuando apruebe).

- **318A-3 F5/F6 (01-09-2026):** reglas visuales reactivadas y limpiadas. En `sentinel.config.json` se reactivaron `html-nativo-en-vez-de-componente`, `button-clase-especifica`, `componente-artesanal` y `componente-sin-hook-glory` (warning): conteo 38 → 74 → **59** (0e/53w/6h). Limpieza: renames visual-neutrales de clases internas BEM de botón (`botonIconoEncabezado__contador*` → `encabezadoContador*`, `botonPerfilContenido` → `perfilContenido`, `botonAccionIcono` → `accionIcono`); glory-core con `sentinel-disable-file` justificado (§4.2: capa de framework editorial sin design system de app); residuales documentados como excepción (17× `<Select>` deprecated §4.1 — `SelectDropdown` no existe; 4× modales/artesanales sin seam visual-neutral). Varsense: los 2 errores `variableNoDefinida` del WIP 318A-5 resueltos declarando `--dashboard-superposicionActiva`/`--dashboard-sombra` como alias de la escala en ambos temas → **293 scoped / 1701 all, 0 errores**. Desync compartido corregido (patrón F2): `.quality-tools/varsense` restaurado al pin `88f281f` (estaba en rama local no publicada `fix/308A-6-j8-clases-dinamicas`), `dist/` reconstruido desde el pin y evidencia regenerada (`quality:setup` OK) — el gate volvió a pasar. Gate **318A-3 PASS** (0e/53w/6i, exit 0). Type-check frontend exit 0. Commit/push de PT queda a decisión del usuario (árbol con sus commits 318A-5); evidencia en `Agente/completados/tareas-2026-09-01.md`.

- **318A-3 (31-08-2026):** patrón B/C cerrado localmente: `ModalConfigAgente` usa los controles canónicos conservando layout; `ConfigExp` y `ConfigDeficitCalorico` permanecen como escapes documentados por sus layouts específicos. F2 de Sentinel verificado: fix `aa606a8` preservado en rama local, checkout compartido limpio en `643353d7e9683aabe7dc6ce67d025981b4d90b29`, `out/` reconstruido desde el pin. La firma del lock se reprodujo como `sha256(git archive --format=tar HEAD) = 50ddb6c18d93e3b4bc218547cbac7cd72c6a24991c74a2b9415ef2bd6083d2d4`. `workspace-manager` doctor confirma Sentinel 0.7.5 y commit/lock/checkout/CLI alineados; quedan issues de VarSense no publicado y desyncs externos preexistentes (WANDORIUS/PT).

- **318A-4 (02-09-2026):** chat IA — 4 sub-tareas del bug del chat resueltas + modelo gratuito directo. (1) Light-mode: texto invisible corregido (mensajes en cuadro sin fondo, tokens correctos). (2) El chat ya NO pasa por gloryapi: se añade el proveedor `commandcode` (Command Code Provider API directa, `api.commandcode.ai/provider/v1/chat/completions`) con el modelo gratuito `poolside/laguna-s-2.1-free` (Laguna S 2.1 Free) — va PRIMERO en `CHAT_FALLBACK_CHAIN`; si falla, cae a glory (ruta auto → DeepSeek Flash). (3) Avatar del chat eliminado (texto puro). (4) `config_desde_guardada` ahora lee `provider`/`modelo` de la config guardada de la conversación (el selector del front los persiste); el request ya no puede sobreescribirlos. Verificado con sesión admin: turnos en `agente_turnos` registran `commandcode/poolside/laguna-s-2.1-free` (antes `glory/commandcode` por binario desactualizado). Key en `.env` (`COMMAND_CODE_API_KEY`, del vault DPAPI del usuario). Pendiente: el modelo directo dio 503 temporal (overloaded) durante la prueba; se recuperó y responde 200.

- **318A-7 (02-09-2026):** chat IA — desglose de la ventana de contexto de la conversación + botón Compactar. (1) **Backend**: nuevo evento SSE `contexto_detalle` (max_ventana, reserva_salida, system_instrucciones, definiciones_tools, mensajes, resultados_tools, total_entrada, ocupacion_pct) calculado en `src/agent/runtime.rs` con `DesgloseContexto::calcular` y emitido al inicio de cada iteración LLM; endpoint `POST /agente/conversaciones/:id/compactar` que marca `compactado=TRUE` los mensajes ≤ umbral (último mensaje user − 1) e inserta resumen system (`insertar_resumen` con `compactado=FALSE` — crítico para que `cargar_historial` lo cargue al LLM). (2) **Frontend**: `BarraContextoInferior` con tooltip en español (tokens usados/máx, %, secciones System Instructions / Tool Definitions / Messages / Tool Results con % + Reservado para respuesta) y botón Compactar con spinner; store/acciones/`usePanelAgente` cableados (`compactarTab`, `manejarCompactar`); CSS con tokens monocromo (`--dashboard-*`) y `--conAccion` (pointer-events auto). (3) **Galería dev** `/agente/visuales`: ítem 20-contexto-detallado con `fixtureContextoDetallado` (coherente 18.6%); test `.freebuff/galeria-visual.mjs` actualizado 19→20 (corregido además un fallo preexistente del test: verificaba `export function` de re-exports de mensajes.tsx). Verificación: `cargo check` OK, tests backend **30/30**, `tsc --noEmit` exit 0, test galería 9/9, verificación visual en navegador (tooltip con todas las secciones + botón clicable). Pendiente: prueba de integración del endpoint con turno real (solo tests unitarios); `listar_mensajes` no filtra compactado (el frontend sigue mostrando el historial completo tras compactar, solo se reduce el contexto del LLM). Evidencia: `Agente/completados/tareas-2026-09-01.md`.

- **318A-8 (02-09-2026):** chat IA — la config del agente (modelo/modo y todas las configuraciones) se guarda y recarga POR CONVERSACIÓN. **Causa raíz**: el selector del input y el modal leían la config **global** del store (localStorage), pero cada conversación guarda su propia config en el servidor; con varias conversaciones, al recargar la tab activa mostraba la config de otra. **Fix (frontend, `store.ts`)**: la config de la conversación activa es la fuente de verdad — helper `configDeTab`; `cargarConversaciones`/`abrirTab`/`cerrarTab` sincronizan la config global con la tab activa; `establecerConfig` parte de la config de la tab activa. Backend intacto (ya persiste `config` + columna `modo` desde 318A-4). Verificado en navegador con 2 conversaciones de configs distintas: recarga muestra la config correcta de la tab activa, cambio de tab actualiza el selector, cambio de modelo persiste tras recargar, modal coherente. `type-check` + `build` exit 0. Evidencia: `Agente/completados/tareas-2026-09-01.md`.

- **318A-9 (02-09-2026):** menús contextuales aparecían lejos del cursor (bug global). **Causa raíz**: `MenuContextual`/`MenuFlotante` usan `position: fixed` con coordenadas del viewport pero se montaban **inline**; el wrapper de panel del sidebar `.dashboardSidebarPanel--visible` (`DashboardPanelView.tsx`) aplica `transform: translateY(0)` (y `--entrando` `translateY(4px)`), y un transform en ancestro crea un containing block que rompe el `fixed` → el menú se desplazaba (medido: `left/top` correctos pero `getBoundingClientRect` desviado ~13px X / ~68px Y). Afectaba a casi todos los menús montados dentro de paneles. **Fix (frontend)**: `MenuContextual` y `MenuFlotante` ahora se renderizan con `createPortal(menu, document.body)` (mismo patrón que BottomSheet/Modal/SelectorGrupo); los **submenús no se portaléan** (usan `position:absolute` relativo a `.menuContextualItemWrapper`; portalearlos los rompía). No se pierde tema (`--dashboard-*` en `:root`). Verificado en navegador: menú de tarea/hábito con **offset 0,0 del cursor** (antes ~68px Y), submenú a la derecha del menú raíz, menú de usuario junto al botón, cierre por Escape/click fuera OK. `type-check` + `build` exit 0. Evidencia: `Agente/completados/tareas-2026-09-01.md`.

- **318A-11 (02-09-2026):** catálogo de modelos coherente por proveedor (gloryapi real) + selector de razonamiento en el input. **Contexto**: el selector de modelos no organizaba por proveedor y mostraba modelos que el usuario no tiene (groq/*, cerebras/*); pedía SOLO modelos reales de gloryapi agrupados por proveedor + selector de razonamiento TAMBIÉN en el input del panel. **Backend (`src/services/ai.rs`)**: allowlist de `glory` ampliada con los modelos reales de `/v1/models` de gloryapi local (`auto`, `deepseek-v4-flash*`, `deepseek-ai/*`, `deepseek/deepseek-v4-flash`, `meta/muse-spark-1.2-contributor`, `stealth/ox-alpha` + aliases legacy); `modelo_proveedor("glory",…)` mapea `commandcode`/`glm-5.3-flash`/`auto` → `deepseek/deepseek-v4-flash`; guard de `reasoning_effort` (stream y no-stream) ahora incluye `glory` (gloryapi SÍ lo acepta, verificado 200 con `reasoning_content`). **Frontend**: `MODELOS_AGENTE` rediseñado con `modelo`/`proveedor`/`grupo`/`razonamiento` en 3 grupos (Glory API 10 / Command Code Provider / DeepSeek directo); helper `entradaModelo()` desambigua IDs repetidos; `ControlesInputIA` usa submenús por proveedor + selector de razonamiento (Bajo/Medio/Alto, deshabilitado con tooltip si el modelo no lo soporta); `ModalConfigAgente` agrupa por proveedor y elimina el selector manual de proveedor (el modelo lo determina, evita combos inválidos); emoji del selector de razonamiento eliminado (petición del usuario). **Verificación**: `tsc --noEmit` 0, build backend OK, navegador (3 grupos + submenús + selector razonamiento + modal 12 opciones), turno real con "Auto (recomendado)" → respondió "OK"; BD: `config.modelo=auto`, `config.provider=glory`, `config.nivel_razonamiento=medium`. Pendiente: gate bloqueado por desync varsense preexistente (318A-6VAR) — NO se forzaron pins. Evidencia: `Agente/completados/tareas-2026-09-01.md`.

- **318A-12 (02-09-2026):** rutas completas de archivo en los resúmenes de tools (file_read/write/patch). **Contexto**: al editar/crear un archivo, la tarjeta de tool mostraba la ruta relativa cortada con "…". **Backend**: `SandboxArchivos::ruta_presentable` (quita el prefijo verbatim `\\?\` de `canonicalize` en Windows) y resúmenes de `file_read`/`file_write`/`file_patch` con la ruta absoluta (`lectura C:\Users\...\test_prueba.md (167 bytes)`); el diff conserva la ruta relativa. **Frontend**: `.panelAgenteHerramientaTexto` sin ellipsis (wrap con `overflow-wrap: anywhere` + `min-width: 0` + `flex: 1`); fixture de galería con ruta completa. **Verificación**: tests backend 54/54, `tsc --noEmit` 0, galería 9/9, navegador (chat real con `file_read` muestra la ruta completa en 2 líneas sin desbordar). Pendiente: gate bloqueado por desync varsense preexistente (318A-6VAR) — NO se forzaron pins. Evidencia: `Agente/completados/tareas-2026-09-01.md`.

- **318A-14 (02-09-2026):** tabs agnósticos compartidos en paneles de tareas y notas. **Pedido del usuario**: extraer el sistema de tabs del panel de IA a un componente agnóstico compartido (sin duplicar) y activarlo como opción en los paneles de TAREAS (cada grupo de ejecución = tab) y NOTAS (cada nota = tab). **Hecho**: nuevo `TabsPanel` en `components/shared/TabsPanel.tsx` (100% controlado, sin store) + estilos `styles/dashboard/shared/tabsCompartidas.css` (tokens `--dashboard-*`, clases `tabCompartida*` que evitan el substring "panel" → resuelve de raíz el gotcha 318A-5 de `!important`); `TabsWorkspace` (IA) ahora es un wrapper que delega en `TabsPanel` conservando su API pública (galería intacta) y el CSS viejo `panelAgenteTab*` se eliminó de `panelAgente.css`. **TAREAS** (`PanelEjecucion` + `generadoresPropsPanel`): tabs de grupos con tab "Tareas" (`GRUPO_SIN_GRUPO`), activar llama `cambiarGrupo`, `SelectorGrupo` del header se conserva para crear/renombrar/eliminar. **NOTAS** (`PanelScratchpad` + `generadoresPropsLigeros`): tabs de notas (activa + guardadas), al cambiar se guarda la actual si tiene cambios y se selecciona la destino (`seleccionarNota`). **Opción activable por panel**: `usarTabsGrupos`/`usarTabsNotas` (default `true`) con toggles en `SeccionConfigTareas`/`SeccionConfigScratchpad`; los hooks de config ahora mergean con defaults (`{...default, ...valor}`) para que configs guardadas previas a un campo nuevo no dejen `undefined`. **Solo navegación** en tareas/notas (decisión del usuario); IA conserva renombrar/cerrar. **Verificación**: `tsc --noEmit` 0, `npm run build` OK, galería 9/9, navegador (tabs de grupos navegan/filtran, tabs de notas navegan, toggles activan/desactivan, IA sin regresión). Pendiente: gate bloqueado por desync varsense preexistente (318A-6VAR) — NO se forzaron pins. Evidencia: `Agente/completados/tareas-2026-09-02.md`.

- Arranque local: `npm run dev` (glory-rs). Requiere PostgreSQL local en 5432 y overrides `GLORY_DEV_DATABASE_URL_TEMPLATE`, `GLORY_DEV_DB_NAME=glory_backend_local`, `PORT=3000`, `CORS_ORIGINS` (ver `.freebuff/run.md`). Hay helpers `PROYECTO TASKS/.freebuff/start-backend.ps1` y `start-dev.ps1` para levantar solo el backend o el stack completo.
- Migracion de paridad: `migrations/20260826000000_parity_domains.{up,down}.sql` (subscriptions, attachments, backups, feedback, e2e_keys, api_tokens, `users.es_admin`).
- El plan de migracion previo quedo archivado en `Agente/planes/completados/plan-migracion-glory-react-logic-2026-08-11.md` (cerrado el 18-08-2026: el proyecto dejo de ser una migracion).

- **318A-7VAR-2 (02-09-2026) campaña PT→0 — borrado de clases huérfanas verificadas:** varsense PT **731 → 592, 0 errores**. Borrados verificados (0 usos en ts/tsx/js/jsx/html + glory-rs, con 4 generaciones de verificador que atraparon 3 bugs de detección: `/g lastIndex`, BEM ancestor `--`/`__`, template-literals sin punto): `App.css` completo (118 reglas, archivo muerto — 0 puntos de carga en ts/tsx/css/html/php/rs/md), `settings-panel.css` completo (29 reglas, solo `@import`; keyframes `fadeIn`/`spin` redefinidos en `index.css`), + 96 reglas/fragmentos de selector en 34 CSS (claseHuerfana 429→311; valorHardcoded 40→20 — los fragmentos `var(--token, literal)` con token ya definido). **Fix de error real:** `--dashboard-estadoPendienteBorde` (token inexistente) → `var(--dashboard-bordeNeutro)` en panelIA.css:368 (mismo valor resuelto, visual-neutral) → **1 error → 0**. **Efecto lateral honesto:** token-unused 19→20 — `--dashboard-espacio6xl` perdió su único consumidor (reglas `conDockTracking*` borradas); el token vive en `variables.css` (WIP usuario, no se toca). Verificación: type-check exit 0, build frontend verde, sentinel sin regresión (0e/83w/10h), balance de llaves 13/13. Commit local `318A-7VAR-2` (stage explícito; sin push — decisión del usuario). Queda declarado sin acción: token-duplicate ×175 + token-unused ×19 en `variables.css` (WIP), resto claseHuerfana con parientes vivos. Evidencia: `Agente/completados/tareas-2026-09-02.md`.

- **318A-7VAR-3 (02-09-2026) tercera pasada `claseHuerfana` (inspección manual):** varsense PT **592 → 580, 0 errores**; `claseHuerfana` **311 → 305**. Las 96 clases candidatas del clasificador se auditaron una a una con `code_search` repo-wide (ts/tsx/js/jsx/html/php/rs + glory-rs excluido); las familias con parientes vivos o construcción dinámica (`selectorNivelBoton${claseSufijo}`, `modalAyuno*`, `panelActividad*`, `bottomSheetCreacion*`, etc.) se retuvieron por diseño. Borrado final solo de lo confirmado muerto: bloque `.prose`/`.prose-sm` en `glory-core/index.css` (0 usos en código) + `.recienSoltado` en `panelArrastrable.css` (nunca togglado) + refs legacy `selectorNivelBotonCritical/High/Low` de reglas vivas (listas solo producen `MuyAlta/Alta/Media/Baja/MuyBaja/Bloqueante/Urgente/Normal/Chill`); marker `sentinel-disable-file` stale limpio. Verificación: harness fijado 580/0e, balance de llaves OK, type-check exit 0, build verde, sentinel sin regresión (0e/83w/10h), WIP del usuario intacto, sin huérfanas nuevas ni side-effects de tokens. Lo restante (305) tiene parientes vivos o uso dinámico — retenido ante duda. Evidencia: `Agente/completados/tareas-2026-09-02.md` §VAR-3.

- **318A-7VAR-4 (02-09-2026) convergencia `claseHuerfana` (inspección manual de familias semi-vivas):** las 63 candidatas de badgeInfo.css (19), ui.css (17), ui-formulario.css (16) y selectorNivel.css (11) se inspeccionaron una a una — **0 borrables, las 63 son FPs del scanner con uso real dinámico**: `BadgeInfo` emite `badgeInfo--${variante}` (union `VarianteBadge` con los 21 valores + mappers verificados), `Boton` emite `boton--${variante}`/`boton--${tamano}` + 7 flags, `Input/Textarea/Select/Checkbox/Radio` emiten sus `--error`/`--deshabilitado`/`--conIcono`/`--autoAjustar`, `SelectorNivel` emite `selectorNivelBoton${claseSufijo}` desde niveles reales. Causa raíz: el detector solo indexa literales, no template literals con interpolación ni mapas de sufijos. La familia ya estaba en `excepciones.json` (todos) → harness `MANTENIMIENTO 580/580`. **Frente PT→0 de `claseHuerfana` CONVERGIDO** (los 305 restantes tienen uso dinámico verificado o pariente vivo; retener ante duda). Verificación: harness 580/0e invariante, `tsc --noEmit` exit 0, WIP del usuario intacto, sin huérfanas nuevas. Sin commit de código (0 ediciones de producción), solo docs. Siguiente frente accionable: `token-duplicate` fuera de `variables.css` (~5). Evidencia: `Agente/completados/tareas-2026-09-02.md` §VAR-4.

- **318A-7V14 (02-09-2026) fix core varsense — familias de template literal en `claseHuerfana`:** causa raíz de los FPs de VAR-4 resuelta en el detector: un prefijo estático de template literal (`` `badgeInfo--${x}` ``) y los mapas de sufijos cuentan como USO de toda la familia (semántica documentada previa). 4 tests nuevos (74→78), lint + check:core verdes, dist reconstruido y publicado en la cadena V8 (varsense `38889aa`, pins + locks en 8 consumidores, WANDORIUS gitlink+dist, gloryapi no consume varsense). Resultado con harness fijado: **PT 580 → 536, 0 errores** (`claseHuerfana` 305→261); AGAPE 103/0e, WM 47/0e. Auditoría FN sobre PT: 44 desaparecidos / 0 aparecidos — 42 con uso dinámico verificado, 2 FN reales corregidos borrando reglas muertas (`.pillOpcion--premium` en adjuntos.css, `.badgeFiltrosActivos` en encabezado-movil.css, 0 usos repo-wide). Verificación: type-check exit 0, build verde, sentinel sin regresión (0e/83w/10h), WIP del usuario intacto (Cargo.toml glory-harness, variables.css, data/, test_prueba.md). Commit local `318A-7V14`, sin push (decisión pendiente del usuario). Siguiente frente accionable: `token-duplicate` de PT fuera de `variables.css` (~5). Evidencia: `Agente/completados/tareas-2026-09-02.md` §V14.

- **318A-7V15 (02-09-2026) token-duplicate de PT fuera de variables.css + área 9/9 re-verificada:** los 5 pares restantes fuera del WIP → **0 colapsables, 5 excepciones documentadas** (fuente única: `workspace-manager/scripts/quality/excepciones.json` → `PROYECTO TASKS` → `pares`; el harness `analyze-blocks.mjs` las casa 1:1 por par de tokens desde V16). Harness 9/9 con runtime V14 (`38889aa`): **todos MANTENIMIENTO, 0 hallazgos fuera del registro** — WM 47 · RESTAURANTE 141 · AGAPE 103 · WANDORIUS 149 · coolify 26 · gloryapi 76 · Laminal 42 · GLORYPORT 0 · **PT 536** (0 errores). Drift V14 registrado: AGAPE/WANDORIUS/coolify claseHuerfana 38→25 / 10→6 / 12→8. Verificación PT: type-check exit 0, build verde, sentinel **0e/82w/10h** (1w menos que baseline, sin regresión), WIP del usuario intacto. Commits locales sin push: PT `891871b` (docs) + WM commits V15/V16 (excepciones.json + plan §J-11V15/V16). PT→0 **convergido**: sin frentes accionables fuera del WIP del usuario; los 536 hallazgos (0 errores) están documentados en el registro. Evidencia: `Agente/completados/tareas-2026-09-02.md` §V15.
