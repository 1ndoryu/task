# Code Sentinel - Reporte de Workspace

**Fecha:** 2026-05-10 23:38:26  
**Archivos analizados:** 847  
**Archivos con violaciones:** 126  
**Total violaciones:** 328  

| Severidad | Cantidad |
|-----------|----------|
| Error | 1 |
| Warning | 323 |
| Info | 2 |
| Hint | 2 |

---

## App/React/services/notasService.ts (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 135 | Error | api-response-mismatch | Clave 'carpetas' no existe en la respuesta PHP del endpoint 'notas/carpetas'. Claves disponibles: carpeta. |
| 135 | Info | api-response-mismatch | PHP devuelve 'carpeta' en 'notas/carpetas' pero no se declara en el tipo generico. Si se accede a data.carpeta, no tendra tipo seguro. |

---

## App/React/styles/dashboard/componentes/compartidos.css (16 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 68 | Warning | css-elemento-html-directo | Selector ".{clase} h4" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 101 | Warning | modal-semantica-no-canonica | La clase "modalCompartirAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 109 | Warning | modal-semantica-no-canonica | La clase "modalCompartirAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 109 | Warning | css-elemento-html-directo | Selector ".{clase} button" detectado. Pasar className al <Button> en lugar de apuntar al elemento raw. |
| 201 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorCompaneroItem" define especificacion visual local (border, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 277 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorRolOpcion" define especificacion visual local (background, border, border-radius, color, font-size, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 291 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorRolOpcion" define especificacion visual local (border, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 321 | Warning | css-especificacion-diseno-local | La clase CSS ".participanteItem" define especificacion visual local (background, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 393 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 414 | Warning | css-especificacion-diseno-local | La clase CSS ".participanteRolMenu" define especificacion visual local (background, border, border-radius, box-shadow). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 428 | Warning | css-especificacion-diseno-local | La clase CSS ".participanteRolOpcion" define especificacion visual local (background, border, color, font-size, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 443 | Warning | css-especificacion-diseno-local | La clase CSS ".participanteRolOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 533 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 590 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorAsignadoMenu" define especificacion visual local (background, border, border-radius, box-shadow). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 605 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorAsignadoOpcion" define especificacion visual local (background, border, color, font-size, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 620 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorAsignadoOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## Glory/assets/react/src/pageBuilder/styles/constructorPaginas.css (11 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 52 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 67 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 78 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 125 | Warning | css-especificacion-diseno-local | La clase CSS ".campoArrayItem" define especificacion visual local (background, border, border-radius, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 142 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 144 | Warning | css-especificacion-diseno-local | La clase CSS ".botonEliminarItem" define especificacion visual local (background, border, color, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 164 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 166 | Warning | css-especificacion-diseno-local | La clase CSS ".botonAgregarItem" define especificacion visual local (background, border, border-radius, color, font-size, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 235 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 276 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 286 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/data/datosIniciales.ts (9 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 100 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 104 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 108 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 109 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 110 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 111 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 113 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 117 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |
| 123 | Warning | emoji-en-codigo | Emoji Unicode detectado. Usar SVG o componente de icono en su lugar. |

---

## App/React/styles/dashboard/shared/layoutManager.css (8 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 90 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 154 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 185 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 204 | Warning | css-especificacion-diseno-local | La clase CSS ".configLayoutColumnaOpcion" define especificacion visual local (background, border, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 210 | Warning | css-especificacion-diseno-local | La clase CSS ".configLayoutColumnaOpcion" define especificacion visual local (background, border, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 229 | Warning | css-especificacion-diseno-local | La clase CSS ".configLayoutPanelItem" define especificacion visual local (background, border, border-radius, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 276 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 298 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/scratchpad-vistaPrevia.css (7 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 147 | Warning | css-especificacion-diseno-local | La clase CSS ".listaNotasItem" define especificacion visual local (background, border, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 162 | Warning | css-especificacion-diseno-local | La clase CSS ".listaNotasItem" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 170 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 172 | Warning | css-especificacion-diseno-local | La clase CSS ".listaNotasItemBotonMenu" define especificacion visual local (background, border, border-radius, color, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 190 | Warning | css-especificacion-diseno-local | La clase CSS ".listaNotasItemBotonMenu" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 284 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 311 | Warning | modal-semantica-no-canonica | La clase "modalNotasBotonTexto" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |

---

## App/React/styles/dashboard/componentes/modalLimiteAlcanzado.css (7 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 22 | Warning | modal-semantica-no-canonica | La clase "modalLimiteAlcanzadoTitulo" duplica semantica visual compartida de Modal. Usa className="modalTitulo" y deja el layout/estado en clases locales separadas. |
| 31 | Warning | modal-semantica-no-canonica | La clase "modalLimiteAlcanzadoDescripcion" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |
| 71 | Warning | modal-semantica-no-canonica | La clase "modalLimiteAlcanzadoBeneficiosTitulo" duplica semantica visual compartida de Modal. Usa className="modalTitulo" y deja el layout/estado en clases locales separadas. |
| 78 | Warning | css-especificacion-diseno-local | La clase CSS ".modalLimiteAlcanzadoLista" define especificacion visual local (padding, text-align). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 85 | Warning | css-especificacion-diseno-local | La clase CSS ".modalLimiteAlcanzadoLista" define especificacion visual local (border, color, font-size, font-family, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 102 | Warning | modal-semantica-no-canonica | La clase "modalLimiteAlcanzadoAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 107 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/carpetasNotas.css (7 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 32 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 84 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 145 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 216 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 251 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 297 | Warning | css-especificacion-diseno-local | La clase CSS ".menuMoverACarpetaOpcion" define especificacion visual local (background, border, border-radius, color, font-size, font-family, padding, text-align). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 327 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/paginasLegales.css (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 24 | Warning | css-elemento-html-directo | Selector ".{clase} h1" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 45 | Warning | css-elemento-html-directo | Selector ".{clase} h2" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 54 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 136 | Warning | css-elemento-html-directo | Selector ".{clase} h1" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 140 | Warning | css-elemento-html-directo | Selector ".{clase} h2" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 144 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |

---

## App/React/styles/dashboard/shared/configuracionModerna-campos.css (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "shared/" contiene 35 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "shared" a codeSentinel.directoryExceptions en settings.json. |
| 126 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (background, border, border-radius, color, font-size, font-family, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 142 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 182 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (background, border, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 191 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 197 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (background, border, color, font-size, font-family, line-height, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/panelAyuno-base.css (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 53 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 147 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 209 | Warning | css-especificacion-diseno-local | La clase CSS ".panelAyunoDuracionOpcion" define especificacion visual local (font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 214 | Warning | css-especificacion-diseno-local | La clase CSS ".panelAyunoDuracionOpcion" define especificacion visual local (background, border, border-radius, color, font-size, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 225 | Warning | css-especificacion-diseno-local | La clase CSS ".panelAyunoDuracionOpcion" define especificacion visual local (border, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 243 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/modalPlugins.css (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 72 | Warning | modal-semantica-no-canonica | La clase "modalPluginsDescripcion" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |
| 124 | Warning | modal-semantica-no-canonica | La clase "modalPluginsAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 129 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 223 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 272 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 287 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/selectorEstado.css (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 69 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoTareaPanel" define especificacion visual local (border, padding, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 89 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoTareaOpcion" define especificacion visual local (background, border, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 105 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoTareaOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 190 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoHabitoPanel" define especificacion visual local (border, padding, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 199 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoHabitoOpcion" define especificacion visual local (background, border, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 215 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorEstadoHabitoOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## Glory/assets/react/src/index.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 38 | Warning | css-elemento-html-directo | Selector ".{clase} h2" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 47 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 56 | Warning | css-elemento-html-directo | Selector ".{clase} h4" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 153 | Warning | css-elemento-html-directo | Selector ".{clase} h2" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 159 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |

---

## App/React/styles/dashboard/componentes/tareas.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 9 | Warning | css-especificacion-diseno-local | La clase CSS ".tareaItem" define especificacion visual local (padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 173 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 194 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 301 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 566 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/panelAyuno-modales.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 63 | Warning | css-especificacion-diseno-local | La clase CSS ".modalAyunoTimelineItem" define especificacion visual local (background, opacity). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 133 | Warning | modal-semantica-no-canonica | La clase "modalAyunoTimelineTitulo" duplica semantica visual compartida de Modal. Usa className="modalTitulo" y deja el layout/estado en clases locales separadas. |
| 183 | Warning | modal-semantica-no-canonica | La clase "modalAyunoFinalizarAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 191 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 247 | Warning | css-especificacion-diseno-local | La clase CSS ".panelAyunoHistorialItem" define especificacion visual local (border, color, font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/bottomSheetCreacion.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 90 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 196 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 304 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 412 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 576 | Warning | modal-semantica-no-canonica | La clase "modalSeleccionPropiedad__opcionDescripcion" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |

---

## App/React/styles/dashboard/componentes/feedback.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 39 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 40 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 163 | Warning | modal-semantica-no-canonica | La clase "modalFeedbackAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 169 | Warning | modal-semantica-no-canonica | La clase "modalFeedbackAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 169 | Warning | css-elemento-html-directo | Selector ".{clase} button" detectado. Pasar className al <Button> en lugar de apuntar al elemento raw. |

---

## App/React/styles/dashboard/admin/panelAdministracion.css (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 116 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 176 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 192 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 388 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 438 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/configuracionModerna-adjuntos.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 18 | Warning | css-especificacion-diseno-local | La clase CSS ".adjuntosGrid__item" define especificacion visual local (background, border, border-radius, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 29 | Warning | css-especificacion-diseno-local | La clase CSS ".adjuntosGrid__item" define especificacion visual local (border, box-shadow). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 194 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorRepeticionPanel" define especificacion visual local (background, border, border-radius, box-shadow, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 266 | Warning | css-elemento-html-directo | Selector ".{clase} button" detectado. Pasar className al <Button> en lugar de apuntar al elemento raw. |

---

## App/React/styles/dashboard/componentes/panelDeficitCalorico.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 240 | Warning | css-especificacion-diseno-local | La clase CSS ".deficitComidaItem" define especificacion visual local (border, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 406 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 440 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 510 | Warning | css-especificacion-diseno-local | La clase CSS ".deficitHistorialItem" define especificacion visual local (font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/panelAyuno-historial.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 71 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 105 | Warning | modal-semantica-no-canonica | La clase "modalAyunoUltimaComidaTexto" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |
| 157 | Warning | modal-semantica-no-canonica | La clase "modalAyunoUltimaComidaAcciones" duplica semantica visual compartida de Modal. Usa className="modalAcciones" y deja el layout/estado en clases locales separadas. |
| 161 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/panelActividad.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 54 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 93 | Warning | css-especificacion-diseno-local | La clase CSS ".panelActividadDetalleItem" define especificacion visual local (background, border, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 160 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 280 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/modalConfiguracionMCP.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 103 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 130 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 157 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 316 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/modalConfigGlobal.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 63 | Warning | css-especificacion-diseno-local | La clase CSS ".configGlobalNavItem" define especificacion visual local (background, border, border-radius, color, font-size, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 81 | Warning | css-especificacion-diseno-local | La clase CSS ".configGlobalNavItem" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 86 | Warning | css-especificacion-diseno-local | La clase CSS ".configGlobalNavItem" define especificacion visual local (background, color, font-weight). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 230 | Warning | css-especificacion-diseno-local | La clase CSS ".configMovilItem" define especificacion visual local (border-radius, color, font-size, padding, text-align). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/adjuntos.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 18 | Warning | css-especificacion-diseno-local | La clase CSS ".adjuntoItem" define especificacion visual local (background, border, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 29 | Warning | css-especificacion-diseno-local | La clase CSS ".adjuntoItem" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 170 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 235 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/formulario.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 80 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 196 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 213 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 245 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/encabezado-botones.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 46 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 72 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 263 | Warning | modal-semantica-no-canonica | La clase "buscadorModalTitulo" duplica semantica visual compartida de Modal. Usa className="modalTitulo" y deja el layout/estado en clases locales separadas. |

---

## App/React/styles/dashboard/shared/configuracionModerna-responsables.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 99 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 226 | Warning | css-especificacion-diseno-local | La clase CSS ".seccionResponsables__companeroItem" define especificacion visual local (background, border, border-radius, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 307 | Warning | css-especificacion-diseno-local | La clase CSS ".seccionResponsables__menuOpcion" define especificacion visual local (background, border, border-radius, color, font-size, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/shared/configuracionModerna-formularios.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 9 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorIconoProyecto__trigger" define especificacion visual local (background, border, border-radius, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 22 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorIconoProyecto__trigger" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 27 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorIconoProyecto__dropdown" define especificacion visual local (background, border, border-radius, box-shadow, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 194 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/selectorBadge.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 13 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 120 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorBadgeMenu" define especificacion visual local (background, border, border-radius, box-shadow, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 148 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorBadgeOpcion" define especificacion visual local (background, border, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 165 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorBadgeOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/shared/panelSeguridad.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 87 | Warning | css-elemento-html-directo | Selector ".{clase} h3" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 221 | Warning | css-especificacion-diseno-local | La clase CSS ".detalleCifradoItem" define especificacion visual local (font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 233 | Warning | css-especificacion-diseno-local | La clase CSS ".detalleCifradoItem" define especificacion visual local (color, letter-spacing). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 239 | Warning | css-especificacion-diseno-local | La clase CSS ".detalleCifradoItem" define especificacion visual local (background, border-radius, color, font-family, font-weight, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/shared/suscripcion.css (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 158 | Warning | css-elemento-html-directo | Selector ".{clase} h2" detectado. Usar className="modalTitulo" en JSX en lugar de estilar por selector de elemento. |
| 307 | Warning | css-especificacion-diseno-local | La clase CSS ".modalUpgrade__planOpcion" define especificacion visual local (background, border, border-radius, font-family, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 322 | Warning | css-especificacion-diseno-local | La clase CSS ".modalUpgrade__planOpcion" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 368 | Warning | modal-semantica-no-canonica | La clase "modalUpgrade__planDescripcion" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |

---

## App/React/components/dashboard/ModalConfiguracionLayout.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 69 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 109 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 129 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 149 | Warning | modal-acciones-no-canonico | Clase "configLayoutAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/components/dashboard/creacion-rapida/MenusCreacionRapida.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 57 | Warning | menu-contextual-override-diseno | "className=textoInfo" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |
| 58 | Warning | menu-contextual-override-diseno | "className=textoExito" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |
| 59 | Warning | menu-contextual-override-diseno | "className=textoAdvertencia" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |
| 73 | Warning | menu-contextual-override-diseno | "className=textoApagado" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |

---

## App/React/styles/dashboard/shared/accionesItem.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 12 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 14 | Warning | css-especificacion-diseno-local | La clase CSS ".accionesItem" define especificacion visual local (background, border, border-radius, color, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 27 | Warning | css-especificacion-diseno-local | La clase CSS ".accionesItem" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/movilGrid.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 98 | Warning | css-especificacion-diseno-local | La clase CSS ".dashboardPanel" define especificacion visual local (background, border, border-radius, box-shadow). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 108 | Warning | css-especificacion-diseno-local | La clase CSS ".dashboardPanel" define especificacion visual local (border, border-radius, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 187 | Warning | css-especificacion-diseno-local | La clase CSS ".menuOpcionesPanelItem" define especificacion visual local (background, border, border-radius, color, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/movilComponentes.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 41 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 194 | Warning | css-especificacion-diseno-local | La clase CSS ".drawerMovil__item" define especificacion visual local (border-radius, color, padding, text-decoration, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 207 | Warning | css-especificacion-diseno-local | La clase CSS ".drawerMovil__item" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/components/shared/ModalFeedback.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 41 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 48 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 87 | Warning | modal-acciones-no-canonico | Clase "modalFeedbackAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/styles/dashboard/componentes/scratchpad-modalNotas.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 107 | Warning | css-especificacion-diseno-local | La clase CSS ".modalNotasItem" define especificacion visual local (background, border, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 121 | Warning | css-especificacion-diseno-local | La clase CSS ".modalNotasItem" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 139 | Warning | modal-semantica-no-canonica | La clase "modalNotasItemTitulo" duplica semantica visual compartida de Modal. Usa className="modalTitulo" y deja el layout/estado en clases locales separadas. |

---

## App/React/styles/dashboard/componentes/panelGruposFb.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 312 | Warning | css-especificacion-diseno-local | La clase CSS ".panelGruposFb__menuColumnasItem" define especificacion visual local (color, font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 526 | Warning | css-especificacion-diseno-local | La clase CSS ".editorCategorias__pickerItem" define especificacion visual local (background, border, border-radius, font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 540 | Warning | css-especificacion-diseno-local | La clase CSS ".editorCategorias__pickerItem" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/listaOrdenPaneles.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 43 | Warning | css-especificacion-diseno-local | La clase CSS ".listaOrdenItem" define especificacion visual local (background, border, border-radius, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 89 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 178 | Warning | css-especificacion-diseno-local | La clase CSS ".listaOrdenPreviewItem" define especificacion visual local (background, border, border-radius). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/historialBackups.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 96 | Warning | css-especificacion-diseno-local | La clase CSS ".badgeTrigger" define especificacion visual local (background, border, border-radius, font-size, letter-spacing, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 110 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 130 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/equipos.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 188 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 248 | Warning | css-especificacion-diseno-local | La clase CSS ".solicitudItem" define especificacion visual local (background, border, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 367 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/encabezado-movil.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 59 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 154 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 258 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/drawerMovil.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 34 | Warning | css-especificacion-diseno-local | La clase CSS ".drawerMovilPanel" define especificacion visual local (background, border, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 206 | Warning | css-especificacion-diseno-local | La clase CSS ".drawerMovilItem" define especificacion visual local (background, border, border-radius, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 231 | Warning | css-especificacion-diseno-local | La clase CSS ".drawerMovilItem" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/configuracionHabitos.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 84 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 200 | Warning | css-especificacion-diseno-local | La clase CSS ".listaTareasHabito__item" define especificacion visual local (background, border-radius, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 364 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/selectorProyecto.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 71 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorProyectoPanel" define especificacion visual local (border, padding, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 96 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorProyectoOpcion" define especificacion visual local (background, border, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 112 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorProyectoOpcion" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/admin/detalleUsuario.css (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 32 | Warning | css-especificacion-diseno-local | La clase CSS ".detalleUsuarioPanel" define especificacion visual local (background, border, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 90 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 324 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/components/compartidos/ModalCompartir.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 65 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 82 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 93 | Warning | modal-acciones-no-canonico | Clase "modalCompartirAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/components/dashboard/PanelSeguridad.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 56 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 68 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 112 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |

---

## App/React/components/shared/ModalLimiteAlcanzado.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 63 | Warning | modal-con-titulo | <h2> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 87 | Warning | modal-acciones-no-canonico | Clase "modalLimiteAlcanzadoAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/styles/dashboard/componentes/ventanaOportunidad.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "componentes/" contiene 64 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "componentes" a codeSentinel.directoryExceptions en settings.json. |
| 9 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/tabla.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 490 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 525 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/proyectos.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 27 | Warning | css-especificacion-diseno-local | La clase CSS ".proyectoItem" define especificacion visual local (background, border, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 151 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/overlayEnfoque.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 80 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 122 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/notificaciones.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 82 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 220 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/navegacionInferior.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 101 | Warning | css-especificacion-diseno-local | La clase CSS ".navegacionInferiorMenuFabItem" define especificacion visual local (background, border, border-radius, box-shadow, color, font-size, font-family, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 120 | Warning | css-especificacion-diseno-local | La clase CSS ".navegacionInferiorMenuFabItem" define especificacion visual local (background, color). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/chatHistorial.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 43 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |
| 313 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/bottomSheet.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | css-especificacion-diseno-local | La clase CSS ".bottomSheetPanel" define especificacion visual local (background, border, border-radius, color, font-size, font-family, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 113 | Warning | css-especificacion-diseno-local | La clase CSS ".bottomSheetItem" define especificacion visual local (background, border, border-radius, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/frecuencia.css (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 63 | Warning | css-especificacion-diseno-local | La clase CSS ".selectorFrecuenciaPanel" define especificacion visual local (border, padding, animation). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |
| 137 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/components/dashboard/AccionesDatos.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 38 | Warning | button-clase-especifica | Clase especifica de boton "botonAccionIcono" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |
| 43 | Warning | button-clase-especifica | Clase especifica de boton "botonAccionIcono" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |

---

## App/React/components/dashboard/ModalConfiguracionHabitos.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 146 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |
| 169 | Warning | modal-con-titulo | <h4> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |

---

## App/React/components/dashboard/encabezado/EncabezadoAcciones.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 77 | Warning | button-clase-especifica | Clase especifica de boton "botonIconoEncabezado__contadorNotificaciones" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |
| 92 | Warning | button-clase-especifica | Clase especifica de boton "botonIconoEncabezado__contador" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |

---

## App/React/components/dashboard/ModalPerfil.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 76 | Warning | button-clase-especifica | Clase especifica de boton "botonPerfilContenido" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |
| 79 | Warning | button-clase-especifica | Clase especifica de boton "botonPerfilContenido" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |

---

## TemplateReact.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "glorytemplate/" contiene 16 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "glorytemplate" a codeSentinel.directoryExceptions en settings.json. |

---

## mcp/test-with-env.js (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "mcp/" contiene 14 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "mcp" a codeSentinel.directoryExceptions en settings.json. |

---

## mcp/src/index.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 373 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (314 lineas efectivas). Dividir obligatoriamente. |

---

## App/Services/SuscripcionService.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "Services/" contiene 20 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "Services" a codeSentinel.directoryExceptions en settings.json. |

---

## App/Services/AdminService.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 211 | Info | query-doble-verificacion | Query de verificacion seguida de query de datos sobre 'tablatareas'. Combinar en una sola query. |

---

## App/Repository/TareasRepository.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "Repository/" contiene 15 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "Repository" a codeSentinel.directoryExceptions en settings.json. |

---

## App/Repository/BackupsRepository.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 90 | Hint | todo-pendiente | Marcador pendiente detectado. Resolver o migrar a roadmap como tarea formal. |

---

## App/React/utils/validadores.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "utils/" contiene 23 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "utils" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/types/timeTracker.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "types/" contiene 12 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "types" a codeSentinel.directoryExceptions en settings.json. |

---

## Glory/src/Services/EventBus.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "Services/" contiene 11 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "Services" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/hooks/useWebSocket.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "hooks/" contiene 61 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "hooks" a codeSentinel.directoryExceptions en settings.json. |

---

## App/Api/SuscripcionApiController.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "Api/" contiene 23 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "Api" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/styles/prueba/paginaPrueba.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 81 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/hooks/shared/useSwipeableItem.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "shared/" contiene 18 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "shared" a codeSentinel.directoryExceptions en settings.json. |

---

## Glory/src/Manager/PostTypeManager.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "Manager/" contiene 16 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "Manager" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/styles/dashboard/variables.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "dashboard/" contiene 13 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "dashboard" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/styles/dashboard/utilidades/inline-fix.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 8 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/utilidades/acciones.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 16 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## Glory/assets/react/src/pageBuilder/components/PageBuilder.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 97 | Hint | any-type-explicito | Tipo "any" explicito. Usar un tipo especifico o "unknown" si el tipo es desconocido. |

---

## App/React/hooks/dashboard/useTareaOrdenamiento.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "dashboard/" contiene 48 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "dashboard" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/styles/dashboard/shared/campoFechaLimite.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 90 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/alertas.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 191 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/movilNavegacion.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | css-especificacion-diseno-local | La clase CSS ".navegacionInferior__item" define especificacion visual local (color, font-size, text-decoration, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/movilListas.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 24 | Warning | css-especificacion-diseno-local | La clase CSS ".proyectoItem" define especificacion visual local (background, border). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/movilFormularios.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 60 | Warning | css-especificacion-diseno-local | La clase CSS ".pillOpcion" define especificacion visual local (font-size, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/components/shared/TooltipSystem.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "shared/" contiene 80 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "shared" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/components/shared/MenuContextual.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 50 | Warning | menu-contextual-override-diseno | "className=menuContextualSeparador" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |

---

## App/React/components/paneles/ThOrdenable.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "paneles/" contiene 14 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "paneles" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/components/paneles/ayuno/ModalUltimaComida.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 36 | Warning | modal-acciones-no-canonico | Clase "modalAyunoUltimaComidaAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/components/paneles/ayuno/ModalFinalizarAyuno.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 133 | Warning | modal-acciones-no-canonico | Clase "modalAyunoFinalizarAcciones" dentro de <Modal> no es canonical. Usar className="modalAcciones" para la zona de botones. |

---

## App/React/styles/dashboard/componentes/toast.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 41 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/swipeableItem.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 133 | Warning | css-especificacion-diseno-local | La clase CSS ".swipeableItem" define especificacion visual local (background, border-radius, transition, opacity). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/scratchpad-vistaExpandida.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 24 | Warning | css-especificacion-diseno-local | La clase CSS ".vistaNotasEncabezadoLista" define especificacion visual local (background, border, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/perfil.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 84 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/panelIA.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 227 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/panelConfiguracion.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 140 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/modalLogin.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 19 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/modalCreacionRapida.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 113 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/menuContextual.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 36 | Warning | css-especificacion-diseno-local | La clase CSS ".menuContextualOpcion" define especificacion visual local (background, border, color, font-size, font-family, padding, text-align, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/configBarraInferior.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | css-especificacion-diseno-local | La clase CSS ".configBarraInferiorItem" define especificacion visual local (border-radius, padding). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/buscador.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 98 | Warning | css-especificacion-diseno-local | La clase CSS ".buscadorItem" define especificacion visual local (padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/Landing/landingAnimations.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 252 | Warning | css-especificacion-diseno-local | La clase CSS ".landingSortItem" define especificacion visual local (background, border, border-radius, box-shadow, padding, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/Landing/landing.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 134 | Warning | css-especificacion-diseno-local | La clase CSS ".landingHeroGridItem" define especificacion visual local (background, border, color, transition). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/componentes/experimentos.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 173 | Warning | modal-semantica-no-canonica | La clase "modalExperimentos__accionDescripcion" duplica semantica visual compartida de Modal. Usa className="modalTexto" y deja el layout/estado en clases locales separadas. |

---

## App/React/styles/dashboard/componentes/estadoVacio.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 53 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/componentes/encabezado-base.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 188 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/mapaCalorHabito.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 379 | Warning | css-especificacion-diseno-local | La clase CSS ".mapaCalorHabitoLeyendaItem" define especificacion visual local (color, font-size, font-family). Usa una receta compartida como Button/ContextMenu/dropdown canonico y deja aqui solo layout contextual. |

---

## App/React/styles/dashboard/shared/dockTracking.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 106 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/configuracionModerna-tareasCompactas.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 229 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/configuracionModerna-secciones.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 50 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/selectorNivel.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 10 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/dashboard/shared/selectorDias.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 9 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/arbitraje/tabla.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 59 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/arbitraje/simulador.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 64 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/styles/arbitraje/modal.css (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 56 | Warning | css-adhoc-button-style | Bloque CSS de boton detectado fuera de Button.css. Si es un boton, usar <Button variante="..."> en su lugar. |

---

## App/React/stores/timeTrackerStore.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "stores/" contiene 16 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "stores" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/components/dashboard/BottomSheetTarea.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | directorio-abarrotado | Directorio "dashboard/" contiene 39 archivos (max 10). Reorganizar en subdirectorios por dominio. Excepcion: agregar "dashboard" a codeSentinel.directoryExceptions en settings.json. |

---

## App/React/components/configuracion/ModalConfiguracionMCP.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 44 | Warning | modal-con-titulo | <h3> detectado dentro de <Modal>. Los modales no llevan titulo — contenido directo + modalAcciones solamente. |

---

## App/React/components/configuracion/global/SeccionesConfigGeneral.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 171 | Warning | button-clase-especifica | Clase especifica de boton "botonPerfilContenido" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |

---

## App/React/components/dashboard/encabezado/EncabezadoPerfil.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 106 | Warning | menu-contextual-override-diseno | "className=inputOculto" inyecta diseno local en <MenuContextual>. Modifica el sistema UI (ContextMenu.css) en vez de parchear via prop. |

---

## App/React/components/dashboard/encabezado/EncabezadoMovil.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 56 | Warning | button-clase-especifica | Clase especifica de boton "botonIconoEncabezado__puntoNotificacion" detectada. Preferir variantes/tamanos de <Button> y mover la semantica a contenedores o contenido interno. |
