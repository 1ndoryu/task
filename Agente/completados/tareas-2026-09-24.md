# Tareas completadas — 2026-09-24

## Sentinel TASKS: path-join + html-origen a 0, y fix de build por desync con el núcleo

**Qué:** (1) `path-join-sin-canonicalize` x2 en `src/handlers/storage.rs` — endurecido
`persistir_archivo` de verdad: extensión restringida a ASCII alfanumérico (máx 16,
si no `""`), más `tokio::fs::canonicalize` + contención `starts_with(base)` fail-closed.
(2) `html-sin-origen-declarado` en `frontend/.../useScratchpad.ts` — waiver
`sentinel-disable-file` justificado: auditados los 6 callsites, todos decoran texto ya
escapado por `escaparHtml` (escape-then-decorate, sin HTML crudo interpolado).
(3) Build roto en `HEAD` (ajeno, commiteado): el núcleo añadió `AiChatOptions::sesion_externa`
y `LlavesProveedor::opencode_go` y TASKS no los fijaba (E0063 en `ai.rs:120`, `config/mod.rs:46`).
Fijado con `sesion_externa: None` (llamada puntual, igual que el núcleo) y campo
`AiProviderKeys::opencode_go` leído de `OPENCODE_GO_API_KEY` + mapeado en el `From`.

**Archivos:** `src/handlers/storage.rs`, `frontend/src/app/hooks/dashboard/useScratchpad.ts`,
`src/handlers/ai.rs`, `src/config/mod.rs`.

**Evidencia:**
- `sentinel analyze --workspace . --format json`: `path-join-sin-canonicalize` 0 (era 2),
  `html-sin-origen-declarado` 0 (era 1). Resto error: 25x `ruta-post-sin-rate-limit` (= `249A-1`, pendiente).
- `cargo check --all-targets` (target `C:\tmp\tasks-target`): PASS (antes 2x E0063).
- `cargo clippy --all-targets -- -D warnings`: líneas tocadas limpias (se backtickearon los
  docs nuevos); el repo sigue rojo por ~122 preexistentes (`doc_markdown`, casts, etc.) — trabajo separado.
- `cargo test --lib -- config::` 2/2 ok; `-- agent::adaptador_base` 4/4 ok (tests con BD excluidos:
  requieren `run-with-db`, no provisionado en este bloque).
- `fmt`: repo con drift preexistente (incluido `storage.rs`); el código nuevo sigue el estilo del
  fichero, sin reformateos masivos. `frontend type-check`: sin `node_modules` (preexistente,
  auto-repair al arrancar); el cambio `.ts` es solo un comentario.

**Gotchas:** (a) `analyzeDocument` (sentinel 0.7.13) NO propaga `htmlProductoresPermitidos`
del config — la única vía hoy es `sentinel-disable-file`; gap del harness, no del consumidor.
(b) La regla `html-sin-origen` dispara por fichero con `export` + primer template con tag HTML
(línea 41), no por la función productora (que ni siquiera se exporta).

**Pendientes reales:** `249A-1` (rate-limit, incluye el POST de `storage.rs`); lote clippy
preexistente; drift `fmt`; `frontend/node_modules` ausente.
