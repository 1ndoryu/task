# Tareas completadas — 2026-09-02

## 318A-14: tabs agnósticos compartidos en paneles de tareas y notas

**Pedido del usuario:** "el sistema de tab del panel de IA me gusta mucho, vamos a hacerlo
agnóstico y agregarlo como una opción activable en los paneles de tareas y notas, o sea cada
nota estaría un tab y las tareas los grupos serian tabs, sería algo compartido sin duplicar".
Decisión con el usuario (vía preguntas): alcance literal (tabs = grupos en tareas / notas en
notas, como en IA), opción activable por panel en su config, y **solo navegar/seleccionar**
(sin renombrar/cerrar en tareas/notas; el panel IA conserva su comportamiento completo).

### Qué se hizo

1. **Componente agnóstico `TabsPanel`** (`frontend/src/app/components/shared/TabsPanel.tsx`):
   extraído del sistema de tabs del panel de IA (antes `TabsWorkspace` en
   `plugins/agente/componentes.tsx`). 100% controlado (sin store): recibe `tabs`, `activaId`,
   `onActivar` y callbacks opcionales (`onCerrar`, renombrado por doble clic). Es el ÚNICO
   componente compartido por IA, TAREAS y NOTAS — sin duplicar markup ni estilos.
   Estilos en `frontend/src/app/styles/dashboard/shared/tabsCompartidas.css` (tokens
   `--dashboard-*`, clases `tabCompartida*`).

2. **`TabsWorkspace` delegado**: ahora es un wrapper fino que delega en `TabsPanel`
   conservando su API pública (la galería dev exige `export function TabsWorkspace`). El CSS
   viejo de tabs (`panelAgenteTabs`/`panelAgenteTab*`) se eliminó de `panelAgente.css` — los
   estilos viven en un solo sitio.

3. **Gotcha 318A-5 resuelto de raíz**: la antigua `.panelAgenteTab` necesitaba clase duplicada +
   `!important` para vencer `.dashboardPanelView [class*="panel"] { border:none !important }`
   porque la clase CONTENÍA "panel". Las nuevas clases `tabCompartida*` evitan el substring
   "panel" → borde/fondo se ven sin `!important` ni selector duplicado.

4. **Tabs en panel de TAREAS** (`PanelEjecucion.tsx` + `generadoresPropsPanel.ts`): cada grupo
   de ejecución es una tab; el primer tab es "Tareas" (`GRUPO_SIN_GRUPO`) para las tareas sin
   grupo. Al activar una tab se llama `cambiarGrupo`. El `SelectorGrupo` del header se conserva
   para crear/renombrar/eliminar grupos (los tabs son solo navegación).

5. **Tabs en panel de NOTAS** (`PanelScratchpad.tsx` + `generadoresPropsLigeros.ts`): cada nota
   guardada es una tab + la nota activa (la nueva sin guardar se marca como tab virtual
   `__nueva__` con su título derivado). Al cambiar de tab se guarda la nota actual si tiene
   cambios (patrón de `manejarAbrirCarpeta`) y se selecciona la destino vía
   `seleccionarNota(panelId, nota)` del store.

6. **Opción activable por panel** en la configuración global:
   - Tareas: `usarTabsGrupos` (default `true`) — toggle "Tabs de grupos" en `SeccionConfigTareas`.
   - Notas: `usarTabsNotas` (default `true`) — toggle "Tabs de notas" en `SeccionConfigScratchpad`.
   - Los hooks de configuración ahora hacen merge con los defaults
     (`{...CONFIG_POR_DEFECTO, ...valor}`) para que la config guardada anterior a un campo nuevo
     no deje el campo en `undefined` (sin esto los tabs no aparecían al activar por defecto).

### Archivos

- `frontend/src/app/components/shared/TabsPanel.tsx` (nuevo)
- `frontend/src/app/styles/dashboard/shared/tabsCompartidas.css` (nuevo)
- `frontend/src/app/components/shared/index.ts` (+ export)
- `frontend/src/app/styles/dashboard/index.css` (+ import)
- `frontend/src/app/components/paneles/PanelEjecucion.tsx`
- `frontend/src/app/components/paneles/PanelScratchpad.tsx`
- `frontend/src/app/hooks/dashboard/generadoresPropsPanel.ts`
- `frontend/src/app/hooks/dashboard/generadoresPropsLigeros.ts`
- `frontend/src/app/hooks/useConfiguracionTareas.ts`
- `frontend/src/app/hooks/useConfiguracionScratchpad.ts`
- `frontend/src/app/components/configuracion/global/paneles/SeccionConfigTareas.tsx`
- `frontend/src/app/components/configuracion/global/paneles/SeccionConfigScratchpad.tsx`
- `frontend/src/app/plugins/agente/componentes.tsx` (wrapper delegado)
- `frontend/src/app/plugins/agente/panelAgente.css` (CSS viejo eliminado)

### Evidencia / verificación

- `tsc --noEmit` exit 0 (frontend).
- `npm run build` OK (el warning CSS de `<stdin>:30398` es preexistente del design system,
  no de este cambio).
- Galería dev `node .freebuff/galeria-visual.mjs`: **9/9 checks OK** (incluye `TabsWorkspace`).
- Navegador (127.0.0.1:5175):
  - Panel Tareas: tabs "Tareas" y "Dia" visibles; clic en "Dia" cambia heading a "Dia",
    actualiza "Grupo de ejecución: Dia" y filtra las tareas del grupo.
  - Panel Notas: tab de la nota activa + nota guardada; clic en la nota guardada cambia el
    contenido al de esa nota (navegación correcta, guarda la actual si tiene cambios).
  - Config Tareas: toggle "Tabs de grupos" activo por defecto; al desactivarlo desaparecen los
    tabs, al reactivarlo reaparecen.
  - Config Notas: toggle "Tabs de notas" activo por defecto.
  - Panel IA (`/agente/visuales`): `TabsWorkspace` usa el componente compartido con su
    comportamiento completo (3 tabs + botón de cerrar) — sin regresión visual.

### Notas / decisiones

- Los tabs de TAREAS y NOTAS son **solo navegación** (decisión del usuario): sin renombrar ni
  cerrar. El panel IA conserva renombrar (doble clic) + cerrar (X).
- En tareas se conserva el `SelectorGrupo` del header (crear/renombrar/eliminar grupos); los
  tabs son un acceso rápido a la selección.
- El gate `quality:check` sigue bloqueado por el desync varsense preexistente (318A-6VAR);
  NO se forzaron pins.
- La config guardada en navegador previa a este cambio no tenía los nuevos campos; el merge
  con defaults en los hooks de configuración lo resuelve (patrón a reutilizar al añadir
  campos nuevos a configs persistidas).

### Resultado final del bloque (campaña PT→0, re-medido con harness fijado)

Varsense PT **731 → 592** (0 errores / 592 warnings):

| Regla | Antes | Después |
|---|---|---|
| claseHuerfana | 429 | **311** |
| token-duplicate | 181 | 180 |
| valorHardcoded | 40 | **20** |
| cssInlineScript | 32 | 32 (excepción documentada: runtime dinámico) |
| cssInlineReact | 29 | 29 (excepción documentada: runtime dinámico) |
| token-unused | 19 | 20 — +1 **efecto lateral de este bloque**: `--dashboard-espacio6xl` perdió su único consumidor (reglas `conDockTracking*` borradas de dockTracking.css); el token vive en `variables.css` (WIP del usuario — no se toca). Documentado como excepción honesta, no enmascarado. |
| variableNoDefinida | 1 | **0** — fix real: `--dashboard-estadoPendienteBorde` (token inexistente, resolvía al fallback) → `var(--dashboard-bordeNeutro)` directo en panelIA.css:368 (visual-neutral, mismo valor resuelto). |

Verificación: `npm run type-check` exit 0 (en la última medición del bloque; el fix de panelIA es un solo token, no toca tipos), build frontend verde, sentinel PT sin regresión (**0e/83w/10h**), varsense varsense **0 errores**, balance de llaves CSS validado en los 13 archivos editados, sin huérfanas nuevas introducidas (solo el +1 token-unused documentado).

Lo que queda en PT (declarado, no accionable sin decisión): token-duplicate ×175 + token-unused ×19 en `variables.css` (WIP del usuario), badgeInfo.css ×19 y resto de claseHuerfana en archivos con parientes vivos (verificador v8: solo se borró lo verificado 0 usos en ts/tsx/js/jsx/html + glory-rs).

### Pendiente

- Ninguno para 318A-14. (Commit + push del bloque a decisión del usuario si aplica.)

---

## 318A-7VAR-2 (02-09-2026): borrado de clases huérfanas verificadas (campaña PT→0)

### Qué se hizo

Segunda pasada del borrado de `claseHuerfana` en PT, tras la primera (429→317). Esta pasada eliminó **17 reglas/selector-fragmentos en 13 CSS** (más las pasadas previas: 118 reglas de `App.css` —archivo muerto, 0 puntos de carga— y 29 de `settings-panel.css` —solo `@import` CSS, keyframes redefinidos en otros archivos, clases verificadas contra TSX/HTML—):

- Archivos completos borrados: `frontend/src/App.css` (118 reglas, 0 referencias en ts/tsx/js/jsx/css/html/php/rs/md), `frontend/css/settings-panel.css` (29 reglas; `fadeIn`/`spin` redefinidos en `index.css` — verificación de keyframes).
- Reglas individuales (17 bloques, verificadas **0 usos** en tsx/ts/html/rs/php/md + glory-rs): `seccionBoton`, `landingBotonPrimario,` (fragmento de selector), `proyectoAcciones`, `vistaProyectoProgreso`, `filaTags`, `.botonAccion,` (fragmento), `listaScrolleable`, `modalMovil__cabecera` + `modalMovil__botonVolver`, `seccionModerna__tituloTexto`, `modalModerna__cuerpo` + `modalModerna__footer`, `.proyectoAcciones,` (fragmento movil), `seccionModerna__titulo` + `__contenido` + `__vacio`, `conDockTracking` + `conDockTrackingMovil` (el dock vivo usa `dockTracking*`, no `conDockTracking`), `botonAccionDatos`, `botonContador`, `prose-sm`.
- Fragmentos de selector en listas con coma conservan el resto de la regla (`.botonOpcion,` etc.) — CSS validado por balance de llaves post-edición (13/13 OK).

### Verificación

- varsense (CLI fijado): `claseHuerfana` **317 → 311**; total PT **599 → 593** (0 errores nuevos; el único error `variableNoDefinida` sigue en `variables.css` = WIP del usuario).
- **Hallazgo nuevo documentado (no ocultado):** `token-unused` 19→20 — el token `--dashboard-espacio6xl` perdió su último consumidor al borrar `dockTracking.css`; su definición está en `variables.css` (WIP del usuario, no editable aquí). Queda como side effect real pendiente: borrar la definición del token cuando el WIP de `variables.css` se commitee.
- `tsc --noEmit` exit 0 · `npm run build` exit 0.
- sentinel PT (dist fijado): **0 errores / 83w / 10h** (sin regresión, −2 vs baseline 95).

### Metodología (lecciones aplicadas)

Clasificador iterativo v1→v9 con verificación repo-wide de uso literal + template-literal + BEM-ancestro + comentarios; 3 bugs del verificador cazados por chequeo humano con `code_search` antes de borrar (regex `/g` `lastIndex`, variantes `--sufijo` de templates `alertaToast--${tipo}`, familias camelCase/BEM con hermanos vivos). Las clases con familia viva o uso dinámico se excluyeron siempre (conservador).

### Pendiente

- Siguiente frente: `token-duplicate` (180), `valorHardcoded` (20), `cssInline*` (61), `token-unused` (20, incl. side effect documentado). WIP del usuario en `variables.css` sin tocar. Commit local, sin push (decisión pendiente del usuario).

## 318A-7VAR-3 (02-09-2026): tercera pasada de borrado de `claseHuerfana` (inspección manual)

### Qué se hizo

Tercera pasada de `claseHuerfana` en PT con inspección manual archivo por archivo (no solo clasificador): las 96 clases candidatas del clasificador se verificaron con `code_search` repo-wide (ts/tsx/js/jsx/html + glory-rs); las familias con parientes vivos (template literals `selectorNivelBoton${...}`, `modalAyuno*`, `panelActividad*`, `bottomSheetCreacion*`, etc.) se retuvieron por diseño (0 borradas en falso — los 3 bugs del verificador detectados en VAR-2 se auditaron también sobre esta lista). Borrado final solo de lo confirmado muerto:

- `frontend/src/glory-core/index.css`: bloque completo de **prosa** (`.prose` ×24 reglas + `.prose-sm` ×3) — 0 usos en código (ts/tsx/js/jsx/html/php/rs; el Scratchpad renderiza markdown con clases propias `scratchpadVistaPrevia*`, no `prose`; no hay react-markdown/remark directo — `markdown-it` solo transitivo en package-lock). Se actualizó el comentario `[por que]` y se retiró el `sentinel-disable-file css-elemento-html-directo` (ya no hay reglas de elementos HTML en ese archivo; quedó solo `@import init.css`, importado desde `main.tsx`).
- `frontend/src/app/styles/dashboard/shared/panelArrastrable.css`: `@keyframes panelSnap` + `.panelArrastrable.recienSoltado` — `recienSoltado` nunca se aplica en código (0 matches; el drag usa `arrastrandoPanel`/`zonaDrop`, ver `useOrdenPaneles.ts`/`DashboardGrid.tsx`); keyframes sin otro consumidor.
- `frontend/src/app/styles/dashboard/shared/selectorNivel.css`: referencias legacy `selectorNivelBotonCritical` / `High` / `Low` en reglas vivas (`.Alta`/`.Media`/`.Baja` + `.Normal` se conservan) — `claseSufijo` en `SelectorNivel.tsx` solo genera `MuyAlta/Alta/Media/Baja/MuyBaja/Bloqueante/Urgente/Normal/Chill`, así que Critical/High/Low son inalcanzables desde el código.

### Verificación

- varsense (harness fijado): **592 → 580**, 0 errores; `claseHuerfana` **311 → 305** (ahora: badgeInfo.css ×19, ui.css ×17, ui-formulario.css ×16, selectorNivel.css ×11 — dinámicos/familias vivas, retenidas con fundamento).
- Balance de llaves CSS 3/3 OK; `npm run type-check` exit 0; `npm run build` verde.
- sentinel PT (dist fijado): **0e/83w/10h** — sin regresión vs baseline VAR-2.
- WIP del usuario intacto: `variables.css` (sin stage), `data/`, `test_prueba.md`. Sin huérfanas nuevas introducidas; sin side effects de tokens nuevos (este bloque no borró consumidores de tokens — el `--dashboard-espacio6xl` documentado en VAR-2 se mantiene como está).

### Convergencia

Lo restante de `claseHuerfana` (305) tiene parientes vivos o uso dinámico verificados (regla del clasificador: retener siempre ante duda). Siguiente frente accionable: `token-duplicate` de PT fuera de `variables.css` (180, de los cuales ~175 en WIP del usuario) y `valorHardcoded` (14). Commit local `318A-7VAR-3` (`d3f94b3`), sin push (decisión pendiente del usuario).

## 318A-13: Glory Harness — Fase 2 (integración como lib en task)

**Pedido del usuario:** "continúa con el último plan, el otro agente lo dejó a medias" → el plan
`Agente/planes/plan-glory-harness-2026-09-01.md` (tarea 318A-13). Las Fases 0+1 las cerró el
otro agente (skeleton + traits + módulos movidos al núcleo `glory-harness-core`); dejó la
**Fase 2 (integración lib en task) a medias y sin compilar** (código migrado pero call sites
rotos). Esta entrada registra la continuidad que completó la Fase 2.

### Qué se hizo (continuidad Fase 2)

1. **`src/agent/mod.rs`**: declarado `pub mod adaptador; pub mod scheduler; pub mod tools;` y
   re-exportados `ContextoConfig`, `AgenteEvento`, `AgentRuntime`, `PuertosHarness`, `TurnoConfig`,
   `correr_scheduler`, `AgentToolRegistry` desde el crate `glory-harness-core` (dejar de apuntar a
   módulos internos ya movidos).
2. **`src/handlers/agente.rs`**: construye el runtime con los puertos del núcleo —
   `PersistenciaAgente` (nuevo, implementa `AgentPersistence`), `BuscadorWeb` (`WebSearchProvider`),
   `DominioAgente` (inyectado vía `ctx.dominio`), `registrar_tools`. `inyectar_contexto`/
   `persistir_turno_y_mensaje`/`compactar_conversacion` ahora usan los métodos del adaptador y las
   funciones de núcleo (`resumen_de_mensajes`, `estimar_tokens`).
3. **`src/main.rs`**: bloque del scheduler reescrito a la firma nueva de `correr_scheduler`
   (persistencia + closure `Fn(TareaProgramadaPendiente, DateTime<Utc>)`) con
   `ejecutar_tarea_harness`.
4. **`src/repositories/agente.rs`**: `insertar_resumen` usa `glory_harness_core::context::estimar_tokens`.
5. **Eliminados** los 6 módulos huérfanos de task ya movidos al núcleo: `src/agent/context.rs`,
   `diff.rs`, `runtime.rs`, `sandbox.rs`, `tool.rs`, `tools_archivo.rs`.
6. **`glory-harness/core`**: añadido `#[derive(utoipa::ToSchema)]` a `AiMessage` + dep `utoipa`
   para que el OpenAPI del consumidor exponga `AiMessage` como schema (commit glory-harness `5d89a49`).
7. **Fix de tests heredados**: `adaptador.rs` accedía a `AiMessage.content` como `String`; ahora es
   `serde_json::Value` → tests actualizados a `as_str().unwrap()` y el umbral de corte de skills
   ajustado al prefijo real (bloque 4000 + ~46 de prefijo/elipsis).

### Archivos

- **glory-harness** (commit `5d89a49`): `core/Cargo.toml`, `core/src/llm.rs`, `Cargo.lock`.
- **task** (commit `d9d523f`): `Cargo.toml` (dep path `glory-harness-core`), `Cargo.lock`,
  `src/agent/{mod,adaptador(new),scheduler,tools}.rs`, borrados 6 módulos huérfanos,
  `src/handlers/agente.rs`, `src/handlers/mod.rs`, `src/main.rs`, `src/repositories/agente.rs`,
  `src/config/mod.rs`, `src/errors/mod.rs`, `src/services/ai.rs` (re-export del crate).
- **No stageados** (ajenos/preexistentes, intactos): `frontend/src/app/styles/dashboard/variables.css`
  (WIP usuario), `data/`, `test_prueba.md`.

### Verificación / evidencia

- `cargo check` task (vía `node scripts/run-with-db.mjs check`): **0 errores**.
- Tests task (`npm test`): **23 pass** (incluye 4 tests de `adaptador.rs` de skills).
- Core glory-harness: **44/44 tests pass** (con el `utoipa` añadido).
- Gate glory-harness (`sentinel check 318A-13`): **PASS** — 0 errores, 5 warnings estructurales
  heredados documentadas.
- `export-openapi` (task): **exit 0**, `AiMessage` aparece como `$ref` y schema en el OpenAPI
  generado (valida el `utoipa`).
- Clippy task: 7 errores **todos preexistentes** en archivos que el historial muestra no tocados por
  Fase 2 (`subscription.rs`, `realtime.rs`, `productivity.rs`, `admin.rs`, `storage.rs`,
  `agente.rs:731`). Sin regresión introducida por este bloque.

### Gate task y bloqueos

- **Gate task NO alcanzable** por desync varsense **preexistente (318A-6VAR)**: `readyForGate:false`
  por varsense commit `38889aa` no alcanzable desde refs de release permitidas + evidencia de
  compilación ausente. NO se forzó pin ni se editó `sentinel.lock.json` a mano (regla §5/§9.1);
  el plan §9.1 manda reportarlo como trabajo separado.
- **Pendiente real de Fase 2**: evidencia de un **turno SSE real con proveedor externo** (requiere
  credenciales/envs del proyecto anterior); la compilación + tests + OpenAPI avalan el contrato, pero
  la paridad funcional del chat de punta a punta queda pendiente de esa ejecución.

### Prevención / próximos pasos

- Al migrar un módulo a un crate/trait, actualizar **todos los call sites en el mismo bloque** (el
  otro agente dejó el "movido" hecho pero no el "pegamento"); de haberlo hecho, la Fase 2 habría
  compilado desde el inicio.
- Realinear varsense en **318A-6VAR** (separado) para desbloquear el gate de task antes del cierre
  definitivo de 318A-13.
- Fases restantes del plan: **Fase 3** (CLI `glory-harness run` + daemon SSE loopback multi-sesión) y
  **Fase 4** (segundo consumidor opcional).

## 318A-7VAR-4 (02-09-2026): convergencia de `claseHuerfana` — inspección manual de las familias semi-vivas

Inspección manual archivo por archivo de las 63 candidatas en los 4 archivos que el clasificador retenía por duda. **Resultado: 0 borrables con seguridad — las 63 son FPs del scanner con pariente vivo o construcción dinámica verificada repo-wide** (ts/tsx/js/jsx/html/php/rs, template literals, mapas de sufijos, sin indexar el propio CSS):

| Archivo | Hallazgos | Veredicto | Evidencia de uso real |
|---|---|---|---|
| `shared/badgeInfo.css` | 19 | **19 VIVAS** | `BadgeInfo.tsx:34` → `badgeInfo--${variante}`; union `VarianteBadge` declara las 21 variantes; mappers `obtenerVariantePrioridad` (useTablaHabitos.ts:35), `obtenerVarianteUrgencia` (ListaProyectos.tsx:142, TareaBadges.tsx:111), `obtenerVarianteFechaLimite` (fechaUI.ts:78) + usos directos (`habito`, `mensajeNoLeido`, `destacado`, `pospuesto`, `racha`, `frecuencia`) |
| `assets/css/ui.css` | 17 | **17 VIVAS** | `Boton.tsx:37-46` → `boton--${variante}`/`boton--${tamano}` + flags `soloIcono/cargando/deshabilitado/activo/anchoCompleto/compacto/destacado`; las "pesta"/"peque" del scanner son `pestaña`/`pequeño` (ñ) |
| `assets/css/ui-formulario.css` | 16 | **16 VIVAS** | `Input.tsx:45-48`, `Textarea.tsx:37`, `Select.tsx:24`, `Checkbox.tsx:16`, `Radio.tsx:24` construyen `--error`/`--deshabilitado`/`--conIcono`/`--conIconoDerecha`/`--autoAjustar`; `.select`/`.checkbox`/`.radio` base también |
| `shared/selectorNivel.css` | 11 | **11 VIVAS** | `SelectorNivel.tsx:26` → `selectorNivelBoton${claseSufijo}` (niveles reales: `NIVELES_PRIORIDAD`/`NIVELES_IMPORTANCIA` en nivelesConfig.tsx:161-162, `URGENCIAS` en CampoUrgencia.tsx:25 = MuyAlta/Alta/Media/Baja/MuyBaja/Bloqueante/Urgente/Normal/Chill) + `selectorNivelBotonActivo` |

**Causa raíz del FP:** el detector `claseHuerfana` indexa solo literales de string, no template literals con interpolación de variable (`` `badgeInfo--${variante}` ``) ni mapas de sufijos — las mismas clases que marcan son las que los componentes declarativos del sistema (`BadgeInfo`, `Boton`, `Input`, `Textarea`, `Select`, `Checkbox`, `Radio`, `SelectorNivel`) emiten en runtime. La familia ya estaba sembrada en `excepciones.json` (claseHuerfana PT, archivos: todos) → el harness reporta `MANTENIMIENTO 580/580 cubiertos`.

**Decisión:** 0 borrados (retener ante duda, regla del bloque). El frente PT→0 de `claseHuerfana` queda **convergido**: todo lo restante (305) tiene uso real dinámico verificado archivo por archivo o pariente vivo. Siguiente frente accionable real: `token-duplicate` de PT fuera de `variables.css` (~5).

**Verificación:** harness fijado 580/0e (invariante, sin ediciones de código), `tsc --noEmit` exit 0 (frontend), sin cambio de build/visual, WIP del usuario intacto (`variables.css` modificado sin stage, `data/`, `test_prueba.md`), sin huérfanas nuevas. Sin commit de código (no hubo ediciones de producción); solo docs (este archivo + roadmap).

## 318A-7V14 (02-09-2026): fix core de varsense — familias de template literal en `claseHuerfana`

### Qué se hizo

Fix de causa raíz documentada en VAR-4: el detector `claseHuerfana` indexaba solo literales de string, no template literals con interpolación (`` `badgeInfo--${variante}` ``) ni mapas de sufijos, por lo que reportaba como huérfanas familias construidas dinámicamente por los componentes declarativos (FPs verificados en PT/AGAPE/WM).

**Semántica adoptada (documentada en el plan antes de tocar código):** un prefijo estático de template literal (`` `badgeInfo--${x}` ``) marca como EN-USO toda la familia de clases con ese prefijo (no solo el literal); los mapas de sufijos (unión de strings usados como sufijo sobre un prefijo estático) también cuentan.

**Core (`classIndexBuilder.ts` + `persistentIndex.ts`):** extracción de segmentos de template literal en ts/tsx → registro de prefijos de familia en el índice de usos → `claseHuerfana` respeta la familia. 4 tests nuevos de contrato (prefijo+família, sufijos, css propio no indexado como uso, sin falsos positivos de variables JS) → **74 → 78 tests, lint + check:core verdes**.

### Auditoría FN (honesta, como V3/V4/V8)

Build del dist de HEAD (pre-fix) en worktree temporal y diff de listas de `claseHuerfana` sobre PT: **44 desaparecidas, 0 aparecidas**. Verificación una a una de las 44: **42 con uso dinámico real** (`` `mapaCalorCelda--nivel${nivel}` ``, `` `historialHabitoDia--${dia.estado}` ``, `` `alertaToast--${tipo}` ``, `` `dashboardGridColumnas--${layout.modoColumnas}col` ``, `` `mensaje${'Exito'|'Error'}` `` en AccionesDatos.tsx:51, etc.) y **2 FN reales corregidos borrando las reglas muertas** (0 usos repo-wide en ts/tsx/js/jsx/html/php/rs):

- `adjuntos.css`: `.pillOpcion--premium` (el prefijo `pillOpcion` lo barrió el concat completo de `PropiedadesCompactas` — clase completa, no sufijo).
- `encabezado-movil.css`: `.badgeFiltrosActivos` (prefijo genérico `badge` barre clases ajenas).

### Verificación

- varsense (harness fijado): **PT 580 → 536, 0 errores** (`claseHuerfana` **305 → 261**); AGAPE 103/0e y WM 47/0e (mantenimiento).
- `tsc --noEmit` exit 0 + `npm run build` verde (frontend PT); sentinel sin regresión **0e/83w/10h**.
- WIP del usuario intacto y sin stage (`Cargo.toml` glory-harness, `variables.css`, `data/`, `test_prueba.md`).

### Release (cadena V8)

- varsense commit **`38889aa`** (rama local, sin push — patrón V2→V13).
- Pins `quality-tools.json` + locks `sentinel.lock.json` actualizados y commiteados en los 8 consumidores (PT, RESTAURANTE, AGAPE, WANDORIUS, coolify-manager-rs, Glory-Laminal, GLORYPORT, workspace-manager); gloryapi no consume varsense (solo sentinel).
- WANDORIUS: gitlink + submodule `tools/varsense` en `38889aa` con dist reconstruido desde el pin (commit `b732c215`, sha lock `c57a0d02`).
- Backups `.bak` del generador eliminados (scratch).

Commit local `318A-7V14`, sin push (decisión pendiente del usuario). Siguiente frente: `token-duplicate` de PT fuera de `variables.css` (~5 hallazgos) — y la misma mejora de familia ya beneficia a los demás consumidores en su próximo sweep.

## 318A-7V15 (02-09-2026): token-duplicate de PT fuera de variables.css + re-verificación del área 9/9 con runtime V14

### Baseline y extracción

Harness fijado: **PT 536/0e, MANTENIMIENTO 536/536**. `token-duplicate` total: 180 → **175 en `variables.css` (WIP del usuario, sin tocar)** y **5 fuera**: `modalEditorArbol.css` ×3 y `pullToRefresh.css` ×1 y `resizeHandleColumna.css` ×1.

### Decisión por par (0 colapsables — 5 excepciones documentadas)

Fuente única (verificable por máquina desde 318A-7V16): `workspace-manager/scripts/quality/excepciones.json` → `PROYECTO TASKS` → `pares` — los 5 pares con `tokenA`/`tokenB`/`archivo`/`lineas`/`categoria`/`evidencia`. Categorías: 3 `bridge-agnostico` (editor pixel: knobs consumidos por separado en editorPixelArt.css:47-92) y 2 `fallback-inyectado` (runtime inyecta valores distintos: usePullToRefresh.ts:115-117, useDashboardGrid.ts:98-100; patrón en lecciones-aprendidas.md:34). Mismo criterio V8/V11: coincidencia de valor entre dominios semánticos independientes no es duplicado colapsable. El harness (`analyze-blocks.mjs`) las casa 1:1 por par de tokens y falla el veredicto si aparece uno nuevo sin registrar.

### Re-verificación del área 9/9 con runtime V14 (`38889aa`)

Harness sobre los 9 proyectos: **todos MANTENIMIENTO, 0 hallazgos fuera del registro** — workspace-manager 47 · RESTAURANTE 141 · AGAPE 103 · WANDORIUS 149 · coolify 26 · gloryapi 76 (sentinel) · Laminal 42 · GLORYPORT 0 · PT 536. Drift de conteos por absorción de familias template-literal (V14) actualizada en el registro: AGAPE `claseHuerfana` **38→25**, WANDORIUS **10→6**, coolify **12→8**, PT **429→261**.

### Verificación PT

- `tsc --noEmit` exit 0; `npm run build` verde (11.3 s).
- sentinel (CLI fijado 0559576): **0e/82w/10h** — 1 warning menos que el baseline 83w (restos de los borrados de VAR-3), sin regresión.
- Sin ediciones de código en PT (solo docs) → sin riesgo visual/behavior; WIP del usuario intacto y sin stage (`Cargo.toml`/lock, `src/services/ai.rs`, `variables.css`, `data/`, `test_prueba.md`).

Commits locales sin push: PT `318A-7V15` (docs), workspace-manager `318A-7V15` (`excepciones.json` + plan §J-11V15). Siguiente frente declarado: ninguno pendiente de PT accionable fuera del WIP del usuario — PT→0 convergido con 536 hallazgos documentados (0 errores).

## 318A-13: Glory Harness — Fase 3 (CLI + daemon NDJSON)

**Pedido del usuario:** "ya está terminado el plan? terminalo" → cerrar el resto del plan
`Agente/planes/plan-glory-harness-2026-09-01.md` (tarea 318A-13). Fase 2 ya completada; esta
entrada registra la **Fase 3 (CLI `run` + daemon)**.

### Qué se hizo

1. **Binario `glory-harness`** (`cli/` crate) con subcomandos:
   - `run` (one-shot CLI): `glory-harness run --prompt "..."` ejecuta un turno y vuelca la
     respuesta a stdout; exit 2 sin prompt, exit 1 si el turno falla.
   - `daemon` (proceso de fondo): TCP en `127.0.0.1:<puerto>` con **NDJSON** (una línea JSON por
     mensaje), token obligatorio (`GLORY_HARNESS_DAEMON_TOKEN` o autogenerado con
     `--mostrar-token`), **multi-sesión** con lock por `session_id` dentro del daemon (requisito
     §6.7: varios consumidores comparten el proceso sin pisarse).
   - `tools` (lista tools agnósticas) y `doctor` (reporte de claves LLM por proveedor).
2. **Persistencia en memoria** (`cli/src/persistencia.rs`): `PersistenciaMemoria` implementa los
   14 métodos de `AgentPersistence` (turnos, mensajes, acciones, memoria, skills, tareas,
   tareas_tomadas, conversación reciente) para el standalone sin BD.
3. **Transporte daemon** (`cli/src/daemon.rs`): protocolo NDJSON `sesion_abrir`/`turno`/
   `sesion_cerrar`; el turno emite el stream de `AgenteEvento` (mismo contrato H3 que SSE) + `done`.
   Auth: bind loopback + token (R2 del plan). Resuelto el gotcha de `tokio::io::WriteHalf<T>`:
   es genérico sobre el tipo de stream (`WriteHalf<TcpStream>`), no un lifetime — las firmas se
   corrigieron a `WriteHalf<TcpStream>`.
4. **`main.rs`**: dispatch de subcomandos, `--version` (`glory-harness 0.1.0 (contrato core 1.0.0)`).

### Archivos (glory-harness, commit `5da99d0`)

- `cli/Cargo.toml` (deps core path + tokio/serde/chrono/uuid/tracing)
- `cli/src/main.rs` (dispatch + doctor + listar_tools)
- `cli/src/run.rs` (nuevo: turno one-shot)
- `cli/src/daemon.rs` (nuevo: daemon NDJSON multi-sesión)
- `cli/src/persistencia.rs` (nuevo: persistencia en memoria)
- `Cargo.lock`

### Evidencia / verificación

- `cargo build` + `build --release` OK (target en `C:\tmp\glory-harness-target`).
- `cargo test --workspace`: core **44/44**, cli compila (0 tests propios).
- `cargo clippy --workspace --all-targets`: limpio en `cli` (4 warnings restantes son de
  `glory-harness-core`, preexistentes y fuera del alcance de la Fase 3).
- Subcomandos probados: `--version` ✓, `tools` ✓, `doctor` ✓ (1 clave deepseek), `run` sin prompt →
  exit 2 ✓.
- **Daemon NDJSON end-to-end** (Node client TCP): `sesion_abrir` → `sesion_abierta`; `turno` →
  stream `contexto_detalle` + `turno_done`; `sesion_cerrar` → `sesion_cerrada`; **token inválido →
  `error` "token no autorizado"** (R2).
- Gate glory-harness (`sentinel check 318A-13 --workspace . --stages scripts/quality/stages.json`):
  **PASS** — 0 errores, 5 warnings estructurales heredados de core.

### Incidencias resueltas

- **Disco C lleno (os error 112)** durante clippy: `C:\tmp` a 0 bytes; liberado borrando el target
  de la rama inactiva `glory_backend_main` (9.4 GB, no tocado desde 01-09) + swaps temporales de
  glory-harness (`glory-harness-swap`, `glory-target-harness`). Regla AGENTS §1 aplicada.
- **E0107 `WriteHalf`**: `WriteHalf` es genérico sobre `TcpStream`, no un lifetime; firmas
  corregidas en `daemon.rs`.
- **Warning `SalidaTurno.turno_id` nunca leído**: campo eliminado del struct.

### Pendiente (Fase 4, a validar con el usuario)

- **Fase 4 (segundo consumidor)**: identificar un proyecto real (p. ej. WANDORIUS o un script) que
  consuma `glory-harness run`/lib; documentarlo en README. Requiere decisión del usuario sobre qué
  consumidor usar.
- **Turno real con proveedor externo**: el transporte daemon y los subcomandos funcionan, pero un
  turno con respuesta de texto real requiere una clave LLM en env (hoy solo `doctor` confirma la
  presencia de claves; `run --prompt` sin proveedor no emite texto). Pendiente de evidencia SSE
  funcional con proveedor.
- **Gate task** sigue bloqueado por desync varsense preexistente (318A-6VAR), ajeno a esta fase.
