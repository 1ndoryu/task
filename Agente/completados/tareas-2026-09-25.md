# Tareas completadas — 2026-09-25

## 259A-1 — Triaje warnings sentinel 0.7.13 TASKS (commit `5863e21`)

**Medición inicial (0.7.13):** 1003 archivos, 20 con violaciones, 28w+5h.
**Medición final:** 10 archivos, 9w+5h. 0 errores.

### Fixes reales aplicados
- `src/handlers/agente.rs`: turno SSE monolítico → 4 helpers (`verificar_mensaje_y_cuota`,
  `construir_runtime_turno`, `sembrar_permisos_turno`, `mapear_evento_sse`);
  fix tipos (`modo: String`), `unused_async`, `map_or(0,…)` → clippy 121→120.
- `frontend/.../hooks/useConfiguracionTareas.ts`: 11 toggles duplicados → `toggle(clave)`.
- `EncabezadoAcciones.tsx`: `useMenuCrear` co-ubicado + `cerrarMenuCrear` (handlers fuera del JSX).
- `ModalCrearRecordatorio.tsx`: `useModalCrearRecordatorio` co-ubicado + `quitarAdjunto`;
  `<input type=file>` → `<Input tipo=file>`; `EncabezadoPerfil.tsx`: wrapper `div.inputOculto` eliminado.
- `ModalDependencias.tsx`: `useModalDependencias` co-ubicado.
- Monocromo: `panelGruposFb.css:587` y `mapaCalorHabito.css:335` rgba→tokens `*Rgb`;
  `panelAyuno-modales.css:313` y `ventanaOportunidad.css:151` drop-shadow→`--dashboard-sombraSutil`.

### FPs documentados (waivers con `[por que]`, sin renombrar `inline-style-prohibido`)
- `encabezado-botones.css` / `encabezado-movil.css`: `sentinel-disable-file css-adhoc-button-style`
  (subsistema header responsive, precedente `inline-fix.css`).
- `inline-style-prohibido` restantes: posicionamiento dinámico ancla (SelectorRepeticionPill,
  EncabezadoAcciones), layout acordeón (TabsPanel), ancho % datos (barraContexto).
- `TabsPanel` islas `<span>` (interactividad en wrapper), `mensajes.tsx` render-diff,
  `ModalCrearRecordatorio`/`ModalCreacionRapida` patrón bespoke compartido,
  `sqlite-carga-N` startup-secuencia + framework, `large-interface-isp` contratos props/config.

### Verificación
- `tsc --noEmit` exit 0; `node scripts/run-with-db.mjs check` OK (32.66s);
  `cargo clippy` 120 warnings (121 antes, 2 de `agente.rs` resueltos).
- `check:front` BLOQUEADO por boundary pre-existente `main.tsx` (`gloryDashboard`/`localStorage`) — no tocado.

### Pendiente
- `workspace-manager` 10 warnings + `glory-harness` 24 warnings (triaje).
- Reporte INMOBILIARIA, push TASKS (decisión usuario), releer roadmap.
