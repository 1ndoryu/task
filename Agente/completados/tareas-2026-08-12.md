# Tareas cerradas 2026-08-12

## 128A-1 — Reparar Git tras renombrar carpetas y crear rama `task-rs`
- **Que:** El consumidor (`PROYECTO TASKS`) y el repo base (`WANDORIUS`, antes `glory-rust-template`) dejaron de reconocerse tras renombrar sus carpetas: el worktree apuntaba a rutas absolutas antiguas. Se relinkearon los worktrees (principal + submodules `glory-rs`, `tools/sentinel`, `tools/varsense` en `glory-rs-rest`/RESTAURANTE) editando solo metadatos de Git, sin tocar contenido ni ramas. El consumidor quedó en la rama nueva `task-rs` (antes `migracion-rust`). Verificado: `git status`/`log`/`worktree list` funcionan, commits de migración (`4b31b4c2`, `527f7461`, `745f2695`) alcanzables, WANDORIUS y RESTAURANTE intactos en sus ramas (`wandorius`, `glory-rs-rest`).
- **Archivos:** `PROYECTO TASKS/.git`, `PROYECTO TASKS/glory-rs/.git`, `RESTAURANTE/.git`, `RESTAURANTE/glory-rs/.git`, `RESTAURANTE/tools/{sentinel,varsense}/.git`, `WANDORIUS/.git/worktrees/{glory-react-logic-rs,glory-rs-rest}/{gitdir,modules/*/config}`.
- **Evidencia:** `git -C PROYECTO TASKS status --short --branch` → `## task-rs`; `git -C WANDORIUS worktree list` sin `prunable`; `git log` muestra timeline/notificaciones/shared/teams/reparenting/notas.
- **Gotchas:** Los archivos `.git` (ocultos) no se editan con `str_replace`; `write_file` los sobrescribe sin problema. Además del `.git` del worktree hay que corregir el `core.worktree` de cada submodule (relativo) y el `gitdir` dentro del repo base; si no, Git falla con `cannot chdir`. No ejecutar `git worktree prune` hasta confirmar que el checkout renombrado está relinkado.
- **Sentinel:** No aplica (no se tocó el gate).
- **GLORY:** No aplica (solo metadatos de Git de los consumidores).

## 128A-2 — Cerrar bloque: gate del consumidor verde, inventario del slice `own + shared` y regularización documental
- **Que:** Se ejecutó el gate mínimo del consumidor en `task-rs` (todo verde) y se cerró el inventario semántico del slice pendiente (asignaciones + proyección `own + shared`), dejando documentados sus bloqueos reales. Se regularizó la deuda documental del bloque de migración (trabajo del 11-08 ya verificado pero no registrado) y se corrigió la referencia de rama del roadmap.
- **Archivos:** `roadmap.md` (referencia `task-rs`, bloque actual con inventario cerrado y bloqueos), `Agente/documentacion/migracion/inventario-slice-asignaciones-proyeccion-2026-08-12.md` (nuevo), `Agente/completados/tareas-2026-08-12.md` (este), `Agente/lecciones/lecciones-aprendidas.md` (lección Git).
- **Evidencia:** `npm run fmt:check` OK; `cargo check --all-targets` OK; `cargo clippy --all-targets` OK; `cargo test` 9/9; `npm run check:front` OK (tsc + boundary); `npm run openapi:export:local` OK; `npm run codegen` ×2 idempotente sin drift (`git diff` vacío en `frontend/src/api/`); `npm --prefix frontend run build` OK.
- **Decisión:** El inventario confirma que la materialización del slice no se puede cerrar sin inventar datos: faltan fixtures legacy restaurables, la decisión asignación = permiso de compartido vs relación separada, la matriz de mapeo de usuarios (id legacy → UUID) y la columna tipada de `asignadoA` (ADR-03). Se registran como bloqueos, no se inventan.
- **Gotchas:** El PostgreSQL temporal del proyecto (`DATABASE_URL` → `127.0.0.1:55455`) no está activo y no hay Docker; los tests HTTP contra DB no se ejecutaron en esta sesión. El PG local (5432) está activo pero sin credenciales coincidentes. El preflight de exposición sigue no verde (fixture de timeout no reproducible) y no autoriza exposición.
- **Pendientes reales:** desbloquear la materialización del slice (fixtures/mapeo/columna), gate de exposición (memoria, `X-Forwarded-For`, cookies detrás del proxy, `docker build`/healthcheck, ingress), y revisar los cambios locales sin commitear preexistentes en WANDORIUS (`roadmap.md`) y RESTAURANTE (`roadmap.md`, `tools/sentinel`).

## 128A-3 — Preflight de exposición reproducible en local + ADR de asignación
- **Que:** Se cerró el fallo de reproducibilidad del fixture de timeout del preflight y se adoptó el modelo de asignación de tareas. El fixture de timeout ahora espera de forma determinista a que el lock de `dashboard_tasks` aparezca en `pg_locks` (en vez del sleep fijo de 250 ms que era intermitente en este host) y exige backend con `REQUEST_TIMEOUT_SECONDS=3` (< duración del lock, 6 s); con eso la baseline completa local pasó en verde. Se documentó el ADR que decide que la asignación es un campo tipado de la tarea (no una relación separada) y no otorga acceso por sí sola.
- **Archivos:** `scripts/preflight-exposure.mjs` (fixture determinista), `Agente/documentacion/migracion/adr-asignacion-tareas-2026-08-12.md` (nuevo), `roadmap.md` y `plan-migracion-...-2026-08-11.md` (estado del preflight actualizado), `temp/preflight-final-green.json` (evidencia).
- **Evidencia:** baseline completa `--users 20 --rps 10 --duration-seconds 300` contra PostgreSQL temporal local (clúster `initdb` en `C:/tmp/glory-pg-temp`, puerto 55455) y backend en `127.0.0.1:3100`: **PASS** — 3000 muestras, 0 errores, p50 109.65 ms, p95 210.9 ms, `requestTimeout: 408`, rate limit 429 tras presupuesto, sesión expirada 401/limpieza OK, contratos de notas/jerarquía/equipos/compartidos/identidad OK, limpieza OK, 0 fallos. Ejecución corta de validación previa también PASS.
- **Gotchas:** El rate limiter de auth es en memoria y single-replica: una ejecución previa agota el presupuesto y la siguiente falla con `429` en el `register` del preflight; hay que reiniciar el backend entre ejecuciones. El PG temporal del proyecto (`.env` → `127.0.0.1:55455`) estaba caído y sin Docker; se creó un clúster local equivalente con `initdb` + `pg_ctl` y `lc_messages=C` (el locale por defecto rompía sqlx con error no UTF-8). El puerto 3000 lo ocupa un backend viejo de otra sesión (PID propio ajeno); se usó 3100.
- **Sentinel:** No aplica.
- **GLORY:** No aplica (infra de verificación local y documentación).
- **Pendientes reales:** materializar la columna `asignado_legacy_id` + filtro "asignadas" cuando existan fixtures legacy y matriz de mapeo de usuarios; exposición final (Docker/ingress/memoria/`X-Forwarded-For`/cookies detrás del proxy); decidir si se deja corriendo el PG temporal local o se apaga.

## 128A-4 — Materializar el slice own + shared y asignaciones (proyección + columna + mapeo)
- **Que:** Se materializó el slice pendiente usando la semántica extraída del legacy (`glorytemplate` en `C:/Users/Owner/OneDrive/Documentos/WP/app/public/wp-content/themes/glorytemplate`): el dashboard legacy fusiona propios con compartidos (`array_merge` en `DashboardRepository::loadAll`), las tareas llegan por proyecto compartido o asignación (`JSON_EXTRACT(data,'$.asignadoA') = id WP`) y la metadata es `esCompartido/propietarioId/propietarioNombre/propietarioAvatar/miRol`. Se implementó: columna tipada `asignado_legacy_id` + índice + backfill desde el payload, tabla `user_legacy_links` (ADR-02), escritura de la asignación en el PUT de tarea, y la proyección `own + shared` del dashboard (dedup por (propietario, id), hábitos excluidos, tarea compartida directa excluida, `sharedItemsIncluded: true`). Se añadió `checkSharedProjectionContracts` al preflight.
- **Archivos:** `migrations/20260823000000_collaboration_projection.{up,down}.sql`, `src/repositories/dashboard.rs`, `src/models/productivity.rs`, `src/repositories/productivity.rs`, `scripts/preflight-exposure.mjs`, `Agente/documentacion/migracion/{inventario-slice-asignaciones-proyeccion,adr-asignacion-tareas}-2026-08-12.md`, `roadmap.md`.
- **Evidencia:** preflight rápido con el nuevo check PASS (`temp/preflight-projection-quick.json`: `tareas [6001,6002,6003]`, `proyectos [5001]`, metadata completa, `requestTimeout 408`, 0 fallos); baseline completa `--users 20 --rps 10 --duration-seconds 300` PASS (`temp/preflight-final-projection.json`: 3000 muestras, 0 errores, p50 141.42 ms, p95 248.33 ms, `requestTimeout 408`, 0 fallos, cleaned). `cargo check`/`clippy`/`test` (9/9)/`fmt` OK; `openapi:export:local` + `codegen` sin drift; `check:front` OK.
- **Gotchas:** El rate limiter de auth (10/min por IP, in-memory) obliga a reiniciar el backend entre ejecuciones del preflight. La semántica legacy de "tarea compartida directa" NO entra al dashboard (solo proyecto compartido o asignación) — la prueba lo fija. Clippy `too_many_arguments` en builders internos de fila: se permite con comentario (precedente ya existente en `handlers/mod.rs`).
- **Sentinel:** No aplica.
- **GLORY:** La lógica de proyección es específica del consumidor (dashboard propio), no del core; no se mueve a `glory-rs`.
- **Pendientes reales:** (superado por 128A-5) modelo nativo sin mapeo legacy; quedan UI del filtro "asignadas", notificación `tarea_asignada` + eventos de timeline en la mutación y exposición final (Docker/ingress).

## 128A-5 — Rework a modelo nativo: quitar mapeo legacy de usuarios/asignación
- **Que:** Por decisión del usuario el sistema arranca con **datos vacíos** y todo el mapeo legacy de usuarios/asignación se elimina: se borró la migración `20260823000000_collaboration_projection` (tabla `user_legacy_links` + columna `asignado_legacy_id` + backfill) y su rama en el código, quedando solo la migración nativa `20260824000000_assignment_user` (`dashboard_tasks.asignado_user_id UUID REFERENCES users(id) ON DELETE SET NULL` + índice parcial). El PUT de tarea persiste `asignadoA` del payload **como UUID**; la proyección resuelve asignadas = `asignado_user_id = usuario actual` y la metadata usa UUID (`propietarioId`). El preflight se reescribió en modo nativo (fixtures asignan por UUID, sin links). Se adapta el front: `asignadoA` se envía como UUID y `propietarioId` se consume como UUID; los shapes (`esCompartido`/`miRol`/`legacy_id` de ítems) se conservan.
- **Archivos:** `migrations/20260823000000_collaboration_projection.{up,down}.sql` (borradas), `migrations/20260824000000_assignment_user.{up,down}.sql` (nativas), `src/models/productivity.rs`, `src/repositories/productivity.rs`, `src/repositories/dashboard.rs`, `scripts/preflight-exposure.mjs`, `Agente/documentacion/migracion/{adr-asignacion-tareas,inventario-slice-asignaciones-proyeccion}-2026-08-12.md`, `roadmap.md`, plan de migración.
- **Evidencia:** DB recreada desde cero y verificada sin `user_legacy_links` ni `asignado_legacy_id`; `cargo fmt`/`check --all-targets`/`clippy`/`test` (10/10) OK; `openapi:export:local` + `codegen` ×2 sin drift; `check:front` y build frontend OK; preflight nativo: los 16 checks funcionales (incluido `sharedProjection` con `propietarioId` UUID) PASS. La **baseline completa de carga se reejecutó dos veces el 12-08 y falló el umbral p95** (332/343 ms vs <300 ms) con 0 errores y todo 200: la causa es presión de memoria del host (1.6–2.1 GB libres, Chrome/ChatGPT abiertos; los EXPLAIN de dashboard/activity/notes son sub-ms y la lectura es un número fijo de consultas indexadas, sin N+1). Se conserva la evidencia de las baselines verdes previas (`temp/preflight-final-green.json` 210.9 ms y `temp/preflight-final-projection.json` 248.33 ms) del mismo path de código bajo menor presión; el preflight queda rojo para exposición hasta re-verificarlo en host estable/CI, sin debilitar el umbral.
- **Gotchas:** al eliminar una migración aplicada en una DB existente hay que recrear la DB (drop + re-migración) para que no quede rastro; el backend viejo debe detenerse antes del drop. La decisión "no se arrastra identidad de WordPress" queda en el ADR: un ETL futuro mapearía en la importación, fuera del modelo de asignación.
- **Sentinel:** No aplica.
- **GLORY:** No aplica (decisión de identidad del consumidor).
- **Pendientes reales:** UI del filtro "asignadas" (envío/lectura de UUIDs), notificación `tarea_asignada` + timeline en la mutación, prueba de carrera de asignación, exposición final (Docker/ingress).
