# Tareas completadas — 2026-08-19

Bloque UI/UX + bugs (plan `plan-ui-polish-198A-2026-08-19.md`, cerrado).

## 198A-1 — Persistencia de preferencias (explicación, sin cambios)
El nº de columnas vive en `glory_config_layout` (localStorage del navegador).
`usePreferenciasServidor` sube el blob a `PUT /api/dashboard/settings` como backup
y `aplicarPreferenciasServidor` restaura SOLO las claves ausentes localmente
(`preferenciasUsuario.ts`). Por eso dos navegadores distintos conservan cada uno su
nº de columnas (por diseño, config por dispositivo; el servidor es respaldo para
navegador nuevo, no un unificador). Dos pestañas del MISMO navegador comparten
localStorage y se sincronizan en vivo vía el evento `storage` (gana el último write).

## 198A-2 — Paneles ocultos: sin chips en el header, sin descripción en el modal
- Eliminados `PanelesOcultosEncabezado` (chips junto al botón Layout) y sus props
  (`panelesOcultos`/`onMostrarPanel`) de `EncabezadoAcciones`, `DashboardEncabezado`
  y `DashboardIsland`.
- Eliminada la descripción `gestionPanelesDescripcion` del `ModalGestionPaneles` y su CSS.

## 198A-3 — seccionAcciones responsive (botón de 3 puntos)
Nuevo `AccionesPanelResponsivas` (shared): mide el ancho real de las acciones con una
copia invisible (`.seccionAccionesMedidor`) contra el ancho del header menos el título;
si desborda, colapsa a un botón `MoreHorizontal` que abre un popover con las mismas
acciones en columna (cierra con click fuera/Escape). Aplicado en `DashboardPanel` y
`SeccionEncabezado` (cubre todos los paneles, incluido el sidebar). Verificado en vivo:
estrechar → colapsa → popover con 8 acciones → restaurar → vuelve.

## 198A-4 — Selector de grupos unificado sin bordes
`SelectorGrupo.tsx` ahora usa una única `FilaCrearGrupo` para ambas variantes (pill y
badge); CSS unificado en `selectorGrupo.css`: input sin borde, botón "+" sin borde,
sin separador ni margen superior. Eliminadas `selectorGrupoCrearPill`/
`selectorGrupoInputPill`. `selectorBadgeMenu` intacto.

## 198A-5 — Iconos de importancia/urgencia centralizados
- Sin `fill` en ningún icono de nivel (era incoherente: Muy Alta/Alta con relleno, el
  resto sin): `nivelesConfig.tsx` (opciones y decoración), `PropiedadesCompactas`
  (Flag/Zap), `SelectorImportanciaPill` (Star), `ListaHitos` (Flag).
- **Bug real**: `--dashboard-estadoBaja: #000000 0` era CSS inválido → iconos/badges
  "Baja" invisibles en el tema base. Corregido a `#6b7280`.
- Escala de importancia coherente: Baja ahora usa `estadoBaja` (antes `textoApagado`,
  quedaba más claro que Muy Baja).
- Icono de urgencia unificado a `Zap`: `OpcionesCreacionRapida` (tarea/proyecto),
  `BottomSheetProyecto` y `useBottomSheetProyecto` (antes Clock/Hash). Importancia de
  hábito usa `Star` en creación rápida (antes Flag).

## 198A-6 — Dependencias: modal con opciones + acceso directo en menú contextual
- **Bug**: el modal Editar Tarea (`ModalesTareas`) no pasaba `tareas`/`habitos` →
  `ModalDependencias` abría sin opciones. Ahora pasa `dashboard.tareas` y
  `dashboard.habitos`. Verificado: 7 resultados (tareas + hábitos + subhábitos).
- **Acceso directo**: opción "Dependencias" en el menú contextual de tareas, hábitos
  y subhábitos. La solicitud viaja en `dependenciasUIStore`
  (`solicitarAbrirDependencias`) y los formularios (`FormularioTareaModerno`,
  `FormularioHabitoModerno`) la consumen al montar → el modal de edición abre con el
  selector de dependencias ya abierto. Verificado en vivo para tarea y hábito.

## 198A-7 — Eliminado `dashboard-footer`
Quitado `DashboardFooter` (componente, export, CSS en encabezado-base y movilBase,
textos en `appTexts`) y su render en `DashboardIsland`.

## Extra (mismo bloque)
- **Bug preexistente corregido**: `<button>` anidado en `SelectorRepeticionPill`
  (HTML inválido, warning de React). El "quitar repetición" ahora es `<span role=button>`.

## Evidencia
- `npx tsc --noEmit` limpio.
- Suite `.freebuff/verify-parity.mjs`: **94/94**.
- En vivo (preview :5174): modal Paneles sin descripción y sin chips; colapso a 3
  puntos y restauración; selector de grupo sin bordes; iconos sin relleno con colores
  por nivel (BAJA visible tras el fix de la variable); modal de dependencias con 7
  opciones y apertura directa desde menú contextual (tarea y hábito); sin footer;
  consola sin errores (tras fix del botón anidado).
- Backend `:3001` y vite `:5174` vivos.

## Pendientes / notas
- `Agente/auditoria/` (documentación de la auditoría de contrato del 18-08) quedó
  sin commitear por el agente anterior; no pertenece a este bloque.
- Los paneles minimizados ahora solo se gestionan desde el botón "Paneles" del header.
