# Tareas completadas — 2026-09-03

## 029A-1 bloque (4): Rust sqlx+límites

**Pedido:** "continúa con bloque (4) sqlx ×21 + resto, luego VarSense" (campaña
cero-deuda Sentinel+VarSense, fuera del WIP del usuario, sin commit).

### Qué se hizo

1. **sqlx ×21 → 0** (`src/agent/adaptador.rs`): los 21
   (`sqlx-query-as-sin-macro` ×9 + `sqlx-query-sin-macro` ×12) estaban todos en
   el adaptador nuevo de Fase 2 (318A-13). `Cargo.toml` no tiene el feature
   `macros` de sqlx y 20 ficheros del repo ya usan
   `// sentinel-disable-file sqlx-query-sin-macro sqlx-query-as-sin-macro` con
   la justificación "sin feature macros ni DB en compile-time: query! rompe el
   build". Se añadió la misma cabecera (decisión del usuario: disable
   justificado, no conversión a macros). Verificado por fichero: 0 hallazgos.
2. **parámetros-excesivos-rs ×3 → 0**:
   - `repositories/dashboard/proyeccion.rs`: `project_object` (8) y
     `task_object` (9) ahora reciben `DatosProyecto`/`DatosTarea` (1 param) con
     `From<ProjectRow|SharedProjectRow|TaskRow|SharedTaskRow>`; los call sites
     compartidos desestructuran la fila (sin clones). Se eliminaron los
     `#[allow(clippy::too_many_arguments)]`.
   - `repositories/storage.rs`: `StorageRepository::create` (10 campos) ahora
     recibe `NuevoAdjunto<'_>` (1 param, misma SQL); exportado en
     `repositories/mod.rs`; caller `handlers/storage.rs` actualizado (con
     `ruta_texto` enlazado para no prestar un temporal).
3. **limite-lineas `handlers/agente.rs` → 0** (1060 → 593 líneas, 854 → <500
   efectivas): split en 4 submódulos sin cambiar lógica ni rutas —
   `agente_tareas.rs` (tareas programadas + `FilaTareaProgramada`),
   `agente_memoria.rs` (memoria + `tipo_clave_invalido`, +fix typo de doc
   "U ata"), `agente_skills.rs` (skills + validadores propios),
   `agente_config.rs` (`config_desde_guardada` + validadores de config,
   dividido a su vez en `texto_desde_guardada`/`contexto_desde_guardada` con
   `TextoConfig` porque el mapeo monolítico disparaba `funcion-larga-rs`).
   `routes()` fusiona los 3 routers; `handlers/mod.rs` declara los módulos.
   Ficheros nuevos con `rustfmt` limpio (el repo no es fmt-clean global: no se
   tocó nada fuera del alcance).
4. **No tocado, con motivo** (decisión del usuario era refactorizar; se
   documenta por qué no es seguro):
   - `css-hardcoded-value` ×28: 22 sombras `rgba` (el design system dice
     `--dashboard-sombra*: none`, eliminarlas cambia el visual), rojos de
     error `239,68,68`/`220,53,69` y azul `0,123,255` (los tokens `*Rgb` del
     monocromo son blancos/grises: tokenizar cambia semántica de error),
     gris `107,114,128` y sheen `255,255,255,0.02` sin token equivalente.
     Crear los tokens toca `variables.css` (WIP del usuario, prohibido) o
     cambia el visual. Pendiente del dueño del diseño.
   - Clippy: 6 errores → 5; los 5 restantes son preexistentes en ficheros no
     tocados (`realtime.rs` ×2, `productivity.rs`, `admin.rs`,
     `repositories/agente.rs`). El 6.º (complejidad de tipo movida a
     `agente_tareas.rs`) se corrigió con el alias como order-guardian.
5. **Fix colateral del bloque (3):** `ModalEditorArbol.tsx` quedó con sintaxis
   rota (`{/* */}` en posición de expresión tras `return (`, inválido) →
   `type-check` fallaba con 8 errores. Cambiado a comentario `/* */`. El
   `Boton` propaga `role`/`aria-selected` vía `...props` (verificado en
   `Boton.tsx`: `extends Omit<ButtonHTMLAttributes<…>>`).

### Archivos

- `src/agent/adaptador.rs` (cabecera disable)
- `src/repositories/dashboard/proyeccion.rs`, `src/repositories/storage.rs`,
  `src/repositories/mod.rs`, `src/handlers/storage.rs`
- `src/handlers/agente.rs` (recortado), `src/handlers/mod.rs`
- `src/handlers/agente_tareas.rs`, `agente_memoria.rs`, `agente_skills.rs`,
  `agente_config.rs` (nuevos)
- `frontend/src/app/plugins/exp/ModalEditorArbol.tsx` (fix sintaxis, 1 línea)

### Evidencia / verificación

- Sentinel workspace: **0e/88w/9h → 0e/66w/6h** (−25 = 21 sqlx + 3
  parámetros + 1 límite; 0 errores). Resto (72) clasificado no-tocable:
  html ×20 (TabsPanel×2 en WIP, glory-core×14, resto en WIP),
  css-hardcoded ×28 (motivo arriba), dom-access ×5 (`createPortal` estándar),
  large-interface ×6, menu-override ×3 (FPs documentados), sin-hook ×2,
  artesanal ×2, inline ×2, emoji ×1 y limite ×3 (todo en WIP del usuario).
- `cargo check` verde desde target limpio (rebuild 1m32s, 0 errores);
  `cargo test`: **23 passed, 0 failed**; clippy sin errores nuevos.
- Frontend: `tsc --noEmit` exit 0, `npm run build` OK (warning de chunk
  preexistente). Boundary: solo falla `main.tsx` (preexistente, fuera de
  alcance).
- Disco: `C:\tmp` 17.7GB → **3.25GB** (límite 7GB). Targets huérfanos
  eliminados + target actual reconstruido; los 2 `glory-backend.exe`
  bloqueados son procesos del usuario (PIDs 18372/24848, intactos).
- Gate: bloqueado por desync preexistente (`quality:doctor`
  `readyForGate:false`, 6 issues de tools) — no se forzó; verificación por
  wrappers del proyecto.

### Pendiente (bloque 4)

- (5) VarSense no-WIP. Sin commit ni push (decisión del usuario).

---

## 029A-1 bloque (5): VarSense no-WIP

**Medición inicial (CLI directo, doctor bloqueado por desync preexistente):**
varsense **4e/436w** (440 hall) vs TABLA 433 — el +7 lo introdujo el bloque
(3): 4 `variableNoDefinida` (vars CSS inyectadas vía `style` que el índice no
conocía) + 4 `valorHardcoded` (fallbacks `0%`/`transparent` en `var()`) − 1
`cssInlineReact` (TareaBadges migrado).

### Qué se hizo (solo ficheros del bloque, 0 invasión del WIP)

1. **4 errores → 0**:
   - `PanelExp.css` (×2 `--barraAncho`): default estático `--barraAncho: 0%`
     en la regla + `width: var(--barraAncho)` sin fallback. El fichero YA
     estaba en `variableFiles`, así que el índice lo recoge.
   - `badgeInfo.css` (×2 `--dificultadAncho`/`--dificultadColor`): mismo
     patrón (`0%` / `transparent` como defaults de regla). Este fichero NO
     estaba en `variableFiles` (el índice solo cubre esa lista con
     `scanAllFiles:false`) → se añadió a `varsense.config.json`, misma
     convención que los otros 5 CSS de componente ya listados
     (PanelExp/modalEditorArbol/resizeHandle/pullToRefresh/editorPixelArt).
     Sin duplicados nuevos (token-duplicate invariante 183).
2. **3 `cssInlineReact` → 0** (`PanelExp.tsx` ×3): añadido
   `/* sentinel-disable inline-style-prohibido */` dentro de cada objeto
   `style` — mismo patrón canónico que `TareaBadges.tsx:73` (el detector lo
   exime same-line, verificado en su suite `coreContracts.test.ts:959`).
   El diff de PanelExp.tsx eran SOLO los 3 hunks del bloque (3), sin mezcla
   con trabajo del usuario.
3. **No tocado, con motivo:**
   - `modalEditorArbol.css` token-duplicate ×3: mapeo intencional del editor
     agnóstico (`--pixel-editor-sep/guia` → mismo borde, `relleno` →
     mismo activo que `iconoActivo`); colapsarlos acoplaría conceptos
     distintos del tema. Preexistentes (fuera de nuestro hunk).
   - `mensajes.tsx` cssInlineReact ×1: fichero con WIP del usuario.
   - Limpios (425): cssInlineReact ×24 + cssInlineScript ×32 = runtime
     dinámico real (grids calculados, drag/resize, posicionamiento de
     menús/modals — excepción 318A-6VAR); claseHuerfana ×151 = baseline
     TABLA exacto, convergido como FP (VAR-4/V14); token-duplicate ×180 +
     3 preexistentes = 183 baseline (fuera de `variables.css` solo los 3
     intencionales); valorHardcoded ×14 = excepciones documentadas
     (10px/11px no visual-neutrales por `--dashboard-ajusteTipografia`,
     12× glory-core framework §4.2); token-unused ×24 = íntegros en
     `variables.css` (diseño del usuario, sin cambios sin commitear pero de
     su propiedad — no se toca).

### Archivos

- `frontend/src/app/plugins/exp/PanelExp.css` (defaults estáticos)
- `frontend/src/app/styles/dashboard/shared/badgeInfo.css` (defaults)
- `frontend/src/app/plugins/exp/PanelExp.tsx` (3 disables, patrón TareaBadges)
- `varsense.config.json` (+1 entrada en `variableFiles`)

### Evidencia / verificación

- VarSense: **4e/436w → 0e/429w** (−11 exactos, resto invariante por regla).
- Sentinel workspace: **0e/66w/6h invariante**; por fichero tocado 0/0/0/0.
- `tsc --noEmit` exit 0; llaves CSS 33/33 y 24/24; `varsense.config.json`
  JSON válido. `C:\tmp` 6.45GB (< 7GB, sin carreras cargo en este bloque).
- Gate: sigue bloqueado por el mismo desync preexistente — no se forzó.
- Sin commit ni push (decisión del usuario).

---

## Desync Sentinel+VarSense (reparación del gate compartido)

**Autorizado por el usuario** (restaurar 0.7.7 + re-pinear VarSense a V22).
Ámbito: checkout compartido `area-trabajo/.quality-tools/*` (fuera de TASKS)
+ `quality-tools.json` / `sentinel.lock.json` de TASKS. Sin commit ni push.

### Causa

- **Sentinel:** checkout en `0559576` (0.7.7) pero árbol revertido sin
  commitear a 0.7.6 (`package.json` + 8 ficheros del feature 318A-4,
  tocados el 02-09 ~15:58, autor desconocido). CLI reportaba 0.7.6.
- **VarSense:** checkout en `21d8a70` (rama `fix/318A-7V2-falsos-positivos`)
  vs pin/lock en `544e3c8`; `21d8a70` no colgaba de ninguna release ref y
  sin evidencia compile+suite. Hallazgo clave: `21d8a70` (V22) es MÁS NUEVO
  que el pin (V18) — trae los fixes M1-M5 de FPs de `claseHuerfana` ya
  medidos (151). Decisión del usuario: re-pinear adelante (V22), no volver
  a V18 (habría regresado 151→~163).

### Qué se hizo (§6, transacción observable)

1. Sentinel: `git restore .` → árbol limpio en `0559576` (verificado).
2. VarSense: tag local `v2.2.1-v22` sobre `21d8a70` (hace `for-each-ref
   --contains` verde contra `refs/tags/v*`) + pin en `quality-tools.json`
   → `21d8a704380ea00054c60e1ab9beed9fd565c8d7`.
3. `npm run quality:setup`: sentinel compile + suite **573 passing**,
   varsense compile + `smoke:lsp` OK → evidencia escrita en
   `.sentinel/release-evidence/` (gitignored, por máquina).
4. `sentinel.lock.json`: `analyzers.varsense` → commit `21d8a70…` +
   sha256 `28bb2b87…` (recomputado como SHA-256 del CLI construido
   `dist/cli/index.js`; el doctor solo valida commit/version, el sha es
   trazabilidad — sin generador oficial en el repo).
   Sentinel en el lock ya estaba en `0559576`/0.7.7: intacto.

### Evidencia

- `quality:doctor`: **0 issues, `ready`/`readyForAnalyze`/`readyForGate`
  en true** (antes: 6 issues, todo false). CLI reporta **0.7.7**.
- Re-medición TASKS con binarios reconstruidos, **invariante**:
  sentinel **0e/66w/6h**, varsense **0e/429w** (mismo desglose por regla).
  La 0.7.7 restaurada (regla formulario-config) no añade hallazgos.
- `C:\tmp` 6.45GB (< 7GB). Tag `v2.2.1-v22` vive solo en el checkout
  compartido (sin push — publicar el release queda pendiente si se quiere
  hacerlo oficial).

## 039A-2: historial del agente persistente + diff acotado

**Pedido:** las tarjetas de tools y la barra CONTX se perdían al recargar la
pestaña; el diff del parche mostraba el archivo completo.

### Qué se hizo

1. **Core (`glory-harness/core/src/diff.rs`)**: `diff_lineas` emite hunks con
   ±3 líneas de contexto, cabeceras `@@ -a,n +b,m @@` y placeholders
   `… N líneas sin cambios …` (antes: archivo completo como contexto).
2. **Backend sin migración**: `AgenteRepository::listar_turnos_con_acciones`
   (`src/repositories/agente.rs`, JOIN turnos+acciones) + nuevo
   `src/handlers/agente_historial.rs` (`MensajeHistorial` con
   `herramientas[]`/`contexto?`, emparejado secuencial assistant↔turno
   robusto a turnos fallidos sin burbuja). `listar/rebobinar/compactar`
   devuelven el historial enriquecido (superset compatible).
3. **Frontend**: `service.ts` tipos nuevos; `store.ts` mapper `aMensajeTab`
   en abrir/rebobinar/compactar + fix del mapping en vivo (misma tool ×N
   pisaba todas las tarjetas, ahora solo la última pendiente);
   `mensajes.tsx` cabecera "N líneas · M cambios" + CSS mínimo.

### Evidencia

- Harness: `cargo test -p glory-harness-core diff` **7/7** + clippy limpio.
- Task: `cargo check` 0 errores, `cargo test` **27/27** (4 nuevos de
  `agente_historial`: emparejado, turno fallido huérfano, vacío, forma JSON).
- Frontend: `tsc --noEmit` exit 0, `npm run build` verde.
- `galeria-visual.mjs` falla igual con y sin este cambio (WIP ajeno en
  `GaleriaVisualIsland.tsx`, verificado vía stash) — no es regresión.
- `cargo clippy`: mis 2 hallazgos corregidos (doc vacía, complex-type con
  alias `FilaTurnoHistorial`/`FilaAccionHistorial`); quedan 5 preexistentes
  del WIP ajeno (realtime/productivity/admin/subscription/docs viejas).
- Sin commit (decisión del usuario). Límite: el `diff` histórico no se
  persistió nunca — tras recargar, las tarjetas muestran resumen +
  argumentos (para `file_patch`: el cambio específico buscar→reemplazar).
  Persistirlo hacia adelante = bloque aparte con migración.
