# Tareas completadas — 2026-04-24

## 243A-6 — avatarPreview cuadrado 1:1
- **Que:** Añadido `aspect-ratio: 1 / 1` a `.avatarPreview` para que no se estire
- **Archivos:** `App/React/styles/dashboard/componentes/perfil.css`
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 243A-7 — selectorRepeticionPanel span centrado
- **Que:** Añadido `display: flex; place-content: center` a `.selectorRepeticionPanel__numero span`
- **Archivos:** `App/React/styles/dashboard/shared/configuracionModerna-adjuntos.css`
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 243A-8 — listaTareasHabito--compacto alineado a izquierda
- **Que:** Cambiado `align-items: center` → `align-items: stretch` en el contenedor compacto. Botón "Añadir" con `align-self: center`. Input con `width: 100%`.
- **Archivos:** `App/React/styles/dashboard/componentes/configuracionHabitos.css`
- **Gotchas:** El botón añadir es hijo directo de `.listaTareasHabito--compacto`, así que se selecciona con `> .boton`
- **Sentinel:** No aplica

## 243A-9 — tareaColapsadorBoton sin flecha, borde ni hover background
- **Que:** Eliminada la flecha ChevronRight del JSX del colapsador. Removidos `border-color` y `background-color` del `:hover`.
- **Archivos:** `App/React/components/dashboard/lista-tareas/TareaConColapsador.tsx`, `App/React/styles/dashboard/componentes/tareas.css`
- **Gotchas:** El icono era importado pero no tenía su propia regla CSS eliminable — había que quitarlo del TSX directamente
- **Sentinel:** No aplica

## 243A-10 — botonPeligro sin hover background
- **Que:** Removido `opacity: 0.8` del hover en proyectos.css. Cambiado rgba background a `background-color: transparent` en accionesItem.css.
- **Archivos:** `App/React/styles/dashboard/componentes/proyectos.css`, `App/React/styles/dashboard/shared/accionesItem.css`
- **Gotchas:** Dos archivos tenían mecanismos distintos para el efecto hover — opacity uno, background rgba el otro
- **Sentinel:** No aplica

## 243A-12 — badgeInfo clickable misma altura que span badge
- **Que:** El badge con `onClick` usa `<Boton>` que por defecto aplica `boton--primario boton--mediano` con padding/height distintos al `<span>` badge. Fix: `variante="icono"` en el Boton + regla CSS `.badgeInfo.boton` que fuerza `padding: 2px 8px`, `font-size` y `line-height` del badge para ganar la especificidad sobre `boton--icono`.
- **Archivos:** `App/React/components/shared/BadgeInfo.tsx`, `App/React/styles/dashboard/shared/badgeInfo.css`
- **Gotchas:** `boton--icono` aplica `padding: var(--dashboard-espacioSm)` que sobreescribía el padding del badge. La doble clase `.badgeInfo.boton` gana especificidad sobre `.boton--icono` sola.
- **Sentinel:** No aplica

## 243A-11 — Eliminar subhábitos desde el panel
- **Que:** El botón de eliminar subhábito tenía `opacity: 0` y solo se mostraba en hover del padre — en móvil/panel era invisible. Fix: `variante="icono"` en el Boton (para que no sobreescriba el CSS customizado), más regla CSS que fuerza `opacity: 1` para `.listaTareasHabito__item--sinDrag`.
- **Archivos:** `App/React/components/dashboard/habitos/ListaSubHabitos.tsx`, `App/React/styles/dashboard/componentes/configuracionHabitos.css`
- **Gotchas:** La clase `--sinDrag` siempre se aplica en `FilaSubHabito` (contexto modal/config), así que es seguro usar esa condición para forzar visibilidad. Sin `variante="icono"`, el boton--primario override hacía que el `opacity` no funcionara.
- **Sentinel:** No aplica

## 243A-13 — Actualizar API nutrición CalorieNinjas → API Ninjas
- **Que:** La API cambió de `api.calorieninjas.com/v1/nutrition` a `api.api-ninjas.com/v1/nutrition` (rebrand). Actualizados: URL base, mensajes de error, comentarios, labels de UI en ConfigDeficitCalorico.tsx y placeholder del input.
- **Archivos:** `geminiCaloriasService.ts`, `useDeficitCalorico.ts`, `ConfigDeficitCalorico.tsx`
- **Gotchas:** El formato de request (GET con `?query=` y header `X-Api-Key`) y el formato de response JSON son idénticos. Solo cambia el dominio.
- **Sentinel:** No aplica

## 243A-14 — Botones primarios fondo blanco letras negras
- **Que:** `.boton--primario` ahora usa `--dashboard-botonPrimarioFondo` (#ffffff) y `--dashboard-botonPrimarioTexto` (#000000). Las variables ya estaban definidas en variables.css. Hover usa `--dashboard-botonPrimarioHover`.
- **Archivos:** `App/Assets/css/ui.css`
- **Gotchas:** La variable `--dashboard-botonPrimarioTexto` ya era #000000 (negro) — solo faltaba conectarla al CSS del botón.
- **Sentinel:** No aplica

## 243A-15 — Iconos Sol/Luna en selector ventana oportunidad
- **Que:** Añadidos dos botones flanqueando el reloj circular del selector: ☀️ (sol) preselecciona 06:00-18:00 y 🌙 (luna) preselecciona 18:00-06:00 (cruza medianoche). La lógica vive en el hook (`preseleccionarDia`, `preseleccionarNoche`), el TSX importa `Sun, Moon` de lucide-react y el CSS actualiza el reloj a flex row con gap.
- **Archivos:** `useSelectorVentanaOportunidad.ts`, `SelectorVentanaOportunidad.tsx`, `ventanaOportunidad.css`
- **Gotchas:** Al preseleccionar, se sincronizan también `setTempInicio`/`setTempFin` para que los inputs de texto reflejen el valor inmediatamente sin esperar re-render del prop. La luna usa ventana que cruza medianoche (18:00 → 06:00), el hook ya soporta este caso.
- **Sentinel:** Falso positivo pre-existente en overlay mobile del componente — ver prevencion/
