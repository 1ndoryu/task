# Tareas completadas — 2026-04-02

## 024A-11 — Corregir errores reales de Sentinel y VarSense
- **Que:** Corregidos todos los errores de variables CSS indefinidas reportados por VarSense. Los 6 errores de Sentinel fueron falsos positivos documentados.
- **Archivos:**
  - `App/React/styles/dashboard/componentes/panelIA.css`: Reemplazadas ~15 variables sin prefijo `--dashboard-` (ej: `--texto-terciario`, `--fondo-terciario`, `--color-primario`) por sus equivalentes existentes. También mapeados valores hardcodeados a variables existentes donde había match exacto.
  - `App/React/styles/dashboard/componentes/configBarraInferior.css`: `--dashboard-fuenteBase` → `--dashboard-tamanoBase`, `--dashboard-textoPrincipal` → `--dashboard-textoNormal`, `--dashboard-fuentePequena` → `--dashboard-tamanoPequeno`
  - `App/React/styles/dashboard/componentes/subhabitos.css`: `--dashboard-borde` → `--dashboard-bordePrincipal`, `--dashboard-texto` → `--dashboard-textoNormal`
- **Gotchas:**
  - `panelGruposFb.css` ya estaba corregido (024A-9)
  - `--mapa-calor-tamano-celda` es variable seteada dinámicamente por JS hook — falso positivo de VarSense
  - Los 6 errores nivel "Error" de Sentinel son todos falsos positivos: Ñ en constante PHP, proc_open confundido con exec, y response keys que sí existen en los controllers
  - Algunos `border-radius` hardcodeados en panelIA.css no se reemplazaron porque las variables del tema (`--dashboard-radioSm/Md/Lg`) están en 0 (diseño plano), y las burbujas del chat necesitan radios reales
- **Sentinel:** Requiere corrección de 4 reglas. Documentado en `Agente/prevencion/prevencion-sentinel-falsos-positivos-2026-04-02.md`

## 024A-12 — Fuente en modal de iniciar sesión
- **Que:** Añadido `font-family`, `font-size` y `color` del dashboard a `.modalContenedor` en `modal.css`. Esto cubre TODOS los modales (login, config, etc.) y no solo `.modalLoginContenido`. Eliminado el `font-family` redundante de `.modalLoginContenido` en `modalLogin.css`.
- **Archivos:** `modal.css`, `modalLogin.css`
- **Gotchas:** El fix anterior (233A-32) solo cubría `.modalLoginContenido` — el header del modal quedaba sin fuente. Ahora `.modalContenedor` hereda fuente, tamaño y color del dashboard globalmente.
- **Sentinel:** No aplica.

## 024A-13 — SVG en landing hero + eliminar landing-features
- **Que:** Reemplazado import de `test.svg` (que no cargaba) por `Task.svg` en el hero de la landing page. Eliminada la sección `#landing-features` completa del componente (HTML + CSS). Eliminados imports de LandingHabitAnimation, LandingProjectsAnimation y LandingSortingAnimation.
- **Archivos:** `Landing.tsx`, `landing.css`
- **Gotchas:** Se eliminaron ~170 líneas de CSS de la sección features (estilos de grid, cards, animaciones de feature)
- **Sentinel:** No aplica.

## 024A-15 — Fix VarSense: reemplazar variables CSS indefinidas con tokens existentes
- **Que:** Corregidos los errores de variables CSS indefinidas reportadas por VarSense en `.varsense-report.md`. Sin crear variables nuevas innecesarias.
  - `--dashboard-fondoInput` (3 usos en panelGruposFb.css) → `--dashboard-fondoTerciario`
  - `color: var(--dashboard-texto)` en panelGruposFb.css (mi bug en 024A-14) → `--dashboard-textoNormal`
  - `#f59e0b` y `#fbbf24` en estrellas → `--dashboard-estadoAdvertencia` y `--dashboard-estadoDestacado` (match exacto)
  - `--dashboard-radioBase` en configBarraInferior.css → `--dashboard-radioSm`
  - `--dashboard-fondoItemActivo` con fallback en configBarraInferior.css → `--dashboard-superposicionSutil`
  - `gap: 6px` en badgeInfo.css → `--dashboard-espacioS` (match exacto)
  - `padding: 6px` en tareas.css → `--dashboard-espacioS` (match exacto)
  - `gap: 16px` en modalConfigGlobal.css → `--dashboard-espacioLg`
  - `padding: 10px` en modalConfigGlobal.css → `--dashboard-espacioSmMd` (match exacto, removido sentinel-disable)
  - Variables restantes (hardcoded `56px`, `20px`, `12px`, `14px`, `1px`, vh values) → sin variable exacta, dejadas como están
  - `--mapa-calor-tamano-celda` → variable dinámica seteada por JS, falso positivo, documentado
- **Archivos:** `panelGruposFb.css`, `configBarraInferior.css`, `badgeInfo.css`, `modalConfigGlobal.css`, `tareas.css`
- **Gotchas:** No todas las variables del VarSense report tienen match en el sistema de diseño — las que no tienen equivalente exacto no se tocan (por instrucción "NO CREES NUEVAS VARIABLES INNECESARIAS")
- **Sentinel:** No aplica.

## 024A-14 — Empty state diagnóstico en PanelGruposFb + extracción FilaGrupo
- **Que:** Reemplazado el mensaje genérico "No hay grupos detectados" por un empty state que distingue 3 escenarios:
  1. `todosLosGrupos.length > 0` pero `grupos.length === 0` → filtros activos ocultan grupos (muestra conteo y cuál filtro lo causa)
  2. `estadisticas.total > 0` pero store vacío → todos ocultos en BD
  3. `estadisticas.total === 0` → BD genuinamente vacía (sync nunca funcionó → instrucciones específicas: extensión → Config → Sync)
  Además: extraído `FilaGrupo` a su propio archivo para cumplir límite de 300 líneas. Corregidos emojis unicode `★`, `✓`, `📁` con SVG de lucide-react.
- **Archivos:** `PanelGruposFb.tsx`, `FilaGrupo.tsx` (nuevo), `panelGruposFb.css`
- **Gotchas:** `todosLosGrupos` (sin filtrar) vs `grupos` (filtrado) — el empty state debe usar `todosLosGrupos` para detectar "filtros ocultan" vs "BD vacía". El `★` en estrellas se reemplazó con `<Star fill={...}>` para mantener el relleno condicional sin CSS adicional.
- **Sentinel:** No aplica nuevas reglas.
