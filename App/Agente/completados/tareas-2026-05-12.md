## 125A-3 — Corregir procesamiento de audio WhatsApp
- **Que:** se corrigió el parseo de media wacli para usar `Media.MimeType` real en Whisper/visión y conservar `Media.Type` solo como kind.
- **Archivos:** `App/Services/WhatsAppWebhookService.php`, `App/Agente/documentacion/ia/wacli-whatsapp-2026-05-11.md`, `App/Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** `Media.Type=audio` no empieza por `audio/`; por eso el LLM recibía `[Audio]` sin transcripción.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 126A-1 — Fix: editar_recordatorio no soportaba cambiar recurrence_minutes
- **Que:** El handler `editar_recordatorio` ignoraba el parámetro `recurrence_minutes`, por lo que cambiar el intervalo de un recordatorio (ej: de 30 a 15 min) no surtía efecto. El LLM recibía `exito: true` pero el payload quedaba con el intervalo viejo, haciendo que la siguiente recurrencia se programara con el intervalo incorrecto.
  - Se agregó `recurrence_minutes` al system prompt del LLM en la definición de `editar_recordatorio`.
  - Se agregó recolección de `recurrence_minutes` en el handler, mergándolo al payload via `payload_merge`.
- **Archivos:** `App/Services/AgentChatProcessor.php`
- **Gotchas:** El cambio solo afecta a la acción pendiente actual. Si ya hay ocurrencias futuras creadas, no se actualizan retroactivamente (pero al ejecutarse la siguiente, usará el nuevo intervalo automáticamente).
- **Sentinel:** no aplica (el bug era lógico, no estructural).
- **GLORY:** No aplica (es específico del scheduler del agente).

## 125A-4 — Corregir marcado real de hábito desde WhatsApp
- **Que:** `completar_habito` ahora usa fecha local del canal, renueva `updatedAt`, guarda solo el hábito afectado y re-lee la entidad antes de reportar éxito.
- **Archivos:** `App/Services/AgentChatProcessor.php`, `App/Agente/documentacion/ia/wacli-whatsapp-2026-05-11.md`, `App/Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** `saveAll()` podía retornar true aunque el cambio no quedara observable, y un `updatedAt` viejo permitía que el sync del cliente pisara el cambio.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.