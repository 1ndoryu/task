# Tareas completadas — 2026-04-03

## 034A-1 — Auto-sync categorías/entornos con extensión
- **Que:** Implementado `syncCategoriasDesdeGrupos()` en GruposFbRepository.php. La extensión envía categorías durante sync; el backend las inserta automáticamente si no existen en `categorias_grupos_fb`.
- **Archivos:** `App/Api/GruposFbApiController.php`, `App/Repository/GruposFbRepository.php`
- **Sentinel:** No aplica.

## 034A-2 — Fix "marcar como publicado" (real-time, desmarcar, 24h)
- **Que:** `togglePublicado()` en repo PHP ahora alterna estado. Frontend usa optimistic UI con rollback. `esPublicadoReciente()` calcula ventana de 24h configurable.
- **Archivos:** `GruposFbApiController.php`, `GruposFbRepository.php`, `gruposFbService.ts`, `gruposFbStore.ts`, `FilaGrupo.tsx`
- **Sentinel:** No aplica.

## 034A-4 — SVG 404 en producción (Task.svg)
- **Que:** El tema en producción se llama `glory` no `glorytemplate`. Reemplazada URL hardcodeada por `useGloryContext().themeUrl` que inyecta PHP dinámicamente.
- **Archivos:** `App/React/components/landing/Landing.tsx`
- **Gotchas:** `window.GLORY_CONTEXT.themeUrl` ya existía inyectado por PHP. Solo había que usarlo.
- **Sentinel:** No aplica.

## 034A-5 — Rediseño modal gestión categorías
- **Que:** EditorCategorias ya no se renderiza inline sino dentro de `<Modal>`. Eliminado header redundante (título + botón X) del componente — ahora lo provee el Modal. CSS de header/titulo eliminado.
- **Archivos:** `PanelGruposFb.tsx`, `EditorCategorias.tsx`, `panelGruposFb.css`
- **Gotchas:** `onCerrar` prop sigue necesaria para el hook `useEditorCategorias` (cierra tras guardar). Solo el header visual era redundante.
- **Sentinel:** No aplica.

## 034A-6 — Remove gap de .internaColumna
- **Que:** `.internaColumna { gap: 0; }` en base.css.
- **Archivos:** `App/React/styles/dashboard/base.css`
- **Sentinel:** No aplica.

## 034A-7 — Fix bottom sheet button widths
- **Que:** Solo botones de `menuOpcionesPanelGrupo` y `bottomSheetContenido` mantienen width 100%. Resto de botones con `width: auto`.
- **Archivos:** `bottomSheet.css`
- **Sentinel:** No aplica.

## 034A-8 — Fix botón guardar bottomSheet
- **Que:** `.bottomSheetHabito__botonGuardar` sin fondo, sin borde, color blanco.
- **Archivos:** `bottomSheetCreacion.css`
- **Sentinel:** No aplica.

## 034A-9 — Fix modal sin botón regresar en móvil/tablet
- **Que:** `ArrowLeft` ahora aparece en pantallas ≤768px (tablet) y no solo ≤480px (móvil). Hook `useModal` expone `esTablet`.
- **Archivos:** `Modal.tsx`, `useModal.ts`
- **Gotchas:** Cuando `ocultarBotonCerrar=true` (ej: ModalHabito), screens 481-768px quedaban sin forma de cerrar.
- **Sentinel:** No aplica.

## 034A-10 — Fix padding/nav + scratchpad altura
- **Que:** 
  1. Selector doble `.dashboardContenedor.dashboardContenedor--conNavegacionInferior` para ganar especificidad CSS sobre movilBase.css.
  2. `.panelDashboard--movil` cambiado de `height: auto` a `flex: 1; min-height: 0`. Propagado flex chain a `.internaColumna` y `.dashboardPanel`.
  3. `gap: 14px` hardcodeado → `var(--dashboard-espacioMd)`.
- **Archivos:** `navegacionInferior.css`, `movilGrid.css`
- **Gotchas:** Orden de imports CSS importa para especificidad de selectores con misma cantidad de clases.
- **Sentinel:** No aplica.

## 034A-11 — Fix drawer mostrando páginas de plugins inactivos
- **Que:** Agregada `obtenerPluginDePanelId()` en registroPlugins.ts. `useEncabezadoMovil` filtra panels excluyendo los de plugins inactivos consultando `usePluginsStore.pluginsActivos`.
- **Archivos:** `registroPlugins.ts`, `useEncabezadoMovil.ts`
- **Sentinel:** No aplica.

## 034A-3 — Fix backup consoles
- **Que:** 18 archivos .bat de backup (Daily + Weekly) parchados con auto-ocultamiento VBS. Cuando Task Scheduler los ejecuta, el bat detecta que no tiene `HIDDEN_RUN` y se relanza via `wscript.exe run-hidden.vbs` que ejecuta el mismo bat en ventana oculta.
- **Archivos:** `C:\Users\Owner\AppData\Local\CoolifyManager\*.bat`, `run-hidden.vbs`
- **Gotchas:** Google Drive no funciona: `oauthRefreshToken` vacío y falta archivo Service Account. Requiere ejecutar `coolify-manager auth-drive` manualmente una vez para completar OAuth.
- **Sentinel:** No aplica.
