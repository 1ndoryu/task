# Tareas completadas — 2026-03-25

## 253A-1 — Fix subhábitos: edición nombre, prioridad hereda padre, filtro fantasmas
- **Que:** Corregidos 3 problemas con subhábitos: (1) No se podía cambiar el nombre — añadida edición inline con doble clic o botón lápiz. (2) Prioridad mostraba 'media' en vez de heredar del padre — ahora el panel de ejecución usa la importancia del hábito padre. (3) Subhábitos fantasma sin nombre aparecían en el panel — añadido filtro en useHabitosComoTareas, ListaSubHabitos y validación en crearSubHabito del store.
- **Archivos:** ListaSubHabitos.tsx, useHabitosComoTareas.ts, habitosStore.ts, FormularioHabitoModerno.tsx, ModalHabito.tsx, subhabitos.css
- **Gotchas:** El componente Input de la UI usa `tipo` y `claseAdicional` en vez de props nativos. Se necesitaba `forwardRef` compatible — ya lo soporta.
- **Sentinel:** No aplica — bugs de lógica, no patrón detectable estáticamente.

## 253A-2 — Fix tracked se queda activo al completar hábito
- **Que:** Al completar un hábito (toggleHabito o completarHabitoHoy), el time tracker no se detenía. Agregada verificación y auto-completado del tracking en ambas funciones del habitosStore, usando el mismo patrón que ya existía en useTareas.ts para tareas.
- **Archivos:** habitosStore.ts
- **Gotchas:** Ninguno. El patrón ya existía para tareas, solo faltaba replicarlo para hábitos.
- **Sentinel:** No aplica — bug puntual de integración.

## 253A-3 — Fix CSS: hovers no deseados + overflow cuadros días
- **Que:** Corregidos múltiples issues CSS: (1) Removido hover background en botón "Terminar ayuno" (panelAyunoBotonCircular base y --terminar). (2) Corregido desbordamiento de cuadros de días en historial de cumplimiento y actividad — causa raíz: min-width:12px en celdas impedía shrinking, cambiado a width:100%+max-width. Agregado min-width:0 a semanas y overflow:hidden a historialHabitoContenedor. (3) Removido hover background en deficitComidaEliminar. (4) Forzado background:transparent en hover de creacionRapidaBotonEnviar para que .boton--primario:hover no pinte fondo blanco.
- **Archivos:** panelAyuno-base.css, panelDeficitCalorico.css, modalCreacionRapida.css, mapaCalor.css, mapaCalorHabito.css, historialHabito.css
- **Gotchas:** El overflow de cuadros fue reportado 4 veces. Las correcciones anteriores solo agregaban overflow:hidden a contenedores sin eliminar la causa raíz (min-width en celdas).
- **Sentinel:** Podría detectarse botones con hover background cuando la clase sugiere "ghost" o "circular".

## 253A-4 — Permitir más altura máxima para el panel de nota
- **Que:** El resize handle del scratchpad (panel de notas) tenía un máximo hardcodeado de 600px. Incrementado a 1000px para permitir notas más largas sin necesidad de scroll.
- **Archivos:** useScratchpad.ts
- **Gotchas:** Ninguno. Solo 2 líneas con Math.min(600,...) que limitan el drag resize.
- **Sentinel:** No aplica — cambio de constante numérica.

## 253A-5 — Icono de IA en barra de paneles ocultos + quitar del encabezado
- **Que:** El panel IA mostraba un icono genérico (LayoutGrid) al minimizarse porque faltaba la entrada 'ia' en PANELES_INFO_FALLBACK. Se agregó el icono Bot al fallback. También se quitó el icono Bot del SeccionEncabezado del panel IA (el usuario no lo quería ahí).
- **Archivos:** PanelIA.tsx, BarraPanelesOcultos.tsx
- **Gotchas:** El archivo inicializarPaneles.ts es .ts (no .tsx) así que no se puede usar JSX para el campo icono del registro. El fallback en BarraPanelesOcultos ya cubre el caso.
- **Sentinel:** No aplica.

## 253A-6 — Permitir quitar fecha en modal de creación rápida
- **Que:** El menú de fecha del modal de creación rápida no tenía opción para quitar una fecha ya seleccionada. Se agregó opción "Sin fecha" al inicio del menú, y el handler seleccionarFecha ahora limpia la fecha cuando se elige 'ninguna'.
- **Archivos:** MenusCreacionRapida.tsx, useModalCreacionRapida.ts
- **Gotchas:** Ninguno. Patrón idéntico al de proyecto (id 'ninguno' → undefined).
- **Sentinel:** No aplica.

## 253A-7 — UX posponer: renombrar "Posponer hoy" a "Omitir hoy" + separador visual
- **Que:** En el menú de posponer hábitos, "Posponer hoy" se confundía con "Mañana" porque no quedaba clara la diferencia. Se renombró a "Omitir hoy" (y "Deshacer omisión" cuando ya está activo). Se añadió separador visual entre la opción de omisión diaria y las opciones con tiempo. Las opciones de posponer con tiempo (1h, 4h, 8h, Mañana, 2 días, 1 semana) ya existían y cubren la selección de duración.
- **Archivos:** opcionesMenuHabito.tsx
- **Gotchas:** La confusión estaba en que "Posponer hoy" es un toggle booleano (no aparece hoy) mientras que "Mañana" establece una fecha hasta mañana 8am. Con la etiqueta "Omitir hoy" la semántica es más clara.
- **Sentinel:** No aplica.

## 253A-8 — Mejorar prompt IA calorías: medidas informales + porciones conservadoras
- **Que:** El prompt de nutrición (`PROMPT_NUTRICION` en `geminiCaloriasService.ts`) sobreestimaba calorías porque: (1) no diferenciaba arroz cocido vs crudo (130 vs 360 kcal/100g), (2) no entendía medidas informales latinas (puños, tajadas, media arepa), (3) asumía porciones de restaurante en vez de hogar. Se reescribió el prompt con: tabla de medidas informales con gramajes, calibración con valores ancla de alimentos comunes venezolanos, instrucción explícita de asumir cocido y ser conservador.
- **Archivos:** geminiCaloriasService.ts
- **Gotchas:** El prompt anterior era "compacto" a propósito para evitar alucinaciones. El nuevo es más largo pero las referencias de calibración son necesarias porque los modelos sobreestiman si no tienen anclas concretas. Se mantiene la instrucción de responder solo JSON.
- **Sentinel:** No aplica.

## 253A-9 — Selector de fecha con calendario custom
- **Que:** Reemplazado el MenuContextual de fecha (solo opciones rápidas: Hoy/Mañana/Semana) por un componente `SelectorFechaCalendario` con calendario visual completo. Incluye: (1) Opciones rápidas arriba (Hoy, Mañana, 7 días, Quitar fecha), (2) Grid de calendario con navegación por mes, (3) Highlights para hoy y fecha seleccionada, (4) Cierre con Escape/clic fuera, (5) Auto-ajuste de posición si se sale de pantalla. Integrado en `PropiedadesCompactas` y `MenusCreacionRapida`. También actualizado `calcularFechaDesdeKey` para aceptar fechas ISO como pass-through y `obtenerEtiquetaFecha` para formatear fechas ISO legibles.
- **Archivos:** SelectorFechaCalendario.tsx (nuevo), useSelectorFechaCalendario.ts (nuevo), selectorFechaCalendario.css (nuevo), PropiedadesCompactas.tsx, MenusCreacionRapida.tsx, ModalCreacionRapida.tsx, useModalCreacionRapida.ts, fechaUI.ts, shared/index.ts
- **Gotchas:** El componente `<Boton>` variante ghost agrega `border: 1px solid` que hay que overridear con `.clase.boton`. Las celdas del calendario (28-42 por mes) usan `<button>` nativo por rendimiento — Sentinel flaggea esto pero es intencional.
- **Sentinel:** Falso positivo: regla "boton-nativo" en celdas del calendar grid donde Boton sería overhead innecesario.

## 253A-10 — Submenú botón + en panel de notas: nueva nota aquí o en ventana
- **Que:** El botón "+" del panel de notas ahora abre un submenú contextual con dos opciones: "Nueva nota" (crea nota en el panel actual, comportamiento original) y "Abrir en ventana" (crea nota nueva y abre inmediatamente el modal expandido con lista de notas). Permite trabajar con múltiples notas simultáneamente.
- **Archivos:** PanelScratchpad.tsx
- **Gotchas:** Boton soporta forwardRef, necesario para posicionar el MenuContextual debajo del botón+.
- **Sentinel:** No aplica.

## 253A-11 — Panel Grupos FB: backend (DB + API + Repository) + frontend (panel React)
- **Que:** Fase 1 y 2 de la integración de grupos de Facebook. Backend: tablas `glory_grupos_fb` y `glory_categorias_grupos_fb` en Schema.php, GruposFbRepository con CRUD + sync upsert atómico, GruposFbApiController con autenticación dual (cookie WordPress + Bearer token, SHA-256 hash en user_meta). Frontend: gruposFbService.ts (API client), gruposFbStore.ts (Zustand), usePanelGruposFb.ts (hook), PanelGruposFb.tsx (tabla con filtros, estrellas, categorías, menú contextual), panelGruposFb.css (dark theme).
- **Archivos:** Schema.php, GruposFbRepository.php (nuevo), GruposFbApiController.php (nuevo), gruposFbService.ts (nuevo), gruposFbStore.ts (nuevo), usePanelGruposFb.ts (nuevo), PanelGruposFb.tsx (nuevo), panelGruposFb.css (nuevo), inicializarPaneles.ts, generadoresPropsPanel.ts, plan-grupos-fb-2026-03-25.md (nuevo)
- **Gotchas:** Code Sentinel requiere usar componentes UI (Input, Select, Checkbox, Boton) en vez de nativos. OpcionSelect usa `{valor, etiqueta}`.
- **Sentinel:** No aplica — nueva funcionalidad.

## 253A-12 — Extensión FB: sync API + fix 8.1/8.3/8.4
- **Que:** Fase 3 del plan de integración de grupos FB. Cambios en la extensión Chrome:
  (1) **8.4 Fix comentarios**: `hideGroupFromPage()` ya no oculta `[role="article"]` anidados (que son comentarios de Facebook). Solo oculta articles de nivel superior.
  (2) **8.3 Fix popover**: Inyectados estilos CSS completos para `#fbgm-action-popover` y todas sus clases hijas. Sin estos estilos el popover aparecía sin formato.
  (3) **8.1 Fix deja de funcionar**: Reemplazada invalidación permanente del contexto por sistema de tolerancia a fallos (consecutiveFailures counter con MAX_FAILURES=5). Keepalive del service worker con `chrome.alarms` cada 30s.
  (4) **Sync API**: Nuevo `syncGroupsWithApi()` en background.ts que envía grupos al endpoint `/grupos-fb/sync` con Bearer token. Se ejecuta fire-and-forget después de cada detección. Full sync manual desde dashboard. Configuración persistente (ApiSettings) en chrome.storage.local.
  (5) **Dashboard UI**: Sección "Sincronización con Task" en Ajustes con URL, token, toggle de sync automático y botón "Sincronizar ahora".
- **Archivos:** types.d.ts, manifest.json, group-detector.ts, background.ts, dashboard.html, dashboard.ts
- **Gotchas:** chrome.alarms requiere permiso `alarms` en manifest. host_permissions necesita incluir el dominio del backend (`*.nakomi.studio`). Los errores de Code Sentinel en la extensión son falsos positivos (reglas diseñadas para GloryTemplate, no para extensiones Chrome).
- **Sentinel:** No aplica (proyecto externo a GloryTemplate).
