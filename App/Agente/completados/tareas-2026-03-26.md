# Tareas completadas — 2026-03-26

## 263A-1 — Fix cuadros panelActividadMapa overflow (3 tamaños configurables)
- **Que:** Corregido el overflow de cuadros del mapa de calor reportado 5 veces. Raíz: `TAMANO_CELDA` en JS no coincidía con `max-width` del CSS (JS: {10,12,14} vs CSS: {10px,14px,16px}), causando que el auto-cálculo generara más semanas de las que cabían. También `.mapaCalorSemana` tenía `max-width: 22px` hardcodeado sin adaptar al tamaño de celda.
- **Archivos:** `useMapaCalor.ts`, `MapaCalor.tsx`, `mapaCalor.css`, `mapaCalorHabito.css`, `PanelGruposFb.tsx` (fix import no usado)
- **Gotchas:** El desajuste entre JS y CSS existía desde siempre. Las 4 correcciones previas solo agregaban `overflow:hidden` o `min-width:0` a contenedores sin atacar la causa raíz: los valores de tamaño discordantes entre JS y CSS.
- **Sentinel:** No aplica — los tamaños son específicos de este componente.

## 263A-2 — Fix subhábitos fantasma que reaparecen + no se borran
- **Que:** Los subhábitos virtuales (IDs negativos) en el panel de ejecución no se podían eliminar porque el botón llamaba a `eliminarTarea` del tareasStore (donde no existen) → falla silenciosa → "reaparecen". Fix: interceptor `manejarEliminarTareaHabito` que rutea a `eliminarSubHabito` del habitosStore. También agregado `onRehydrate` al persist para limpiar subhábitos con nombre vacío de localStorage.
- **Archivos:** `useHabitosComoTareas.ts`, `useDashboardCompleto.ts`, `generadoresPropsPanel.ts`, `habitosStore.ts`
- **Gotchas:** El patrón de interception ya existía para toggle (`manejarToggleTareaHabito`) pero faltaba para delete. Los subhábitos fantasma (nombre vacío) se crearon antes de que se añadiera la validación y persistieron en localStorage.
- **Sentinel:** No aplica.

## 263A-3 — Duplicar panel notas (reemplaza "Abrir en ventana")
- **Que:** El menú + del panel Notas ahora ofrece "Duplicar panel" en lugar de "Abrir en ventana". Al duplicar, se crea una nueva instancia del scratchpad (e.g., `scratchpad-1`) en la misma columna, justo debajo del original. Los paneles duplicados tienen un botón X para cerrarse. Sistema genérico: `obtenerPanelOBase()` resuelve IDs con sufijo numérico al panel base del registro, `crearDuplicadoPanel()` y `eliminarPanelDuplicado()` son funciones puras en `layoutLogica.ts`.
- **Archivos:** `registroPaneles.ts`, `useConfiguracionLayout.ts`, `layoutLogica.ts`, `DashboardGrid.tsx`, `PanelScratchpad.tsx`
- **Gotchas:** IDs como `deficit-calorico` usan guión, así que el regex `^(.+)-(\d+)$` resuelve correctamente solo si la parte base está registrada. La migración de layout no elimina IDs desconocidos, así que las instancias duplicadas sobreviven recargas. Las funciones de duplicación se extrajeron a `layoutLogica.ts` para no exceder el límite de 300 líneas del hook.
- **Sentinel:** No aplica.

## 263A-4 — Rediseño PanelGruposFb: filtros en header con SelectorBadge
- **Que:** El panel de grupos de Facebook ahora sigue el patrón visual de los demás paneles. Los Select nativos se reemplazaron por `SelectorBadge` (categoría + importancia). La búsqueda se movió al header como input expandible estilo `modalNotasBusqueda--headerCentrado`. El toggle de ocultos es ahora un badge button con icono Eye/EyeOff. Las estadísticas se mantienen como fila compacta debajo del header.
- **Archivos:** `PanelGruposFb.tsx`, `panelGruposFb.css`
- **Gotchas:** Se eliminaron imports de `Select` y `Checkbox` que ya no se usan. La búsqueda se implementa como input colapsable que se abre con click en el icono de lupa y se colapsa al perder foco si está vacío.
- **Sentinel:** No aplica.

## 263A-5 — Modal configuración GruposFb con token y API URL
- **Que:** Agregada sección de configuración para el panel GruposFb en el modal de configuración global. Incluye verificación de token existente, generación de nuevo token, copiado al portapapeles, y muestra la URL de la API. Se agregó icono de Settings en el header del panel para acceso directo. El panel ahora es tratado como plugin con su propia sección de configuración.
- **Archivos:** `ModalConfiguracionGlobal.tsx`, `useModalesDashboard.ts`, `SeccionConfigGruposFb.tsx` (nuevo), `useSeccionConfigGruposFb.ts` (nuevo), `PanelGruposFb.tsx`, `generadoresPropsPanel.ts`
- **Gotchas:** Code Sentinel flaggeó >3 useState en el componente inicial → se extrajo a hook dedicado. Import path del hook corregido (2 niveles, no 3). Se eliminó `seccionSubtitulo` del panel como pidió el usuario.
- **Sentinel:** No aplica.

## 263A-6 — Fix avatarPreview círculo 1:1
- **Que:** El `.avatarPreview` se estiraba en contextos flex porque no tenía `flex-shrink: 0` ni dimensiones mínimas. Agregados `min-width: 60px`, `min-height: 60px` y `flex-shrink: 0` para garantizar que mantiene sus dimensiones 1:1 independientemente del layout padre.
- **Archivos:** `perfil.css`
- **Gotchas:** El width/height ya estaban correctos pero flex-shrink por defecto (1) permitía que el contenedor flex comprimiera el avatar horizontalmente.
- **Sentinel:** No aplica.

## 263A-7 — Click en nota en móvil cierra modal y abre en panel
- **Que:** Al seleccionar una nota en el listado del ModalNotasExpandido en móvil (<=480px), ahora se selecciona la nota en el store y se cierra el modal automáticamente, de modo que el PanelScratchpad muestra la nota seleccionada. Se usa `useEsMovil` directamente en el hook `useModalNotasExpandido` para detectar viewport.
- **Archivos:** `useModalNotasExpandido.ts`
- **Gotchas:** Se envuelve `seleccionarNota` del store en un callback que agrega `onCerrar()` condicional. No se necesita modificar el componente ni el panel.
- **Sentinel:** No aplica.

## 263A-8 — Renombrar "posponer mañana" a "1 día"
- **Que:** La opción "Mañana" del submenú de posponer en hábitos/tareas se renombró a "1 día" porque el usuario no entendía el contexto de "mañana". La lógica (posponer 1 día) no cambió.
- **Archivos:** `opcionesMenuHabito.tsx`
- **Gotchas:** Ninguno. Solo cambio de etiqueta.
- **Sentinel:** No aplica.

## 263A-9 — Duplicar panel scratchpad crea nota nueva
- **Que:** Al duplicar un panel scratchpad, ahora se crea automáticamente una nota nueva para que ambos paneles no compartan la misma nota activa. El menú se renombró de "Duplicar panel" a "Nota en nuevo panel" para mayor claridad. La implementación encadena `layout.duplicarPanel()` + `useNotasStore.getState().crearNuevaNota()` en el callback.
- **Archivos:** `DashboardGrid.tsx`, `PanelScratchpad.tsx`
- **Gotchas:** Todos los paneles scratchpad comparten un solo `notaActiva` global en el store. La solución pragmática (crear nota nueva al duplicar) evita refactorizar a notas por instancia.
- **Sentinel:** No aplica.
