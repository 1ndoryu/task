# Tareas completadas — 2026-05-13

## 135A-1 — Miniagentes: PlannerAgent, ContextAgent, ReminderAgent ✅
- Creados 3 miniagentes en `App/Services/Agents/`
- `ReminderAgent`: normaliza fechas en lenguaje natural via LLM (groq/llama-3.3-70b, temp=0), fallback cuando fecha calculada queda en el pasado
- `ContextAgent`: resume bloques de conversación y comprime contexto maestro (kimi-k2, temp=0.2)
- `PlannerAgent`: descompone solicitudes complejas en pasos, detecta si necesita OpenCode (gpt-oss-120b, temp=0.3)
- `ReminderAgent` integrado en `programar_recordatorio` de `AgentChatProcessor.php`
- `buildContexto()` ahora inyecta fecha/hora actual al inicio del contexto (via `horaLocalActual()`)

## 135A-2 — Reducción y optimización del system prompt ✅
- 9 reglas del bloque REGLAS en `buildSystemPrompt()` reducidas ~25% en tokens
- Eliminadas instrucciones redundantes y verborrea, conservada semántica
- Reglas afectadas: leer_nota, NOTAS, NUNCA eliminar, INFERENCIA, AYUNO, CALORÍAS, crear_tarea (merge), actualizar_contexto_maestro, reportar_contexto+compactar_ahora

## 135A-3 — OpenCode/DeepSeek como canal LLM directo ✅ (documentación)
- Resultado: OpenCode NO es viable como canal LLM (entorno de ejecución de código, no chat API)
- DeepSeek ya soportado como modelo primario via `glory_chatbot_proveedor`/`glory_chatbot_modelo` user_meta
- Admin puede activar: `update_user_meta($userId, 'glory_chatbot_proveedor', 'deepseek')` + `glory_chatbot_modelo = 'deepseek-v4-flash'`
- No se requirió código adicional — la infraestructura ya lo soportaba

## 135A-4 — Auto-reconexión wacli cuando pierde el WebSocket ✅
- Aplicado via SSH directo al servidor (systemd unit no está en git)
- `Restart=on-failure` → `Restart=always`
- Añadido `RuntimeMaxSec=21600` (fuerza restart cada 6h como safety net para procesos zombies)
- Confirmado: servicio activo con `Connected.`, muestra "Until: 06:39:24 CEST"

## 135A-5 — Fix: MemPalace con timeouts frecuentes (~8s) bloquea respuestas ✅
- `MemPalaceService.php`: `TIMEOUT` reducido de 8s a 3s
- Servicio confirmado corriendo en `host.docker.internal:4001` (responde `/health` → `{"ok":true}`)
- La lentitud es del embedding, no del servicio en sí; con 3s se degrada gracefully sin bloquear

---

## Observaciones del ciclo

- **groq 429 TPD**: `openai/gpt-oss-120b` alcanzó el límite diario de 200k tokens el 2026-05-12. El fallback chain (kimi-k2 → llama-3.3-70b → deepseek) debería absorberlo.
- **PHP Fatal 30s**: El timeout de 30s de PHP fue causado por MemPalace 8s + lógica de agente. Ya mitigado con timeout 3s.
- **wacli bug pre-fix**: El daemon perdió conexión a las 18:49 UTC pero siguió vivo (proceso sin reinicio). `Restart=on-failure` no lo atrapó porque el proceso no terminó. `Restart=always` + `RuntimeMaxSec=21600` resuelve ambos casos.
