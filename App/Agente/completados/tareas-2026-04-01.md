# Tareas completadas — 2026-04-01

## 014A-8 — Fix sincronización entre dispositivos (actividad/heatmap)
- **Que:** Cuando un dispositivo remoto completaba una tarea/hábito vía WebSocket, el cache del panel de actividad (heatmap) en el dispositivo local no se invalidaba. Los datos quedaban stale hasta que el TTL de 5 minutos expiraba o el usuario recargaba manualmente.
- **Archivos:**
  - `App/React/hooks/useSincronizacionTiempoReal.ts` — Nuevo callback `onCambioRemotoAplicado` en `CallbacksSincronizacion`
  - `App/React/hooks/dashboard/useDashboardSync.ts` — Conectar `invalidarCache()` al nuevo callback
  - `App/React/services/actividadStore.ts` — TTL reducido de 5min a 60s como safety net
- **Gotchas:** El sistema de listeners de `actividadStore` ya existía (`suscribirACambios`) y `useActividad` ya se suscribía a cambios. Solo faltaba el trigger desde WebSocket.
- **Sentinel:** No aplica (lógica de runtime, no detectable estáticamente)

## 014A-4 — Fix tareas pierden título al cambiar prioridad
- **Que:** Merge defensivo en `TareasRepository::saveAll()`. Al actualizar una tarea existente, se fusionan los datos de BD con los entrantes (`array_merge`). Si llegan datos parciales (ej: solo prioridad via WebSocket sin campo texto), los campos existentes no se pierden.
- **Archivos:** `App/Repository/TareasRepository.php` — pre-fetch de `data` existente + merge en el loop de upsert
- **Gotchas:** Ya se hacía pre-fetch de `id, id_local` — solo se amplió a incluir `data` y se decodifica para merge.
- **Sentinel:** No aplica

## 014A-1 — Fix textarea chat IA: ancho 100% y bordes del botón enviar
- **Que:** El textarea del panel IA no ocupaba el 100% del ancho porque el contenedor flex no tenía `min-width: 0`. El botón enviar mostraba bordes porque `.boton--ghost` define `border: 1px solid` que ganaba en cascada a `.panelIABotonEnviar { border: none }`.
- **Archivos:** `App/React/styles/dashboard/componentes/panelIA.css` — `min-width: 0` en `.panelIAInput .textareaContenedor`, `border: none !important` en `.panelIABotonEnviar`
- **Gotchas:** `.boton--ghost` en `App/Assets/css/ui.css` línea 71 tiene `border: 1px solid var(--dashboard-bordePrincipal)`. Sin `!important` no se puede overridear porque la especificidad es la misma y `.boton--ghost` carga después.
- **Sentinel:** No aplica

## 014A-2 — Fix textarea notas: altura 100% del bloque
- **Que:** `.scratchpadEditor` no era flex container (`display: flex` faltaba), así que `.textareaContenedor` (hijo directo) con `flex: 1` no resolvía. La cadena de herencia de altura se rompía en ese punto.
- **Archivos:** `App/React/styles/dashboard/componentes/scratchpad-vistaPrevia.css` — añadido `display: flex; flex-direction: column` a `.scratchpadEditor`
- **Gotchas:** La cadena `.scratchpadPanel` → `.scratchpadEditor` → `.textareaContenedor` → `textarea` requiere que CADA nivel sea flex container para que `flex: 1` propague correctamente.
- **Sentinel:** No aplica
