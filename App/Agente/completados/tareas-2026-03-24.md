# Tareas completadas — 24 de marzo de 2026

## 243A-1 — Config IA movida al modal global
- **Qué:** El botón de Settings del panel IA ahora abre el modal global en la sección "Asistente IA" (bajo grupo Paneles), igual que tareas/hábitos/proyectos. Se eliminó la config inline colapsable.
- **Archivos:** `useModalesDashboard.ts`, `ModalConfiguracionGlobal.tsx`, `SeccionesConfigPaneles.tsx` (nueva `SeccionConfigIAPanelChat`), `generadoresPropsPanel.ts`, `PanelIA.tsx`, `panelIA.css`
- **Gotchas:** `SeccionConfigGlobal` es un tipo union — agregar 'panelIA' requirió actualizarlo en `useModalesDashboard.ts` y añadir entradas en SECCIONES_SIDEBAR, TITULOS_SECCION y ContenidoSeccion del modal.
- **Sentinel:** No aplica

## 243A-2 — Icono Bot en encabezado del panel IA
- **Qué:** Se cambió `icono={null}` por `icono={<Bot size={14} />}` en `SeccionEncabezado` del PanelIA. Ahora cuando el panel se minimiza se ve el icono de robot en lugar de nada.
- **Archivos:** `PanelIA.tsx`
- **Gotchas:** Bot ya estaba importado del contexto del chat — solo conectarlo al prop del encabezado.
- **Sentinel:** No aplica

## 243A-3 — Botón enviar del chat más grande y separado
- **Qué:** Se cambió `variante="badge"` por `variante="primario"` con clase adicional `panelIABotonEnviar`. Icono de 16px (antes 14px). CSS: 32×32px, border-radius 8px, margin-bottom 2px para alineación visual con el textarea.
- **Archivos:** `PanelIA.tsx`, `panelIA.css`
- **Gotchas:** El botón usa `soloIcono` — `variante="primario"` da buena visibilidad sin necesitar label.
- **Sentinel:** No aplica

## 243A-4 — Modelos Groq actualizados (más inteligentes)
- **Qué:** Se actualizó `MODELOS_IA` en `iaService.ts` con los modelos más capaces de Groq a marzo 2026: GPT-OSS 120B (producción, más inteligente), Kimi K2 262K contexto, Llama 4 Maverick, Qwen3 32B, Llama 3.3 70B versatile, Llama 4 Scout (ultrarrápido). Se eliminaron DeepSeek R1 y Gemma 2 9B (menos capaces).
- **Archivos:** `iaService.ts`
- **Gotchas:** `MODELOS_IA` es `as const` — los tipos se derivan automáticamente.
- **Sentinel:** No aplica

## 243A-5 — badgeGroup gap + altura uniforme de badges
- **Qué:** (1) `.badgeGroup` gap aumentado de `--dashboard-espacioXxs` (2px) a `--dashboard-espacioXs` (4px) — separación visible entre badges. (2) Se agregó `button.badgeInfo` CSS para normalizar altura/padding de botones renderizados como badge (antes `boton--mediano` los hacía más altos que un span.badgeInfo). (3) `.badgeInfo--habito` padding corregido de `2px 6px` a `2px 8px` para igualar el estándar.
- **Archivos:** `badgeInfo.css`
- **Gotchas:** `BadgeInfo` con onClick usa `<Boton>` sin variante, que por defecto añade `boton--primario boton--mediano`. La regla `button.badgeInfo { height: auto; ... }` tiene especificidad (0,1,1) que supera a `.boton--mediano` (0,1,0), sin necesitar `!important`.
- **Sentinel:** Posible regla para detectar `<Boton claseAdicional="badgeInfo">` sin override de tamaño.
