# Tareas completadas â€” 24 de marzo de 2026

## 243A-1 â€” Config IA movida al modal global
- **QuÃ©:** El botÃ³n de Settings del panel IA ahora abre el modal global en la secciÃ³n "Asistente IA" (bajo grupo Paneles), igual que tareas/hÃ¡bitos/proyectos. Se eliminÃ³ la config inline colapsable.
- **Archivos:** `useModalesDashboard.ts`, `ModalConfiguracionGlobal.tsx`, `SeccionesConfigPaneles.tsx` (nueva `SeccionConfigIAPanelChat`), `generadoresPropsPanel.ts`, `PanelIA.tsx`, `panelIA.css`
- **Gotchas:** `SeccionConfigGlobal` es un tipo union â€” agregar 'panelIA' requiriÃ³ actualizarlo en `useModalesDashboard.ts` y aÃ±adir entradas en SECCIONES_SIDEBAR, TITULOS_SECCION y ContenidoSeccion del modal.
- **Sentinel:** No aplica

## 243A-2 â€” Icono Bot en encabezado del panel IA
- **QuÃ©:** Se cambiÃ³ `icono={null}` por `icono={<Bot size={14} />}` en `SeccionEncabezado` del PanelIA. Ahora cuando el panel se minimiza se ve el icono de robot en lugar de nada.
- **Archivos:** `PanelIA.tsx`
- **Gotchas:** Bot ya estaba importado del contexto del chat â€” solo conectarlo al prop del encabezado.
- **Sentinel:** No aplica

## 243A-3 â€” BotÃ³n enviar del chat mÃ¡s grande y separado
- **QuÃ©:** Se cambiÃ³ `variante="badge"` por `variante="primario"` con clase adicional `panelIABotonEnviar`. Icono de 16px (antes 14px). CSS: 32Ã—32px, border-radius 8px, margin-bottom 2px para alineaciÃ³n visual con el textarea.
- **Archivos:** `PanelIA.tsx`, `panelIA.css`
- **Gotchas:** El botÃ³n usa `soloIcono` â€” `variante="primario"` da buena visibilidad sin necesitar label.
- **Sentinel:** No aplica

## 243A-4 â€” Modelos Groq actualizados (mÃ¡s inteligentes)
- **QuÃ©:** Se actualizÃ³ `MODELOS_IA` en `iaService.ts` con los modelos mÃ¡s capaces de Groq a marzo 2026: GPT-OSS 120B (producciÃ³n, mÃ¡s inteligente), Kimi K2 262K contexto, Llama 4 Maverick, Qwen3 32B, Llama 3.3 70B versatile, Llama 4 Scout (ultrarrÃ¡pido). Se eliminaron DeepSeek R1 y Gemma 2 9B (menos capaces).
- **Archivos:** `iaService.ts`
- **Gotchas:** `MODELOS_IA` es `as const` â€” los tipos se derivan automÃ¡ticamente.
- **Sentinel:** No aplica

## 243A-5 â€” badgeGroup gap + altura uniforme de badges
- **QuÃ©:** (1) `.badgeGroup` gap aumentado de `--dashboard-espacioXxs` (2px) a `--dashboard-espacioXs` (4px) â€” separaciÃ³n visible entre badges. (2) Se agregÃ³ `button.badgeInfo` CSS para normalizar altura/padding de botones renderizados como badge (antes `boton--mediano` los hacÃ­a mÃ¡s altos que un span.badgeInfo). (3) `.badgeInfo--habito` padding corregido de `2px 6px` a `2px 8px` para igualar el estÃ¡ndar.
- **Archivos:** `badgeInfo.css`
- **Gotchas:** `BadgeInfo` con onClick usa `<Boton>` sin variante, que por defecto aÃ±ade `boton--primario boton--mediano`. La regla `button.badgeInfo { height: auto; ... }` tiene especificidad (0,1,1) que supera a `.boton--mediano` (0,1,0), sin necesitar `!important`.
- **Sentinel:** Posible regla para detectar `<Boton claseAdicional="badgeInfo">` sin override de tamaÃ±o.

## 243A-16  modalContenido: padding 20px directo en el contenedor
- **Que:** `.modalContenido` ahora tiene `padding: 20px` directamente (no en `> *`). Más robusto  CSS de componentes hijos no puede sobrescribirlo.
- **Archivos:** `modal.css`, `modalConfigGlobal.css`.
- **Gotchas:** El formateador revirtió los cambios dos veces (`!important` en `> *`). Solución: padding en el contenedor + overrides explícitos para excepciones.
- **Excepcion configGlobal:** Ahora usa `.modalConfigGlobal .modalContenido { padding: 0 }` (era `.modalContenido > .configGlobalLayout`).
- **Sentinel:** Falsos positivos pre-existentes en `sentinel-disable-line`, no aplica corrección nueva.

## 243A-17  Sistema nutricional propio con Groq (eliminar API Ninjas)
- **Que:** API Ninjas pasó a freemium  `calories`/`protein_g` son premium (\+/mes). Se eliminó la dependencia y se reemplazó por una sola llamada a Groq con prompt compacto que usa el conocimiento USDA del modelo.
- **Archivos:** `geminiCaloriasService.ts`, `useDeficitCalorico.ts`, `useConfigDeficitCalorico.ts`, `ConfigDeficitCalorico.tsx`, `deficitCaloricoStore.ts`, `deficitCalorico.ts`.
- **Decisión:** Se descartó tabla venezolana embebida (approach anterior)  consume contexto y el modelo ya conoce esos alimentos. Prompt compacto: ''use USDA, Venezuelan foods, never fabricate''.
- **Beneficio:** De 2 llamadas (traducir + consultar) a 1 sola. Más rápido y sin API key extra.
- **Sentinel:** No aplica.
