# Tareas completadas â€” 2026-04-04

## 034A-14+034A-15 â€” Sistema de entornos completo (backend + frontend + extension)
- **Que:** Sistema de entornos para grupos de Facebook. Backend: 3 tablas nuevas (entornos, overrides, config), repositorio, controlador REST. Frontend: servicio, store con entornoActivoId y routing de overrides, hook useEntornos, componente SelectorEntornos, integraciÃ³n en PanelGruposFb. Extension: syncEnvironmentsToApi, backendId mapping, override sync con fbGroupId, activaciÃ³n de entorno backend.
- **Archivos:** Schema.php, EntornosGruposFbRepository.php (nuevo), EntornosGruposFbApiController.php (nuevo), GruposFbApiController.php, GruposFbRepository.php, gruposFbService.ts, gruposFbStore.ts, useEntornos.ts (nuevo), SelectorEntornos.tsx (nuevo), PanelGruposFb.tsx, panelGruposFb.css, background.ts (extension), types.d.ts (extension), dashboard.ts (extension)
- **Gotchas:** OverrideGrupoFb tiene campos nullable que no coinciden con GrupoFb â€” se resolviÃ³ usando los datos originales en vez del override tipado. Extension usa string IDs vs backend int IDs â€” resuelto con campo backendId.
- **Sentinel:** No aplica (feature nueva)

## 034A-17 â€” Config duraciÃ³n publicado
- **Que:** UI para configurar cuÃ¡ntas horas dura un "marcado como publicado" en SeccionConfigGruposFb. Backend ya tenÃ­a config table + endpoint desde 034A-14. Se agregÃ³ estado publicadoHoras al hook y secciÃ³n con input numÃ©rico al componente.
- **Archivos:** useSeccionConfigGruposFb.ts, SeccionConfigGruposFb.tsx
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 034A-18 â€” Prompt IA por entorno
- **Que:** Cada entorno de la extensiÃ³n tiene su propio prompt de IA. Se lee primero del entorno activo, fallback a textarea/localStorage. AI classification results ahora usan updateGroupField para respetar overrides de entorno.
- **Archivos:** types.d.ts (extension, +aiPrompt), dashboard.ts (extension), dashboard.html (extension)
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 034A-20 â€” EditorCategorias fixes visuales
- **Que:** Modal sin padding propio (claseContenido modalContenido--sinPadding), padding interno en editorCategorias. Lista sin overflow-y: auto para que pickers no se recorten. Color picker 4 columnas con muestras 26px. Clase CSS modalContenido--sinPadding reutilizable.
- **Archivos:** PanelGruposFb.tsx, modal.css, panelGruposFb.css
- **Gotchas:** El overflow-y: auto de __lista cortaba los pickers absolute â€” era la causa real del "selector aplastado"
- **Sentinel:** No aplica

## 034A-19 â€” Sentinel + VarSense cleanup masivo
- **Que:** Sentinel: 128 â†’ 13 violaciones (8 emoji-en-codigo pendientes de SVG, 3 n-plus-1-query sin soporte disable, 1 Info, 1 Hint). VarSense: 79 â†’ 2 (falsos positivos viewport units). Reemplazo masivo de valores hardcodeados CSS â†’ tokens del design system. Nuevos tokens aÃ±adidos: tamanoDetalle, espacioHairline, espacioLgXl, radioSutil, radioMedio, radioSmMd, radioGrande, mapa-calor-tamano-celda, estadoMuyAltaRgb.
- **Archivos:** variables.css (tokens), panelGruposFb.css, inline-fix.css, panelIA.css, configuracionModerna-tareasCompactas.css, landingAnimations.css, scratchpad-modalNotas.css, mapaCalor.css, badgeInfo.css, subhabitos.css, bottomSheetCreacion.css, modalVersiones.css, selectorFechaCalendario.css, scratchpad-editor.css, configBarraInferior.css, adjuntos.css, modal.css, modalConfigGlobal.css, scratchpad-vistaExpandida.css + sentinel-disable fixes en 10+ archivos
- **Gotchas:** VarSense diagnostics son STALE despuÃ©s de editar archivos â€” siempre ejecutar `cssVarsValidator.scanAllDiagnostics` para refrescar. `sentinel-disable-next-line` para fallo-sin-feedback va ANTES de `} catch (error) {`. n-plus-1-query no soporta sentinel-disable (limitaciÃ³n de extensiÃ³n).
- **Sentinel:** 2 MDs de prevenciÃ³n creados: varsense-disable-line (soporte supresiÃ³n) y sentinel-n-plus-1-disable (fix extensiÃ³n)

## 044A-1 â€” Drag-to-move roto: hÃ¡bitos excluidos del Reorder.Group
- **Que:** Los hÃ¡bitos virtuales estaban filtrados fuera de `tareasPrincipalesPendientes` con `!esTareaHabito(t)`, lo que los excluÃ­a del `Reorder.Group` de framer-motion. Se eliminÃ³ el filtro para incluirlos en la lista unificada de drag. Se removiÃ³ el bloque de renderizado separado de hÃ¡bitos fuera del Reorder.Group. Se agregÃ³ guardia en `puedeSerSubtareaDe` para prevenir que hÃ¡bitos participen en jerarquÃ­a padre-hijo (no convertirse en subtarea ni ser padre).
- **Archivos:** useListaTareas.ts, ListaTareas.tsx, jerarquiaTareas.ts
- **Gotchas:** `TareaHabito extends Tarea` â€” son estructuralmente compatibles con el Reorder.Group. El gesto de indentaciÃ³n (arrastrar derecha) necesita bloquearse para hÃ¡bitos via `puedeSerSubtareaDe`.
- **Sentinel:** sentinel-disable-file limite-lineas en jerarquiaTareas.ts (utilidad central, justificado)

## 044A-2 â€” Resolver Agente\prevencion: mejorar extensiones y eliminar falsos positivos
- **Que:** Implementadas 5 mejoras en Code Sentinel y 1 en VarSense para resolver los 6 MDs de prevenciÃ³n acumulados. Todos los falsos positivos corregidos en la raÃ­z (extensiÃ³n), no con disables en el cÃ³digo.
- **Cambios en VarSense:**
  - Soporte `varsense-disable-next-line`, `varsense-disable-line`, `varsense-disable`/`varsense-enable` (bloques) en diagnosticProvider.ts
  - Aplicado a landing.css (35vh) y encabezado-botones.css (15vh)
- **Cambios en Code Sentinel:**
  - `n-plus-1-query`: ahora verifica sentinel-disable tanto en la lÃ­nea del loop como en la lÃ­nea del query (antes solo en el loop)
  - `exec-sin-escapeshellarg`: soporte sentinel-disable-next-line + rango de type-hint expandido a 50 lÃ­neas
  - `undefined-class-constant`: regex ampliada a Unicode (\u00C0-\u024F) para soportar Ã‘/Ã‰/Ã en constantes PHP
  - `api-response-mismatch`: agregado 'success' a CLAVES_ERROR en apiContractIndexer para que `['success'=>false,'error'=>'...']` se clasifique como error response y no oculte el success response real
- **Archivos:** diagnosticProvider.ts (VarSense), gloryQualityRules.ts, phpSecurityRules.ts, gloryConstantRules.ts, apiContractIndexer.ts (Sentinel), notasService.ts (quitados 3 sentinel-disable), landing.css, encabezado-botones.css, GitCommandRunner.php (formato disable corregido)
- **Gotchas:** La causa raÃ­z del falso positivo api-response-mismatch era que `['success'=>false,'error'=>'...']` no se reconocÃ­a como error response porque 'success' no estaba en CLAVES_ERROR. Esto hacÃ­a que el indexer tomara el response de error como el real y nunca viera el de Ã©xito.
- **Sentinel:** PrevenciÃ³n resuelta â€” 6 MDs eliminados

## 044A-12+044A-11 â€” Fix subtareas fantasma en hÃ¡bitos y drag abriendo modal
- **Que:** Dos bugs relacionados. (1) `reordenarTareas` persistÃ­a tareas virtuales de hÃ¡bitos (IDs negativos de `useHabitosComoTareas`) en localStorage como tareas reales. Cada reorder multiplicaba las copias, causando "0/56" subtareas fantasma con el nombre del hÃ¡bito. (2) Los hÃ¡bitos tenÃ­an `dragListener={true}` en Reorder.Item, lo que activaba el gesto de drag y al soltar disparaba el click handler que abrÃ­a el modal de configuraciÃ³n.
- **Archivos:** useTareas.ts (filtro IDs negativos en reordenarTareas), useDashboard.ts (cleanup one-time de datos corrompidos al cargar), ListaTareas.tsx (dragListener=false para hÃ¡bitos + import esTareaHabito), useListaTareas.ts (actualizaciÃ³n comentario)
- **Gotchas:** La multiplicaciÃ³n ocurre porque `handleReorder` construye la lista desde `pendientes` que incluye `tareasConHabitos` (virtual + real). Las copias almacenadas aparecen como subtareas vÃ­a `tareas.filter(t => t.habitoId === habito.id)` y se re-incluyen en cada reorder subsecuente. TambiÃ©n corrompe `parentId` de tareas reales a IDs negativos.
- **Sentinel:** No aplica (regla nueva no necesaria â€” el bug es lÃ³gico, no detectectable por patrÃ³n estÃ¡tico)

## 044A-13 â€” Nueva prioridad "Muy baja" en todos los contextos
- **Que:** Nuevo nivel de prioridad `muy_baja` (tareas) y `Muy Baja` (hÃ¡bitos) aÃ±adido a todo el stack. Color gris mÃ¡s claro que "Baja" (#4b5563 dark, #9ca3af light). Peso de ordenamiento: -100 en tareas, 1 en subtareas/hitos (mÃ­nimo). TambiÃ©n se corrigieron 3 errores pre-existentes (unused var en ListaTareas, return types incompletos en TareaBadges y useTablaHabitos).
- **Archivos:** types/dashboard.ts, nivelesConfig.tsx, useOrdenarTareas.ts, variables.css, selectorNivel.css, badgeInfo.css, tabla.css, formulario.css, validadores.ts, MensajesService.php, accionesIA.ts, dataService.ts, useTareaMenu.tsx, useListaTareasLogica.ts, ListaSubtareas.tsx, ListaTareasHabito.tsx, useListaHitos.ts, useHabitosComoTareas.ts, useOrdenarHabitos.ts, TareaBadges.tsx, useTablaHabitos.ts, BadgeInfo.tsx, ListaTareas.tsx, Schema.php, AIApiController.php, mcp/types/dashboard.ts, mcp/tools/tareas.ts, mcp/tools/habitos.ts (28 archivos)
- **Gotchas:** El MCP server tenÃ­a `NivelPrioridad` sin `muy_alta` â€” se aÃ±adiÃ³ tambiÃ©n. El schema DB (enum) necesita ALTER TABLE en producciÃ³n para aÃ±adir los nuevos valores al enum. Pre-existÃ­an 3 errores TS (unused var tareasHabitoPendientes, switch sin default return) que se corrigieron en este commit.
- **Sentinel:** No aplica

## 044A-14 â€” AuditorÃ­a sentinel-disable-next-line + emojis a SVG + mejora precisiÃ³n Code Sentinel
- **Que:** (1) Reemplazados 14 emojis/simbolos Unicode por iconos SVG de lucide-react en 7 archivos JSX (ModalLimiteAlcanzado, ModalLogin, ListaSubHabitos, TablaComparacion, ModalDetalleRuta, CabeceraArbitraje, PaginaPruebaIsland). (2) Mejorada regla emoji-en-codigo de Code Sentinel para no flaggear emojis dentro de string literals ni comentarios â€” eliminadas 7 supresiones innecesarias. (3) Mejorada regla objeto-mutable-exportado: look-ahead extendido de 5 a 100 lineas para detectar `as const satisfies` en objetos grandes, y exclusion de nombres con 'Definition' â€” eliminadas 11 supresiones innecesarias. Total: ~25 sentinel-disable-next-line eliminados.
- **Archivos:** ModalLimiteAlcanzado.tsx, ModalLogin.tsx, ListaSubHabitos.tsx, TablaComparacion.tsx, ModalDetalleRuta.tsx, CabeceraArbitraje.tsx, PaginaPruebaIsland.tsx, datosIniciales.ts, accionesIA.ts, useAuth.ts, geminiCaloriasService.ts, ModalInspeccionIA.tsx, nivelesConfig.tsx, opcionesMenuHabito.tsx, PricingBlock.tsx, HeroBlock.tsx, FeaturesBlock.tsx, .agent/code-sentinel (staticAnalyzer.ts, reactComponentRules.ts)
- **Gotchas:** Sentinel flaggeaba emojis en comentarios. La regex de string stripping usa delimiters alternados.
- **Sentinel:** Precision mejorada directamente en esta tarea (emoji-en-codigo y objeto-mutable-exportado)

## 044A-15+044A-16 â€” Boton entornos badge->ghost + centrado contenedores panel grupos
- **Que:** (1) Cambiado el boton del selector de entornos de `variante="badge"` a `variante="ghost"` â€” badge no es para botones con texto. (2) Agregado `display: inline-flex` a `.selectorEntornos` y `.panelGruposFb__columnasContenedor` para que se alineen correctamente como flex items dentro de `.seccionAcciones`. El problema era que estos contenedores div eran block-level dentro de un flex parent, a diferencia de `.selectorBadgeContenedor` que ya tenia `inline-flex`.
- **Archivos:** SelectorEntornos.tsx, panelGruposFb.css
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 044A-17 â€” Gestionar categorias: fallback emoji a SVG
- **Que:** EditorCategorias.tsx tenia fallback `<span>{nombre}</span>` cuando el icono lucide no se encontraba. Datos legacy con emojis Unicode mostraban emoji raw. Cambiado fallback a `<FolderOpen />` SVG, consistente con FilaGrupo y PanelGruposFb que ya lo tenian.
- **Archivos:** EditorCategorias.tsx
- **Gotchas:** El codigo del editor y tabla ya usa lucide desde 034A-13/034A-14. Solo el fallback del editor mostraba emojis legacy.
- **Sentinel:** No aplica

## 044A-18 â€” Ocultar secciones de config de plugins desactivados
- **Que:** ModalConfiguracionGlobal.tsx mostraba todas las secciones del sidebar (gruposFb, panelIA, etc.) sin importar si el plugin asociado estaba activo. Se aÃ±adiÃ³ mapa SECCION_PLUGIN_MAP que asocia IDs de secciÃ³n a IDs de plugin, y se filtra SECCIONES_SIDEBAR y GRUPOS dentro del componente usando usePluginsStore. Las secciones sin plugin asociado siempre se muestran; las que dependen de un plugin solo aparecen si ese plugin estÃ¡ activo.
- **Archivos:** ModalConfiguracionGlobal.tsx
- **Gotchas:** GRUPOS a nivel de mÃ³dulo quedÃ³ sin uso al reemplazarlo por gruposVisibles (computado dentro del componente) â€” se eliminÃ³ para evitar error TS6133.
- **Sentinel:** No aplica

## 044A-19 â€” Espejo del repositorio en github.com/1ndoryu/task
- **Que:** Configurado repo espejo en https://github.com/1ndoryu/task. Se aÃ±adiÃ³ remote `task`, push inicial de todas las ramas y tags con `--all` y `--tags`, y se configurÃ³ dual push URL en `origin` para que cada `git push origin` envÃ­e a ambos repos (glorytemplate.git + task.git) automÃ¡ticamente. No requiere acciÃ³n manual del usuario.
- **Archivos:** ConfiguraciÃ³n git local (`.git/config`), sin cambios en cÃ³digo fuente.
- **Gotchas:** PowerShell 5 trata stderr de git como NativeCommandError â€” "Everything up-to-date" aparece como error code 1 pero es correcto. La primera llamada `set-url --add --push` reemplaza el default; la segunda aÃ±ade â€” por eso se llaman las dos URLs explÃ­citamente.
- **Sentinel:** No aplica
## 044A-24 — Subhábitos: dedup destruía distintos + limpieza efímera
- **Que:** Dos bugs en la dedup de subhábitos de 044A-22: (1) `onRehydrateStorage` mutaba state directamente sin llamar `setState`, por lo que localStorage conservaba los duplicados y reaparecían cada recarga. (2) La dedup por ID eliminaba subhábitos que eran distintos (diferente nombre) pero compartían ID por colisión de `Date.now()`. Ahora reasigna IDs nuevos a subhábitos con colisión de ID pero nombre diferente, y fuerza `setState` post-rehydrate para persistir los cambios.
- **Archivos:** habitosStore.ts (setHabitos, onRehydrateStorage)
- **Gotchas:** `onRehydrateStorage` de Zustand recibe el state ya mergeado pero mutaciones directas no disparan `subscribe()` → persist no escribe a localStorage. Solución: `setTimeout(() => useHabitosStore.setState(...), 0)` — el timeout asegura que el store ya está completamente inicializado.
- **Sentinel:** No aplica
## 044A-20 â€” Regla Sentinel para detectar TODOs y pendientes en cÃ³digo
- **Que:** Nueva regla `todo-pendiente` en Code Sentinel que detecta marcadores TODO, FIXME, HACK, PENDIENTE y XXX en comentarios de cÃ³digo. Severidad: hint (para no bloquear desarrollo, pero hacer visible la deuda tÃ©cnica). Aplica a .php, .ts, .tsx, .js, .jsx, .css, .rs. No aplica a .md (documentaciÃ³n). Regex busca marcadores precedidos de delimitadores de comentario (//, /*, #, <!--).
- **Archivos:** .agent/code-sentinel/src/config/defaultRules.ts, .agent/code-sentinel/src/config/ruleRegistry.ts
- **Gotchas:** Ninguno â€” es una regla regex simple con `porLinea: true`.
- **Sentinel:** La regla ES la mejora de Sentinel

## 044A-21 â€” Task repo: solo rama main + submodulos formales
- **Que:** (1) Creado `.gitmodules` con 5 entradas (Glory, code-sentinel, varsense, coolify-manager-rs, coolify-manager) â€” los gitlinks ya existÃ­an (mode 160000) pero sin `.gitmodules` los submodulos no eran formales. (2) Force-pusheado glory-react-logic como main a task. (3) Cambiado default branch de task a main via GitHub API. (4) Eliminadas 19 ramas del remote task (Kura, Padel, agency, etc.), dejando solo main. (5) Configurado refspec `refs/heads/glory-react-logic:refs/heads/main` para que `git push task` mapee automÃ¡ticamente. (6) Eliminado task de los push URLs de origin (por el rename de rama, necesita push separado).
- **Archivos:** .gitmodules (nuevo), .git/config (refspec task)
- **Gotchas:** `.agent/coolify-manager` (sin -rs) tambiÃ©n existÃ­a como gitlink â€” no omitirlo. GitHub API para default branch: PATCH /repos/{owner}/{repo} con body `{"default_branch":"main"}`.
## 044A-23 — README detallado del proyecto
- **Que:** README completo con hero SVG del dashboard, descripción de 20+ features (tareas, hábitos, proyectos, notas, time tracker, sync en tiempo real, encryption, backups, plugins, themes, teams, activity log, notificaciones push, pagos Stripe, AI, mobile, attachments, drag&drop, page builder), sección de plugin system con API, tech stack table, estructura del proyecto, endpoints REST, getting started, y sección de desarrollo. En inglés (README público del repo open source).
- **Archivos:** README.md
- **Gotchas:** El SVG es un screenshot del dashboard (1140x635), no un logo — se usa como hero image con `width="100%"`.
- **Sentinel:** No aplica- **Sentinel:** No aplica

## 044A-22  Fix cientos de subhábitos en panel de ejecución
- **Que:** Triple fix defensivo para prevenir y limpiar subhábitos duplicados/descontrolados: (1) Deduplicación por ID en `onRehydrateStorage` al rehidratar desde localStorage. (2) Deduplicación en `setHabitos` al recibir datos del servidor vía sync HTTP/WebSocket. (3) Deduplicación en `useHabitosComoTareas` al generar tareas virtuales (safety net en renderizado). Además: límite de 50 subhábitos por hábito en store y en rehydrate. Mejor generación de IDs (`Date.now() * 1000 + random`) para evitar colisiones de Date.now() en llamadas rápidas.
- **Archivos:** App/React/stores/habitosStore.ts (onRehydrateStorage, setHabitos, crearSubHabito), App/React/hooks/useHabitosComoTareas.ts (tareasConSubtareas memo)
- **Gotchas:** La `onRehydrateStorage` de Zustand muta el state in-place pero no garantiza write-back a localStorage inmediato  por eso la deduplicación también se añadió en `setHabitos` que es el entry point cuando llegan datos del servidor. El problema original puede haber sido causado por: colisiones de Date.now(), estados corruptos en localStorage acumulados pre-253A-1, o merges de datos duplicados en sync.
- **Sentinel:** No aplica
