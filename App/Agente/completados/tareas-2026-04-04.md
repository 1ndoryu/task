# Tareas completadas — 2026-04-04

## 034A-14+034A-15 — Sistema de entornos completo (backend + frontend + extension)
- **Que:** Sistema de entornos para grupos de Facebook. Backend: 3 tablas nuevas (entornos, overrides, config), repositorio, controlador REST. Frontend: servicio, store con entornoActivoId y routing de overrides, hook useEntornos, componente SelectorEntornos, integración en PanelGruposFb. Extension: syncEnvironmentsToApi, backendId mapping, override sync con fbGroupId, activación de entorno backend.
- **Archivos:** Schema.php, EntornosGruposFbRepository.php (nuevo), EntornosGruposFbApiController.php (nuevo), GruposFbApiController.php, GruposFbRepository.php, gruposFbService.ts, gruposFbStore.ts, useEntornos.ts (nuevo), SelectorEntornos.tsx (nuevo), PanelGruposFb.tsx, panelGruposFb.css, background.ts (extension), types.d.ts (extension), dashboard.ts (extension)
- **Gotchas:** OverrideGrupoFb tiene campos nullable que no coinciden con GrupoFb — se resolvió usando los datos originales en vez del override tipado. Extension usa string IDs vs backend int IDs — resuelto con campo backendId.
- **Sentinel:** No aplica (feature nueva)

## 034A-17 — Config duración publicado
- **Que:** UI para configurar cuántas horas dura un "marcado como publicado" en SeccionConfigGruposFb. Backend ya tenía config table + endpoint desde 034A-14. Se agregó estado publicadoHoras al hook y sección con input numérico al componente.
- **Archivos:** useSeccionConfigGruposFb.ts, SeccionConfigGruposFb.tsx
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 034A-18 — Prompt IA por entorno
- **Que:** Cada entorno de la extensión tiene su propio prompt de IA. Se lee primero del entorno activo, fallback a textarea/localStorage. AI classification results ahora usan updateGroupField para respetar overrides de entorno.
- **Archivos:** types.d.ts (extension, +aiPrompt), dashboard.ts (extension), dashboard.html (extension)
- **Gotchas:** Ninguno
- **Sentinel:** No aplica

## 034A-20 — EditorCategorias fixes visuales
- **Que:** Modal sin padding propio (claseContenido modalContenido--sinPadding), padding interno en editorCategorias. Lista sin overflow-y: auto para que pickers no se recorten. Color picker 4 columnas con muestras 26px. Clase CSS modalContenido--sinPadding reutilizable.
- **Archivos:** PanelGruposFb.tsx, modal.css, panelGruposFb.css
- **Gotchas:** El overflow-y: auto de __lista cortaba los pickers absolute — era la causa real del "selector aplastado"
- **Sentinel:** No aplica

## 034A-19 — Sentinel + VarSense cleanup masivo
- **Que:** Sentinel: 128 → 13 violaciones (8 emoji-en-codigo pendientes de SVG, 3 n-plus-1-query sin soporte disable, 1 Info, 1 Hint). VarSense: 79 → 2 (falsos positivos viewport units). Reemplazo masivo de valores hardcodeados CSS → tokens del design system. Nuevos tokens añadidos: tamanoDetalle, espacioHairline, espacioLgXl, radioSutil, radioMedio, radioSmMd, radioGrande, mapa-calor-tamano-celda, estadoMuyAltaRgb.
- **Archivos:** variables.css (tokens), panelGruposFb.css, inline-fix.css, panelIA.css, configuracionModerna-tareasCompactas.css, landingAnimations.css, scratchpad-modalNotas.css, mapaCalor.css, badgeInfo.css, subhabitos.css, bottomSheetCreacion.css, modalVersiones.css, selectorFechaCalendario.css, scratchpad-editor.css, configBarraInferior.css, adjuntos.css, modal.css, modalConfigGlobal.css, scratchpad-vistaExpandida.css + sentinel-disable fixes en 10+ archivos
- **Gotchas:** VarSense diagnostics son STALE después de editar archivos — siempre ejecutar `cssVarsValidator.scanAllDiagnostics` para refrescar. `sentinel-disable-next-line` para fallo-sin-feedback va ANTES de `} catch (error) {`. n-plus-1-query no soporta sentinel-disable (limitación de extensión).
- **Sentinel:** 2 MDs de prevención creados: varsense-disable-line (soporte supresión) y sentinel-n-plus-1-disable (fix extensión)

## 044A-1 — Drag-to-move roto: hábitos excluidos del Reorder.Group
- **Que:** Los hábitos virtuales estaban filtrados fuera de `tareasPrincipalesPendientes` con `!esTareaHabito(t)`, lo que los excluía del `Reorder.Group` de framer-motion. Se eliminó el filtro para incluirlos en la lista unificada de drag. Se removió el bloque de renderizado separado de hábitos fuera del Reorder.Group. Se agregó guardia en `puedeSerSubtareaDe` para prevenir que hábitos participen en jerarquía padre-hijo (no convertirse en subtarea ni ser padre).
- **Archivos:** useListaTareas.ts, ListaTareas.tsx, jerarquiaTareas.ts
- **Gotchas:** `TareaHabito extends Tarea` — son estructuralmente compatibles con el Reorder.Group. El gesto de indentación (arrastrar derecha) necesita bloquearse para hábitos via `puedeSerSubtareaDe`.
- **Sentinel:** sentinel-disable-file limite-lineas en jerarquiaTareas.ts (utilidad central, justificado)

## 044A-2 — Resolver Agente\prevencion: mejorar extensiones y eliminar falsos positivos
- **Que:** Implementadas 5 mejoras en Code Sentinel y 1 en VarSense para resolver los 6 MDs de prevención acumulados. Todos los falsos positivos corregidos en la raíz (extensión), no con disables en el código.
- **Cambios en VarSense:**
  - Soporte `varsense-disable-next-line`, `varsense-disable-line`, `varsense-disable`/`varsense-enable` (bloques) en diagnosticProvider.ts
  - Aplicado a landing.css (35vh) y encabezado-botones.css (15vh)
- **Cambios en Code Sentinel:**
  - `n-plus-1-query`: ahora verifica sentinel-disable tanto en la línea del loop como en la línea del query (antes solo en el loop)
  - `exec-sin-escapeshellarg`: soporte sentinel-disable-next-line + rango de type-hint expandido a 50 líneas
  - `undefined-class-constant`: regex ampliada a Unicode (\u00C0-\u024F) para soportar Ñ/É/Á en constantes PHP
  - `api-response-mismatch`: agregado 'success' a CLAVES_ERROR en apiContractIndexer para que `['success'=>false,'error'=>'...']` se clasifique como error response y no oculte el success response real
- **Archivos:** diagnosticProvider.ts (VarSense), gloryQualityRules.ts, phpSecurityRules.ts, gloryConstantRules.ts, apiContractIndexer.ts (Sentinel), notasService.ts (quitados 3 sentinel-disable), landing.css, encabezado-botones.css, GitCommandRunner.php (formato disable corregido)
- **Gotchas:** La causa raíz del falso positivo api-response-mismatch era que `['success'=>false,'error'=>'...']` no se reconocía como error response porque 'success' no estaba en CLAVES_ERROR. Esto hacía que el indexer tomara el response de error como el real y nunca viera el de éxito.
- **Sentinel:** Prevención resuelta — 6 MDs eliminados

## 044A-12+044A-11 — Fix subtareas fantasma en hábitos y drag abriendo modal
- **Que:** Dos bugs relacionados. (1) `reordenarTareas` persistía tareas virtuales de hábitos (IDs negativos de `useHabitosComoTareas`) en localStorage como tareas reales. Cada reorder multiplicaba las copias, causando "0/56" subtareas fantasma con el nombre del hábito. (2) Los hábitos tenían `dragListener={true}` en Reorder.Item, lo que activaba el gesto de drag y al soltar disparaba el click handler que abría el modal de configuración.
- **Archivos:** useTareas.ts (filtro IDs negativos en reordenarTareas), useDashboard.ts (cleanup one-time de datos corrompidos al cargar), ListaTareas.tsx (dragListener=false para hábitos + import esTareaHabito), useListaTareas.ts (actualización comentario)
- **Gotchas:** La multiplicación ocurre porque `handleReorder` construye la lista desde `pendientes` que incluye `tareasConHabitos` (virtual + real). Las copias almacenadas aparecen como subtareas vía `tareas.filter(t => t.habitoId === habito.id)` y se re-incluyen en cada reorder subsecuente. También corrompe `parentId` de tareas reales a IDs negativos.
- **Sentinel:** No aplica (regla nueva no necesaria — el bug es lógico, no detectectable por patrón estático)
